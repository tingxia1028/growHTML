# G-A3b — managed-gateway CLIENT wiring (login/balance/token) — BUILD SPEC (approve-with-changes)

Status: Plan → adversarial review → **APPROVE-WITH-CHANGES** (verified foundation + 5 deltas, 2 blockers).
**HEADLINE: ~80% of the managed gateway is ALREADY SHIPPED + green** — do NOT rebuild it. The managed
provider (`src/ai/managed.ts`), the full mock gateway (`src/gateway/*`, 2720 LOC), the credits ledger
(hold/settle/refund + idempotency), the modality×vendor×model pricing, the V-1 vision gate, the REV-2
privacy gate, and unified adaptive-note routing are DONE. **90 gateway/managed tests green; baseline
262f/2677t.** roadmap.md:93 = G-A1–A3a shipped. G-B/G-C (real hosted endpoint, real auth/SMS, real
payments, real vendor fan-out, usage-based invoicing) = EXTERNAL-gated, DEFERRED.

The ONLY remaining LOCAL/CI-verifiable slice: turn the managed provider from a **disabled placeholder**
(`AiProvidersSection.tsx:363,368,432-433` "托管积分即将上线（G-A3b），暂不可选") into a **selectable,
logged-in, balance-showing** provider — client wiring only.

## VERIFIED foundation (cite; do NOT rebuild)
- `ManagedProvider` (`src/ai/managed.ts`): `kind:"managed"` (:196), typed errors (`ManagedNotConfiguredError`:41 / `InsufficientCreditsError`:51 / `ManagedAuthError`:65 / `ManagedGatewayError`:73), SSE reader (:120), `capabilities.vision:false` (:194), registered `index.ts:115-122` with `getSessionToken: () => env.STUDY_VAULT_MANAGED_TOKEN ?? null` (:120 — the env-only read to REPLACE). **baseUrl is NOT hardcoded**: `managed.ts:223` ← `this.deps.gatewayBaseUrl` ← `env.STUDY_VAULT_MANAGED_GATEWAY_URL` (`index.ts:119`).
- Mock gateway (`src/gateway/*`): `createGatewayApp`(`app.ts:94`), `CreditsLedger` hold/settle/refund+idempotency(`ledger.ts:149/173/200`), `Pricing`+`DEFAULT_PRICING`(`pricing.ts:86/183`), `MockSmsSender`(`auth.ts:80`), `MockPaymentAdapter`(`payments.ts:36`), `InMemoryLedgerStore`(`ledgerStore.ts:106`). Routes: `/auth/request-code|verify|refresh`, `/me/balance`(:186), `/agent/stream`(:217), `/topup/create`(:341). `/auth/verify` issues BOTH accessToken(1h) + refreshToken (`app.ts:168`); `/auth/refresh`(:174) rotates.
- Vision gate: `ai.ts:116/136` `!capabilities.vision → VisionUnsupportedError`; pre-flight `assertVisionSupported` from the chat route (`app.ts:1413`). Privacy: managed stripped at `applyProfileContextGate`(`ai.ts:185-193`, managed :189) + `composeAutoContext`(`autoContext.ts:91`). BOTH already fire for managed — NO new gate code.
- Adaptive-note: `app.ts:302-307` emits plain `{role:"assistant",content}`, no special managed path. Chat works: `/api/chat/stream`(`app.ts:1400`) resolves the provider per-request + streams (managed answers here; the agent-loop route `/api/agent/stream` 501s for managed but the UI falls back to plain chat, `agent.ts:6`).
- KeyStore: `SafeStorageKeyStore`(`keyStore.ts:76`) stores base64 ciphertext in `ai-provider-keys.json` (encrypted, NEVER `ai-providers.json`). `EnvOnlyKeyStore`(`keyStore.ts:146`, the dev/web/CLI/vitest default via `createDefaultKeyStore`:181-201) — `setKey` throws `KeyNotPersistableError`.
- `providerForEntry`(`aiProviders.ts:261-285`): a key-chain branch ONLY for `kind==="http"`(:262-283); EVERY other kind incl. managed falls to `createRegisteredProvider(entry.preset??entry.id,{env})`(:284) — so NO stored-token path exists today.

## THE 5 FOLDED DELTAS
1. **[BLOCKER] Store the session token in safeStorage (the BYOK-key path), NEVER `ai-providers.json`; handle `EnvOnlyKeyStore` like BYOK does.** The managed session token (access + refresh) rides `keyStore.setKey/getKey` (encrypted at rest via `SafeStorageKeyStore`). In `EnvOnlyKeyStore` mode (dev/web/vitest) `setKey` throws → managed LOGIN-persist is packaged-desktop-only, exactly like BYOK keys; dev/web falls back to `env.STUDY_VAULT_MANAGED_TOKEN`. MIRROR how the BYOK section already surfaces the non-persistable case (find it in `AiProvidersSection.tsx` — the key-input handling) so managed degrades identically (a clear "本模式下不可持久,用环境变量" affordance, not a crash).
2. **[BLOCKER] Add a `managed` branch to `providerForEntry`(`aiProviders.ts:284`)** that constructs the managed provider with `getSessionToken: () => storedToken ?? env.STUDY_VAULT_MANAGED_TOKEN` (+ `gatewayBaseUrl` from the stored entry ?? env). Land it ATOMICALLY-BEFORE the radio-enable (delta 3) so an enabled managed is actually functional. KEEP `managed.test.ts:324-345` (env-token registry roundtrip) GREEN — the env path stays a fallback, not removed.
3. **[SHOULD-FIX] No-token activation → a 登录 affordance, not a stream error.** Activating managed with no token → `requireConfig()` throws `ManagedNotConfiguredError` → surfaces as an in-band SSE `error` event (`app.ts:1433`) — ugly. The settings section (and, minimally, the chat empty/error state) must show a "登录托管积分" prompt when managed is active but unconfigured. At minimum: the settings sub-panel shows login when no token; do NOT enable-and-strand.
4. **[SHOULD-FIX] UserMenu 账户/积分 (`UserMenu.tsx:202`, `disabled:true` "等待托管上线") — EXPLICITLY DEFER** to a G-A3b.4 follow-up (keep this slice focused on the settings section). Leave a code note that balance/login lives in the AI-Providers settings for now; surfacing it in the user menu is the deferred polish. (Do NOT silently leave it dead with no rationale.)
5. **[SHOULD-FIX] Refresh-token / 401 flow.** `/auth/verify` returns access(1h)+refresh; store BOTH via keyStore. The `ManagedGatewayClient` (settings reads: getBalance/topup) does refresh-on-401 (call `/auth/refresh` once, persist the rotated tokens, retry). The PROVIDER-path auto-refresh (chat using an expired access token → auto-refresh) is a DOCUMENTED follow-up (the provider reads `getSessionToken()` synchronously; under the deterministic mock + fixed clock, tokens don't expire mid-test). Note it honestly; don't silently ship hourly-logout as "done".

## Commit sequence (each compiles + FULL vitest green; baseline 262f/2677t)
**G-A3b.1 — token-through-config + the gateway client (the load-bearing foundation; lands BEFORE the radio-enable).**
- Server (`aiProviders.ts`): a `managed` branch in `providerForEntry` (delta 2) — read the stored session token via `keyStore.getKey` (delta 1) + the stored baseUrl, inject `getSessionToken: () => storedToken ?? env.STUDY_VAULT_MANAGED_TOKEN`. Preserve env fallback + precedence.
- Client (`entityClient.ts` + `settingsIo.ts`): a thin `ManagedGatewayClient` (`requestCode`/`verify`/`getBalance`/`createTopup` + refresh-on-401, delta 5) mapping 1:1 onto the shipped gateway routes; exposed through `settingsIo`(:23,84) so the section calls the seam, never raw fetch. Token persisted via keyStore (delta 1).
- Tests: (a) a stored managed entry → `ManagedProvider` whose `getSessionToken()` returns the keyStore token, not env; (b) no token → `ManagedNotConfiguredError` pre-fetch; (c) env override precedence intact + `managed.test.ts:324` still green; (d) each gateway-client method against the mock gateway (`bootGateway` `managed.test.ts:53-99`, fixed clock): signup bonus balance=1000, code login via `MockSmsSender`, top-up via `MockPaymentAdapter`+`/topup/mock-notify` idempotent, refresh-on-401 rotates+retries; (e) `EnvOnlyKeyStore` setKey-throws path degrades (no crash).

**G-A3b.2 — enable the radio + the 托管 settings sub-panel (atomic-after .1's token store).**
- Client (`AiProvidersSection.tsx`): remove `isManaged` from `pickDisabled`(:368) + the "即将上线" note(:432-433). Add a managed sub-panel: phone→code→verify login (mock), balance + recent-ledger display, top-up buttons (mock QR/stub), gateway-URL field, and the no-token→登录 affordance (delta 3). Activation uses the EXISTING `saveActiveProvider`(:250) — no new activation path. Degrade-not-disappear (logged-out/zero-balance = selectable-with-reason). REUSE existing `.settings-ai-managed-note`/`.settings-ai-*` classes (NO styles.css edit).
- Tests: RTL with `setSettingsIoForTests` fake — (a) managed radio ENABLED + `activate("managed")` calls `saveActiveProvider`; (b) balance renders; (c) login flow updates state; (d) top-up calls the seam; (e) no-token shows 登录, not an error; (f) update `SettingsHub.test.tsx`/`AiProvidersSection.test.tsx` expectations that managed was disabled.

**G-A3b.3 — vision-gate + adaptive-note e2e (verification lock; NO new gate code).**
- Tests: (a) an image chat request under active managed → `VisionUnsupportedError` → clean 400 (`assertVisionSupported`, `capabilities.vision:false`); (b) e2e (`e2e/managed-gateway.spec.ts`): active provider=managed → the mock gateway (boot an in-process gateway or point at a fake) → user chats → text streams → output routes through `resolveForm`→`getNoteType().render` (proves zero special path); insufficient-balance → `InsufficientCreditsError` surfaces the top-up affordance. Deterministic under the mock + fixed clock.

## CORE-vs-KIT (verified right): managed = CORE-ai (a `ModelProvider` under the shared seam, gated by V-1/REV-2 core gates — NOT a kit). credits/ledger/auth = EXTERNAL gateway (`src/gateway`, never touches the JSONL vault — NOT a vault entity/type). settings = CORE-client (the shipped `ai-providers` section + `settingsIo` seam). No new entity/type/kit.

## Collision: touches `src/server/services/aiProviders.ts`, `src/client/data/entityClient.ts`, `src/client/settings/settingsIo.ts`, `src/client/settings/AiProvidersSection.tsx`, + tests + a new e2e. Codex's files (styles.css, SlashPalette.tsx, workspace/anchorViews.*, workspace/actionIcons.ts, FileTree.tsx) UNTOUCHED. Do NOT touch `src/gateway/*` or `src/ai/managed.ts` except (if unavoidable) the one construction seam — the foundation is banked.

## Gates: tsc 0 · full vitest green (baseline 262f/2677t — grow, never regress; keep the 90 gateway/managed green) · build · e2e managed-gateway + the existing suites. Commit G-A3b.N own sequence, DON'T push (I verify+land), DON'T touch docs (report TEXT blocks). SURGICAL git. SECURITY: the session token is a bearer secret — encrypted via keyStore (never plaintext JSON), sent ONLY to the stored gateway origin, never logged.

## Deferred (external/not-CI): real hosted endpoint + Postgres/Redis; real SMS(阿里云/腾讯云) + real JWT rotation; real payments(微信/支付宝 QR + webhook sig); real vendor fan-out (DeepSeek + image adapters = G-B); usage-based invoicing; rate limits; 月费 auto-renew (个体工商户资质); managed VISION (flip `capabilities.vision` when it lands); provider-path token auto-refresh; UserMenu 账户/积分 balance surfacing (G-A3b.4 polish).
