# V-2 — 拍错题 (mistake-photo) KIT — BUILD SPEC (approve-with-changes)

Status: Plan → adversarial review → **APPROVE-WITH-CHANGES** (6 deltas folded). The killer education
flow: **拍照错题 → VLM 抽取 → 预览 → 存错题本**, fully offline-deterministic under the mock. Rides the
JUST-SHIPPED V-1 vision seam. Register-only kit + a small CORE foundation extending the structured seam.

## CORE-vs-KIT (kernel law)
- **CORE (the foundation, ~40 lines of DRY glue over fully-built V-1 machinery):** the structured/kit
  generation seam does NOT carry an image today (`generateForPrompt` builds a bare string
  `src/kits/structured.ts:66`; `generateKitContent` `src/server/services/ai.ts:184` never resolves vision).
  Extend it: carry an image ContentPart into the structured message + JIT resolve/gate for vision providers.
  Domain-neutral (extends V-1's provider seam), NO kit knowledge, NO new entity.
- **KIT (V-2, register-only, mirror teachback):** `src/kits/mistake-photo/` — the VLM extract prompt, the
  capture command/affordance, the deterministic mockContent. REUSES the CORE `mistake` contentType.

## THE 6 FOLDED DELTAS (MANDATORY)
1. **Conditional message branch — bare string when no images (delta forbids always-array).** In `generateForPrompt` + the form-router branch (`src/kits/structured.ts:66,130`): `images?.length ? [...imageParts, {type:"text", text: prompt.build(input)}] : {role:"user", content: prompt.build(input)}`. WHY LOAD-BEARING: `ai.test.ts`'s `capturingProvider` reads `request.messages[1].content` as a STRING and calls `.startsWith`/`.toContain` (`src/server/services/ai.test.ts:52,175,187`) — an always-array wrap throws → the whole REV-2/ACTION-2a suite fails. Text-only stays byte-identical.
2. **Image-present-only gate — early-out no-op when images absent.** In `generateKitContent` (`ai.ts:184`), the resolve+gate runs ONLY when `input.images?.length > 0` (mirror `resolveVisionRequest`'s `if (!anyImage) return` early-out at `ai.ts:78`). WHY: `capturingProvider` sets `vision:false` for ALL kinds (`ai.test.ts:48`); an unconditional `!capabilities.vision → throw` would 400 EVERY existing text kit test (textbook/review.explain/slash/ACTION-2a). With the guard, blast radius = ZERO.
3. **`resolveVisionRequest` DELEGATES to the extracted `resolveImageParts` (thin wrapper, NOT a second gate copy).** Extract the per-message-array → resolved-parts inner mapping into `resolveImageParts(provider, vault, parts)`; `resolveVisionRequest` (`ai.ts:72-103`) keeps its `anyImage`-scan → gate → `Promise.all` shell and calls the helper per message. PRESERVE both `VisionUnsupportedError` strings: the bare gate AND `Attached image not found: ${assetId}` (`errors.ts:76`→400; contract asserts `/image input/i` at `app.test.ts:288`). Chat regression coverage = route-level `app.test.ts:243-299` (there are NO service-unit tests) — the delegation keeps them green.
4. **`images` is a SIBLING of `GenerateStructuredRequest`, NEVER inside `input`; thread it END-TO-END.** Add `images?: ImageContentPart[]` to `GenerateStructuredRequest` (`src/kits/structured.ts:31` / `src/ai/structured.ts:49`) AND widen `kitGenerateSchema` (`ai.ts:41`) with `images?: z.array(imageContentPartSchema)` AND the client `entityClient.generateStructured` (`entityClient.ts:1403`) input type — ALL THREE together (BLOCKER: zod strips an unknown field silently — a client `images` would vanish with no error). LANDMINE: `images` inside `input` would be spread into template render values (`resolvePrompt.ts:131`) + read by `composeAutoContext` (`autoContext.ts:64`) → `{{images}}` renders `[object Object]`. Keep it a sibling. Thread: client command → `generateStructured(images)` → POST body → `kitGenerateSchema.parse` → `generateKitContent` → resolve → `generateOperationContent` → `generateForPrompt`.
5. **The 3 aggregation wirings (register-only one-liners, in the commit list):** `src/kits/index.ts:28` add `...mistakePhotoPrompts` to `kitPrompts` (else `resolvePrompt`→`getKitPrompt` 400s "Unknown promptId"); `src/kits/clientKits.tsx` add `mistakePhotoKit` to `productKits`; NO `contentSpecs` entry + NO server spec touch (mistake is core). These are the teachback precedent, not core-engine changes.
6. **Citation fix:** the array-message builder to mirror is `askAi` in `src/client/commands/registry.ts:281-282` (`images.length ? [...images, {type:"text",text}] : text`), NOT `src/ai/registry.ts` (the provider registry).

## Verified reuse (cite)
- **The `mistake` contentType** is a CORE built-in (`src/core/notes/contentTypes.ts:193-202`, `MISTAKE_CONTENT_TYPE`): REQUIRED `question`/`wrongAnswer`/`correctAnswer` (bare z.string); DEFAULT `retryCount:0`/`mastery:"weak"`; OPTIONAL `exerciseNoteId`/`mistakeReason`/`correction`. Fits photo-extracted mistakes exactly. `generateForPrompt` parses AI output against `spec.schema` (`structured.ts:67`) + enforces `prompt.outputType === contentType` (`:55`). Client render registered (`src/client/notes/mistakeNoteType.tsx`). The kit registers NO spec.
- **The preview loop:** `onGenerated` (`src/client/commands/registry.ts:86`) + `GeneratedDraft` (`registry.ts:35`, `anchorId?`/`sourceId?` BOTH optional :40-41). **Anchor-less precedent:** `generateReviewPackCommand` (`textbook-learning/commands.ts:100-111`) emits `onGenerated({anchorId:undefined, sourceId})` → Save persists unanchored. A photo-origin mistake rides THIS path (preview-then-Save), NOT `autoMaterialize` (which needs a real anchorId, `registry.ts:77`).
- **The kit shape** mirrors `teachbackKit` (`src/kits/teachback/index.tsx:31`). The capture command mirrors `markAsMistakeCommand`/`generateBlock` (`textbook-learning/commands.ts:22-46`). `mockContent` mirrors `markAsMistakePrompt.mockContent` (`markAsMistake.prompt.ts:33-41`, fills all 3 required fields).
- **Photo import:** `importImageBase64` (`entityClient.ts:1208`, V-1) → `{assetId}`; the capture affordance reuses `attachImage`'s `fileToBase64` path (`WorkspaceContext.tsx:1963`). ZERO new asset plumbing.
- **Mock determinism:** `completeStructured` echoes `request.sample` verbatim (`mockProvider.ts:117`), and `generateForPrompt` supplies `mockContent(input)` as the sample (`structured.ts:62`) → the image reaches the mock (proving the wire) but the deterministic fixed mistake rides the sample. Offline e2e.
- **Capability gate (client, degrade-not-disappear):** read `capabilities.vision` via `entityClient.aiProviders()` → active → `.capabilities.vision` (the client cap type has `vision`, `entityClient.ts:646`; the `tools`/`agentAvailable` precedent `WorkspaceContext.tsx:1293-1315`). Affordance STAYS visible; a non-vision send surfaces the clean 400 once.

## Commit sequence (each compiles + full-suite green)
1. **CORE foundation.** `src/kits/structured.ts` (`GenerateStructuredRequest.images?` sibling + the CONDITIONAL array branch delta 1 in generateForPrompt + form-router) · `src/server/services/ai.ts` (extract `resolveImageParts`; `resolveVisionRequest` delegates delta 3; widen `kitGenerateSchema.images?` delta 4; image-present-only resolve+gate in `generateKitContent` delta 2) · `src/client/data/entityClient.ts` (`generateStructured` `images?` delta 4). Test: (chat regression) `app.test.ts:243-299` stay green (delegation byte-safe); (text kit regression) `ai.test.ts` `.startsWith`/`.toContain` still pass (conditional branch); a kit request WITH an image → the built structured message carries an image part (capture-the-wire, mirror `aiSdkProvider.test.ts:231`); `generateKitContent` under mock(vision:true)+image → sample-echoed content; under vision:false+image → `VisionUnsupportedError` 400; text-only kit request byte-identical (regression lock).
2. **Extract prompt + mock projection + prompt aggregation.** `src/kits/mistake-photo/prompts/extractMistake.prompt.ts` (KitPrompt, `outputType:MISTAKE_CONTENT_TYPE`, VLM extract prompt: "从照片抽取 题目/学生的错误答案/正确解法/错因/订正 → question/wrongAnswer/correctAnswer/mistakeReason/correction"; `mockContent` returns a fixed schema-valid mistake) + `prompts/index.ts` + `src/kits/index.ts` `kitPrompts` (delta 5). Test (mirror `teachback/prompts.test.ts`): `build()` names the mistake fields; `mockContent` parses against `mistakeSpec.schema`; outputType routes through `getNoteContentSpec("mistake")`.
3. **Register the kit (ProductKit + capture command) + client aggregation.** `src/kits/mistake-photo/index.tsx` (ProductKit, one member; NO contentSpec) + `commands.ts` (capture command → `ctx.client.generateStructured({promptId:"mistake-photo.extract", contentType:MISTAKE_CONTENT_TYPE, input, images:[{type:"image",assetId}]})` → `onGenerated`) + `src/kits/clientKits.tsx` `productKits` (delta 5). Test (mirror `teachback/register.test.tsx`): kit installs; `mistake` note renders; capture command registers + available.
4. **Capture UI + capability gate.** A file-input affordance (reuse `importImageBase64` → assetId → dispatch the capture command) + the `capabilities.vision` read (degrade-not-disappear, `WorkspaceContext.tsx:1293` precedent) + a command-surface entry (mirror teachback's reachability). Test (component, fake api): pick a photo → dispatches capture with an image ref; on vision:false active provider the affordance stays visible + the 400 surfaces once.
5. **e2e (deterministic mock).** `e2e/mistake-photo.spec.ts` (reuse `vision-chat.spec.ts`'s `PNG_1x1` fixture): 附加 fixture 照片 → 触发 capture → the extracted `mistake` draft appears in preview → Save → a `mistake` note renders (`.tb-mistake` selectors from `mistakeNoteType.tsx`). Fully offline.

## Collision: codex's committed files (styles.css/SlashPalette/anchorViews/actionIcons/FileTree) UNTOUCHED. V-2 = src/kits/* + the structured seam + the generate route. LOW risk.

## Gates: tsc 0 · full vitest green (baseline 254f/2623t) · build · e2e mistake-photo + (regression) vision-chat + agent-tools + a REV-2/ACTION-2a kit test. Commit V2.N own sequence, DON'T push, DON'T touch docs (report TEXT blocks). SURGICAL git (explicit paths, never -A). Kit register-only + core foundation; adaptive-note contract for the mistake output.

## Deferred: PaddleOCR local lane + threshold cost-routing (§4 "optional" — real scope creep, defer entirely); `mistakeSchema.sourceImageAssetId?` (additive optional CORE field — defer unless the card thumbnail is a must-have); V-3 = X2 mobile camera capture; managed-vision (gateway text-SSE-only); per-modality context budget; the audio part.
