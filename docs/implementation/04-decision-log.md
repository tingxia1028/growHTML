# Decision Log

Record durable implementation decisions here. Keep entries short and dated.

## Decisions

| Date | Decision | Rationale | Alternatives Considered | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-07-05 | NOTE-FOCUS-POLISH-001: expanded note polish stays in the shared full-mode note render path; width is owned by CenterView CSS and flashcard/quiz improvements are type CSS, not host branches. | Keeps small/large note rendering unified through `getNoteType().render({mode:"full"})`, while fixing the product issue that focused notes looked like full-width strips and quiz answers wrapped into vertical columns. | Add per-contentType cases in FocusOverlay; add schema/display fields for focus sizing; fork flashcard/quiz host layouts. | Future rich note types should reuse the same CenterView width cap and add only type-local full-mode CSS. |
| 2026-07-04 | LEFT-COLLAPSE-001: the left content sidebar collapses through the IconRail, VS Code-style: clicking the active rail icon hides the left pane; clicking it again reopens; clicking a different rail icon always expands and switches to that pane. The dock model still owns persisted explicit collapse state, but collapsed width is `0` and old dock-local collapse buttons/rails are hidden. No narrow-viewport auto-collapse is restored. | This matches the user's requested mental model and avoids spending recovered width on a second collapsed strip beside the existing activity rail. Keeping the state in the dock layer preserves layout persistence and keeps resize gutters aware of the collapsed boundary. | Add a small collapse button inside the sidebar; restore the old vertical `Sources` rail; auto-collapse on narrow screens. | If mobile/tablet layouts get dedicated shells, treat this as desktop/tablet workspace behavior and use bottom navigation or sheets on phone. |
| 2026-07-04 | I18N-1: locale is a tiny observable store (`src/client/i18n`) plus a field-group-safe workspace prefs slice (`/api/workspace/ui-prefs`), not a third-party i18n runtime and not part of the layout PUT body. Components that call `t()`/`resolveText()` subscribe with `useLocale()` so a Settings radio change rerenders already-mounted chrome immediately. Registry-facing titles accept `LocalizedText` at the contribution boundary, and render/adapters resolve them to strings at the UI edge. | The existing LIB-2 seed already provided typed messages and contribution-facing `LocalizedText`; extending it preserves compile-time missing-translation checks and avoids a new dependency. Keeping locale in its own workspace field group follows the existing onboarding/plugin-prefs single-writer rule, so stale layout saves cannot clobber the language. React does not automatically rerun components that merely call a module function, so explicit `useLocale()` subscriptions are required for live flips. | Store locale only in localStorage (no vault/workspace portability); add locale to `/api/workspace` layout state (violates field ownership and risks lost updates); introduce i18next/react-intl (too heavy for the current typed-message seed); force a whole-app remount on language change (would reset UI state). | Continue I18N-2 panel-deep sweep: AiProvidersSection, operation manager, layer views, speech/profile/trash/settings subpanels, and remaining built-in contribution declarations should move to dictionaries incrementally. |
| 2026-07-04 | E2E-ELECTRON-001: the Electron e2e suite runs on its OWN ephemeral-vault harness (`e2e-electron/harness.ts`) — one pid-stamped OS-temp ROOT per run (globalSetup sweeps only the `growte-e2e-electron-vault-` prefix so the web suite's roots are never touched; globalTeardown deletes with retries) with a FRESH per-SPEC vault subdir, because every spec file boots its own app instance. `_electron.launch` env pins per-spec `STUDY_VAULT_ROOT`, `ELECTRON_DEV=0` (dev mode would attach the window to the user's LIVE Vite session and its real vault), `STUDY_VAULT_AI_PROVIDER=mock`, `STUDY_VAULT_AUTO_BACKUP=0`, `STUDY_VAULT_TRASH_AUTO_PURGE=0`. localStorage isolation comes free: the in-process server binds port 0, so every run is a fresh origin. The harness boot gate dismisses the SHELL-2 onboarding (a fresh vault always auto-opens it in the center slot). Spec-not-product rule held while reconciling pre-R1 drift: removed features were rewritten to today's UX (`section.url-import-box` → Library `+` → 网页/实时打开; select→"Save Note" → select→ask-AI→"Add as note", whose lazy anchor materialization keeps the live-HTML "no anchor" regression pinned; the terminal spec now asserts the PTY surface is OFF the default chrome), never the reverse — zero product-code changes. One real race found and gated in the harness (not product-fixed): `loadSourceData` runs `focus.clear()` on settle, so a guest selection driven before the load resolves is wiped — specs wait for the workspace status dot to reach idle before selecting. | The suite sat at 8 failed / 1 skipped on obsolete selectors and repo-side `.e2e-electron-vault*` dirs — the same vault-hygiene violation class E2E-DEBT-001 repaid for web, plus boot gates that predated the R1 shell. | Reusing `e2e/harness.ts`'s root + prefix (the web setup's stale-sweep would delete a live electron run's vault when the suites run side by side); ONE shared vault for all specs (source leakage across specs breaks "select the first source" determinism); keeping the deep PTY e2e alive inside the PanelMenu popover (a transient overflow surface — multi-step keyboard flows there are flaky by construction); asserting the full "You asked:" echo in card previews (both the Notes row and the in-guest hover card render the CLAMPED per-type card preview — assert the deterministic head line instead). | Un-skip the composer image-pick spec when the manual note-creation UX decision lands (same gate as the web suite's skips); give the terminal a stable home (docked pane/modal), then un-skip the deep PTY flow. |
| 2026-07-04 | UI-LIBRARY-003: Library source rows are compact, title-only, and non-destructive. Visible row metadata is limited to the icon + one-line title; source id/path/type live in the hover tooltip. The row `x` means "remove from this list/close this row" (`removeRecentSourceId` for Recent, panel-local dismissal for Documents), never "delete Source"; destructive source deletion must live behind an explicit menu/trash flow. Library local search uses the same collapsed magnifier + expanded search row pattern as other sidebar panels. | The previous Documents rows looked like folder cards, exposed internal ids in the visual hierarchy, and put a trash button where a misclick could delete real study material. The user's intent was list cleanup, not data deletion, and the shared sidebar rule favors compact scan rows and quiet title bars. | Keep the trash icon but make it softer; hide ids with CSS only; keep the search input permanently in the title row; persist dismissed document rows globally. | If a real "delete source" affordance is reintroduced, route it through an explicit overflow menu or recycle-bin flow with clear destructive copy. |
| 2026-07-04 | SEARCH-1-001: global search is **one shared pure rank core + a services engine + a shell-chrome palette** — `src/core/search/rank.ts` (tiers exact>prefix>word-boundary>substring, case-folded so CJK = plain substring per design §2; recency `updatedAt` desc tiebreak; matched-FIELD report; snippets) is consumed by BOTH the server families (`src/server/services/search.ts` — notes via every spec's `toSearchText` PLUS the note's anchor quotes as extra match fields, so anchors surface through their notes and a quote-only hit snippets the quote; sources via title; sealed/layer visibility rides `listNotes`; cap 20/family) and the client commands family (one capability, one ranking — abstract-recurring-capabilities). `GET /api/search` lands in app.ts AND the direct-transport route table (mobile parity now, not later). The commands family V1 = **navigation only** (`navigateShell` targets: 打开资料库/概念/操作/插件/复习/画像/设置/分层/书签/引导) because every registry action command requires payload/focus context — nav targets are the only entries "safely runnable without required args"; the "/"-passthrough to slash entries rides SC-1. The palette is the SC-0 split (pure `searchEngine.ts` + a dumb-ish `GlobalSearchPalette` over an INJECTED deps seam + a thin `useWorkspace` host), mounted ONCE in WorkspaceShell chrome with the Cmd/Ctrl+K listener inside the component (single mount = single registration; no per-view wiring). Note dispatch reuses the EXISTING focus contract (`focus.setAnchor` on the active source; cross-source = `setActiveSourceId` + a pending-reveal effect that completes when that source's anchors arrive; anchorless = `{type:"note"}` focus — LINKED-NOTE-001). Search IO is its own plain-fetch module (the dataTrust/chat-session carve-out — contended entityClient untouched); new CSS scoped (`globalSearch.css`); all strings via the i18n seed (`searchMessages.ts`). The Library header input STAYS a local section filter (design §1: Cmd+K is the global entry). | The design doc pins the shape (SC-0 palette reuse, toSearchText walk, services seam, no index V1); a second ranking implementation for commands would drift from the server's; the deps-seam split is what makes the palette jsdom-testable without a WorkspaceProvider; anchor quotes as note FIELDS (not a 4th family) matches "anchors surface through their notes" with zero new hit types. | An anchors result family (design says surface through notes); client-side search over ctx data (only the ACTIVE source's notes are client-side — cross-vault needs the server walk); fuse.js / an inverted index now (V1 scale doesn't need it; the seam keeps it swappable); registering the hotkey in WorkspaceShell's own keydown (couples shell to palette state; the component owns its lifecycle); enumerating runnable registry commands via `isAvailable` probes (fires with live focus state — a palette run of e.g. bookmark.add would silently mutate). | SEARCH-2: pinyin+fuzzy (SC-3 shared table), `type:` filters, recent searches (workspace prefs); "/" slash-entry passthrough at SC-1; topbar 搜索 affordance when TopBar frees; perf gate — if the scan exceeds ~50ms at real vault size, add the in-memory inverted map (still no dep); consider surfacing concept hits once the concept capture engine (V1.1) lands. |
| 2026-07-04 | W1-001: chat sessions persist **client-driven over plain CRUD+append** — the SSE transport (`/api/chat/stream`) is byte-untouched and the client appends turns through `/api/chat/sessions/:id/messages`: user msg on send (`onChatHistory` seam), assistant msg on stream end via a NEW `onAssistantDone` command action (the streamed path resolves with the completed message; chunks stay render-only so no partial reply is ever written) or on the non-streamed fallback (`onAssistantMessage`). The F1 slice = **state relocation, not panel extraction**: `useChatSessionDomain` (src/client/chat) owns transcript+sessions behind a FIFO write queue with per-conversation TOKENS (lazy create on first turn binds later appends to the right session even across a mid-flight switch); WorkspaceContext keeps `chatMessages`'s exact shape and gains exactly ONE field (`chatSessions`). `chatSession` joins `entityFileNames` (vault-open ensure + data-trust backup for free) but stays OUT of `vaultEntitySchema` (privacy — the memory/operation precedent, pinned by test); messages carry a server-stamped `ts` (core mirrors the ai ChatMessage rather than importing src/ai — dependency direction). Source switch no longer wipes the chat (§5: switch, not wipe); resume = localStorage last-session id with an explicit-新对话 NONE sentinel; auto title = first user msg, 40 chars, derived server-side (single source, backfilled on append). Chat-session IO uses its OWN transport instance (the dataTrust/speech carve-out) — the contended entityClient is untouched; direct-transport (X2) parity deferred with the rest of the chat lane. | The doc's own shape (§2.2 `useChatSession` "backed by the entity store") — the client is where the completed assistant turn is known (`done` event), the server stream route stays pure transport, and W1 pins "No AI change"; persistence must not add partial-write states; a parallel session owns entityClient/RightSidebarTabs/StudyView-adjacent files, so the domain had to land as new modules with a one-field context seam. | Server-side persistence hooks in `/api/chat/stream` (needs `sessionId` in `chatRequestSchema` — an src/ai wire change W1 forbids, and mixes transport with storage); debounced full-array PATCH auto-save (simpler but rewrites the whole transcript per turn and can persist partials); extracting `<ChatPanel>` now (contended-adjacent StudyView; deferred to W3 where the workspace page needs it anyway); keeping chat state in WorkspaceContext with sessions bolted on (10+ new god-object fields — the exact F1 smell). | W2: attachments UI + `/api/sources/:id/bundle` + multi-source ChatContext (the recorded `attachments` refs become live context); W3: `<ChatPanel>` extraction + the sidebar workspace page; rename UI over the existing PATCH; mid-stream crash still loses the in-flight assistant turn (accepted — revisit if it bites); chat lane into directTransport when X2 mobile lands. |
| 2026-07-04 | E2E-DEBT-001: the web e2e harness is **ephemeral-vault + dedicated-ports by construction** — `e2e/harness.ts` pins API 14177 / client 15173 (far from dev's 4177/5173, `--strictPort` on Vite so a taken port FAILS instead of silently bumping) and a per-run `os.tmpdir()/growte-e2e-vault-<pid>` vault (main-process pid published via `GROWTE_E2E_VAULT_ROOT` env so config, globalSetup/teardown AND worker-process specs all resolve the same path); globalSetup sweeps stale pid-stamped siblings (a hard-killed run leaves one; overlapping runs are impossible on fixed ports), globalTeardown deletes with retries. `reuseExistingServer` stays **false forever** — the reuse path was exactly how test seed data leaked into the REAL `data/vault` (env overrides only apply when Playwright starts the server). Specs import `SERVER` from the harness; no spec hardcodes a port again. Spec-not-product rule enforced while reconciling drift: assertions on removed features (TopBar Document tab, Library kebab, rail Layers/Notes icons, `.layer-group` wrappers, raw contentType badges) were rewritten to today's UX, never the reverse — the one product change was a genuine crash fix (DomReader null-body guard on transitional srcDoc documents). | The old harness bound the dev ports and a repo-side `.e2e-vault`; with the dev server up, `npm run e2e` either collided or (historically, via reuse) seeded the user's real library — the standing mock-data-law violation. Temp-dir vaults + own ports make the leak structurally impossible and let e2e run beside a live session. | Keeping `.e2e-vault` repo-side (still litters the working tree, one config typo from `data/vault`); `reuseExistingServer:true` with env checks (env only applies when Playwright SPAWNS the server — reuse can never be safe); random free ports per run (specs need one importable SERVER constant, and fixed ports make orphan cleanup safe); per-worker vaults (workers=1; the seeding idiom assumes one shared server). | Migrate `e2e-electron` to the harness module + reconcile its pre-R1 selectors (that suite is RED on obsolete UI: `.brand-block`, `.reader-header h2`, top-level Refresh — separate pass); un-skip the 24 pre-existing `test.skip`s as their product decisions land (manual note-creation UX; concepts R7 layout). |
| 2026-07-04 | ACTION-2A-001: envelope truncation priority = selection→doc→learner (learner cut LAST — quotes are unbounded and head-heavy, learner is already compact and highest personalization value/char), whole preamble capped 1500 chars, `[Context]` block prepended as `${preamble}\n\n${prompt}` with EMPTY envelope → prompt verbatim (built-ins returned as the same object — that identity IS the REV-1 byte-compat guarantee). Template namespace = FLAT dotted keys (`values["doc.title"]`, no nested walking) so plain `{{var}}` semantics stay byte-identical; envelope values sit UNDER runtime/declared values. profileContext gate GENERALIZED (prompt-id scoping deleted) with a second independent strip at compose time, and learner is omitted whenever the runtime input already carries profileContext — one weave, never two. Simple ops with no pinned output resolve to the `form-router` sentinel and ride formRouterSchema → routerOutputToNote → registered spec validation (resolveForm's declared-form default downstream); a run-time contentType on such an op is rejected (pins are authoring-time, via the 高级 disclosure in 2b). | The envelope must be safe to attach to EVERY generation: deterministic budget, no prompt regressions for context-free requests, no profile duplication, and the routed output must land on a registered contentType (adaptive-note mandatory contract — no plugin render path). | Per-section fixed budgets (wastes cap when sections are absent); nested-object template walking (changes existing `{{a.b}}`-adjacent semantics); serving learner via ChatContext (would touch every transport now — MEM-3 consent UI still pending); returning raw router output (bypasses spec validation). | ACTION-2b consumes the new fields in the manager/builder (two-field create) + sends contentType-less runs from `operation.run`; doc.collection composition when sources gain a folder handle; consent switch for managed learner rides MEM-3. |
| 2026-07-04 | CONCEPT-UX-1-001: 选中即建概念 links the passage through the EXISTING storage path — ONE markdown marker note carrying `anchorIds:[anchor]` + `conceptIds:[concept]` — because concept↔note links live ONLY on `note.conceptIds` (no `anchor.conceptIds`; `getConceptDetail` back-refs join over notes) and anchors only PAINT when a note references them. Dedupe is normalized-name (case/whitespace-insensitive, shared `conceptName.ts` — the command, the chips autocomplete, and the list filter all use the same identity). Undo = delete the marker note via the toast's 撤销 (`undoConceptMark` calls `entityClient.deleteNote` directly — `note.delete` would window.confirm); the concept RECORD stays because the entity client exposes no concept delete — an empty concept is harmless and reusable. The 10-kind relation form is REMOVED from ConceptInspector (关联到… autocomplete → immediate `relationKind:"related"`); RelationInspector keeps showing the kind read-only and gets NO kind-editing UI (scope law: no new advanced UI). One `ConceptAutocomplete` component serves both the chips ＋ and 关联到… (abstract-recurring-capabilities). New CSS in scoped `conceptUx.css`; new strings via the i18n seed (`conceptMessages.ts`). | The loop must close on today's surfaces (mark → paint in reader → back-ref in inspector → navigable), and the note path is the only one that does all three; a `relation(anchor→concept)` record would be invisible (no paint, raw-id rendering) — dead data; the marker note doubles as the capture artifact (the quote at the passage). The e2e that exercises the old selects (`e2e/concepts.spec.ts`) is `test.skip`ped (pre-existing R7 layout regression), so removing them breaks nothing live. | nodeRef relation anchor→concept (schema supports it; UI/paint don't — rejected as loop-dead); linking into an EXISTING note at the anchor when one exists (surprising mutation of a user's note); a `concept.delete` undo that also removes the created concept (no client API, and the marker-note delete already reverses the visible effect); keeping the 10-kind select beside the autocomplete (the form-heaviness this task exists to kill); a generic toast service (YAGNI — one scoped ConceptMarkToast until a second toast consumer appears). | V1.1 = the capture engine ([[wiki-links]] + AI auto-tagging in the generation contract) + co-occurrence graph kit — design doc to come; consider surfacing marker notes distinctly (e.g. a chip style like bookmarks) if they clutter note lists; concept delete/merge UI when a client API lands; un-skip and update `e2e/concepts.spec.ts` for the autocomplete flow when the R7 concept-pane layout regression is fixed. |
| 2026-07-04 | TRUST-12-001: data trust is ONE server module (`src/server/dataTrust.ts`) where backup, export, import, and restore all share a single walk+zip / validate+swap pipeline — a backup zip IS a `.growte-vault.zip` (same `growte-vault.json` manifest), so restore and manual recovery are the import path, not parallel machinery. Zips are fflate (already a dep) STREAMED file-by-file with skip-on-error (concurrent writes report partial, never fail the backup) and the backups dir excluded even when nested. Rotation = the doc's 7 daily + 4 weekly (newest per UTC day/epoch-week, pure `planBackupRotation` on an injected clock) **plus a 24h protection window** — without it a later same-day backup would rotate away the pre-import safety backup, defeating its purpose. Import is full REPLACE only (doc: merge is svpack's job): explicit typed confirm phrase (`confirm=替换全库`) in the request, full structural validation BEFORE anything is touched (manifest, per-line JSON, counts match, zip-slip guard), automatic pre-import backup FIRST (failure aborts untouched), staging-dir extract → two-rename swap with rollback → in-process reload (`vault.manifest` reassign + `sealed.refresh()`; the jsonl stores read from disk per request so NO server restart is needed — `restartRequired:false` honestly). Automatic behaviors follow the aiConfig idiom: routes always registered, but the pre-清除记忆 backup hook + the 24h scheduler (MEM-2 idle-scheduler idiom) arm only via `createApp.dataTrust` / `startServer.backups` — dev CLI opts in (data/backups, gitignored), Electron arms it only when `app.isPackaged`, and `STUDY_VAULT_AUTO_BACKUP=0` is the e2e kill switch (playwright sets it), so ephemeral test vaults never litter repo-side `backups/` dirs. UI: the hub 数据 section is deferred (SettingsHub contended by a parallel session); three UserMenu 数据 entries + the status readout as the 立即备份 tooltip carry the capability, with the DIFFERENT-VAULT warning driven by a LOCAL fflate manifest peek (nothing uploaded to decide). | One pipeline = one set of invariants to test (the restore roundtrip byte-path is literally the import path); the doc pins sibling `backups/` + zip + 7/4 rotation + confirm-replace semantics; disk-read-per-request stores are what make the swap safe in-process; hermetic legacy tests forbid implicit backup side effects (the mock-data law), which forces the explicit-arm design. | archiver/adm-zip/yazl (new deps — fflate already ships and is streaming-capable); dir-copy backups (no single-file artifact to hand a user); rotation without the protection window (kills same-day safety backups); merge-import (explicitly not V1); restore as a manual file operation only (the doc demands pick→confirm→swap, and it was nearly free on the import pipeline); scheduling inside bare createApp (pollutes every unit-test tmp dir); a second confirm dialog instead of the TYPED phrase (too easy to click through for a destructive replace). | TRUST-3 回收站 (soft delete) next; hub 数据 section (status line, backup picker/restore UI, keep-N config) when SettingsHub frees up; §4 lockfile single-writer guard (sync V0 stance) NOT built this round — schedule with the sync design round or as a small TRUST follow-up; consider Content-Length/progress on export if vaults grow. |
| 2026-07-04 | SPEECH-1B-001: read-aloud is a property of TEXT, not of a surface (user law: "读本质上不是一个 anchor 的能力，而是所有文本的可读能力，包括 AI chat 里的回答"). The universal layer is a HOST-level `GlobalSpeakSelection` chip mounted ONCE in the WorkspaceShell chrome (SelectionFloatingToolbar's sibling + idioms: portal, measure→rAF placement, mousedown-preventDefault so the click doesn't collapse the range) speaking `selection.toString()` — TTS only, no anchors/persistence. Double-serving is prevented by ONE `closest(".reader-panel")` check (the reader pane's stable container, the SAME selector the reader toolbar scopes to — its 朗读 keeps serving reader selections; iframe/webview realms never reach the host selection anyway). Where selection is clumsy the text gets per-block affordances through the SHARED SpeakButton: every assistant chat bubble's actions row (always rendered now) and ONE affordance on the unified note shell header (`FocusOverlay`), reading via `getNoteContentSpec().toSearchText` — never per note type (adaptive contract), and deliberately NOT on ArtifactCard (the whole card is a click-to-open button; nested interactive controls fight the gesture and the overlay it opens has the affordance). While speaking, the chip survives selection loss so 停止 stays reachable. | The kernel design law (one capability behind a shared contract, host-level mount) forbids re-mounting 朗读 per view; host `selectionchange` is the one place ALL host text funnels through; `.reader-panel` is already the established host-realm scope boundary (SelectionFloatingToolbar uses the exact selector), so exclusion is one line, not per-surface logic; chat replies and note full-views are the two places users read blocks rather than selections. | A speak entry in every view's own toolbar (per-surface bolt-ons — the law this task exists to kill); extending SelectionFloatingToolbar to serve non-reader selections (it is anchor-scope: runAction/materialization machinery the global chip must NOT have); anchor-materializing the selection before speaking (persistence for a transient utterance); mounting on ArtifactCard too (nested-interactive + double affordance); a `data-` attribute contract for exclusion zones (YAGNI — one stable class covers the only zone that self-serves). | Reader-surface parity for webview/iframe guests rides their own toolbars (already 朗读-capable); dedicated `toSpokenText` overrides + voice/speed picker remain the open SPEECH-1 follow-ups; if a second self-serving zone ever appears, generalize the exclusion selector into a small registry then (not before). |
| 2026-07-04 | SPEECH-2-001: STT V1 is the LOCAL lane only — a detected faster-whisper SIDECAR (`scripts/stt-sidecar/server.py`, stdlib `http.server` + faster-whisper, adapted from roundtable stt.py: GPU float16 → CPU int8 fallback, single-flight infer lock, zh-biased decode; 127.0.0.1:8765, `STUDY_VAULT_STT_URL` override) behind the SAME lane-seamed `SpeechService` from SPEECH-1 (it gains `transcribe()` + per-lane stt status with injectable `probeSttHealth`/`transcribeStt` — never a second speech service). Availability is the REAL `GET /health` probe (800ms timeout, cached 30s; a failed transcribe flips the cache down immediately) — the mic 点亮s only when the engine truly answers, and unavailable renders an in-place 3-step setup-guide panel (the sidecar README rendered small), NOT a settings surface. Client: the SPEECH-1 status cache factored into a shared `speechStatus.ts` (one fetch serves 朗读+mic); `useVoiceInput` (MediaRecorder webm/opus+fallbacks, seq-guarded cancel) + `VoiceInputButton` with a transcript-CONFIRM popover (确认插入/重录/取消) — no recognized text ever lands in an input without explicit confirm. One mount on the chat composer bar covers BOTH designated surfaces because the slash composer's main text field IS the chat textarea (SC-1 parses `chatInput`). | kit-flatten §4 names the V1 explicitly (detect local sidecar, 点亮, setup guide; sherpa-onnx = V1.1); faster-whisper is roundtable-PROVEN for zh and fully offline/free — recognition must cost the target users nothing; a sidecar keeps Python/CTranslate2 out of our process and installer; the preview-gate principle (design §2) exists because kids' recognition errors are common — silent insertion would poison notes; stdlib http.server keeps sidecar deps = faster-whisper alone (FastAPI adds nothing a 2-endpoint loopback service needs); the REAL probe (vs tts V1's "configured" semantics) is what makes 点亮-on-detect honest. | Web Speech `SpeechRecognition` (Google-proxied — ruled out for China/Electron, design §2); embedding ASR in Node via sherpa-onnx now (deferred to V1.1 — evaluate as the ONE cross-platform engine); BYOK audio-model / managed STT lanes first (LLM-priced or gateway-priced — V1 must be free); auto-inserting the transcript with an undo (violates the preview gate); putting the setup guide in Settings Hub (a dead-end surface for a "start the engine" action); multipart upload from the client (raw blob body is simpler; the server proxies bytes verbatim). | BYOK audio content-part lane (①) + managed lane (②) ride the existing seams; note-editor dictation + review-runner 语音作答 mounts when those files free up; sherpa-onnx consolidation at V1.1; consider sidecar auto-start from the Electron shell (X1 packaging). |
| 2026-07-04 | SPEECH-1-001: TTS is a SERVER-side lane-seamed service (`src/server/services/speech.ts`: `SpeechService.synthesize/status`, V1 lane = `edge` via `msedge-tts@2.0.6`, injectable `synthesizeEdge` seam on `createApp` mirroring the injected modelProvider) behind `POST /api/speech/tts` + `GET /api/speech/status`; the client 朗读 trigger is ONE shared stateful `SpeakButton` (朗读↔停止, status-gated) + a `useSpeakText` hook (blob-URL Audio, one utterance at a time, module-cached status probe) mounted beside the existing action rows — NOT a `ToolbarAction`/command dispatch, NOT a new per-card toolbar. Voices restricted to a curated 3-zh+1-en shortlist (SSML injection guard + honest UI list); text source per surface: selection = the shared focus draft/anchor quote (realm-safe for iframe readers), note rows = the type's own `toSearchText` (the `toSpokenText` V1 default). | edge-tts's custom `Sec-WebSocket-Version` handshake only works in Node → server-side is forced (design doc §1); the lane seam is the recurring "modality lanes" shape so local/managed lanes slot in without reshaping callers; a speaking button's live 朗读↔停止 state doesn't fit the static ToolbarAction/dispatch model, and mounting one shared component beside ActionGrid/SelectionToolbar/note-row strips keeps 复现行为=一个能力 (abstract-recurring-capabilities). Offline = friendly 502 `{error, code:"tts_unavailable"}` — keyless-but-ONLINE is stated honestly, never masked. | Browser `speechSynthesis` (robotic zh, but offline — kept as a FUTURE fallback lane, not V1); a `speech.speak` command through the dispatch registry (loses the stop-toggle state); free-text voice pass-through (SSML attribute injection + a lying voice list); streaming the wss audio through the HTTP response (blob-URL playback is simpler and the utterances are short); putting the button inside ActionGrid items (would need per-action state the model doesn't have). | SPEECH-2 STT reuses the lane seam; runner 听题 + Settings Hub voice/speed picker + `toSpokenText` per-type overrides (quiz order, formula skip) + offline lane = the SPEECH batch; consider streaming playback if long-text 朗读 (whole sections) becomes a surface. |
| 2026-07-04 | LAYER-TREE-001: Layer organization is `parentId` tree first. `Stages` is not a product-level UI section; kit-seeded presets such as 预习/学习/复习/拓展 are ordinary child layers under the owned/Mine layer, and newly-created custom layers also default under Mine. `role` remains only compatibility/permission metadata (`preset` non-deletable, `custom` deletable, `shared` imported/read-only), not a grouping primitive. | The data model already had `parentId`, and a fixed `Stages` bucket made the UI look like it had a second hardcoded hierarchy. Letting the tree do the work keeps Layer generic enough for arbitrary plugins while preserving old preset records and import/share rules. Mine can carry notes and children, so tree roll-up/cascade covers the parent layer itself plus descendants. | Keep Mine/Stages/Custom/Imported hardcoded buckets; introduce a separate Stage entity; make Mine a pure folder with a hidden owned leaf layer; migrate/rename existing layer records. | If manual drag-nesting is added later, write only `parentId`/`order`; do not add new UI grouping enums. |
| 2026-07-04 | UI-SETTINGS-001: Settings is a sidebar work pane, not a standalone settings page. The Hub uses the same 10px white pane shell as the left work panels; sections are unframed groups separated by subtle dividers; AI provider rows are the card primitive with `data-selected` / `data-effective`, hidden native radios, a custom 16px accent-blue radio marker, neutral metadata chips, and accent-blue positive states. | Settings was visually behind the rest of the app: nested section cards, browser radio circles, blue/green mixed status language, and dense provider metadata made it read as a separate legacy page. Treating Settings as another pane keeps the app coherent and reserves accent blue for the active/positive state. | Leave provider rows as native radio rows; make every section a card; keep green success badges; create a Settings-specific control system. | Any new settings section should use unframed groups, card rows for repeated options, shared `.sv-switch` for persistent toggles, and neutral metadata chips unless the state is truly selected/error. |
| 2026-07-04 | SHELL-PRIM-001: 链接文件 is a CORE built-in `file-link` note type (`{path, title?, note?}` spec beside `mistake` in `src/core/notes/contentTypes.ts` + a `registerNoteType` client half with NO `pluginId`), never a plugin/kit and never a bespoke render path. The 打开 action's desktop/web split lives INSIDE the type's one render: bridge present (`window.studyVault.openPath` — a NEW minimal `shell:openPath` IPC → `shell.openPath`, main-side guarded to absolute local paths, error-string contract preserved) → 打开; no bridge → the same button is 复制路径 (clipboard). The composer form reuses the EXISTING `dialog:openFile` picker; styling lives in a scoped `fileLinkNoteType.css`, not `styles.css`. | §3's "not a plugin" ruling means core seed registration but NO registry bypass (adaptive-note contract): recognition = the declared form, display = `getNoteType().render`. The note stores a REFERENCE (path), unlike media types which import bytes as vault assets — linking is the point (huge files, live documents). Bridge-presence detection is the established desktop idiom (`canPickFile`); a guarded one-method IPC keeps the locked-down preload surface minimal. | Importing the file as an asset (duplicates bytes, breaks the "open the live file" use); a kit-provided type (the shell primitive must survive uninstalls); reusing `dialog:openFile`'s window for opening (it picks, it doesn't open); allowing URLs in openPath (shell-opening arbitrary strings is an SSRF-ish footgun — sources/webview already handle URLs). | A dedicated toolbar/composer affordance alongside 写文字/问AI (rides the SC-1 composer mount when views.tsx frees up); file-existence indication on the card (needs a stat IPC — deferred); MARKER_GLYPHS/noteTypeIcon entry deliberately skipped (falls back to the FileText glyph) to stay out of the contended annotationLayer.ts — add when the reader session settles. |
| 2026-07-04 | UI-PLUGIN-001: Kit & Plugin management uses a sidebar-native tab header: `已安装` / `市场` are top underline tabs, search is collapsed into a right-side magnifier, and the input expands as a row below the tabs. Search is panel-level: it filters installed kit/plugin rows in `已安装` and catalog listings in `市场`; All/Plugins/Kits remains a market-internal filter under the search row. | The previous panel mixed large pill tabs, a permanently visible search box, and market-like spacing inside a compact left workspace pane. The top-tab plus collapsed-search pattern matches the rest of the shell, keeps the default panel quieter, and still gives fast search without taking first-row space. | Keep the old circular 已安装/市场 buttons; keep search always visible; move All/Plugins/Kits into the top tab row; split installed and market search into separate controls. | Any new plugin-management pane should reuse this tab/search hierarchy and the shared sidebar panel metrics from `design.md`. |
| 2026-07-04 | REV-CORE-001: review eligibility is a REGISTRY CONTRACT — `NoteContentSpec` gains `review?: { reviewable, expectedAnswer? }` (queue rule 2 + grading) and `mistake?: true` (rule 1), plus a GENERIC contentType alias map (`registerNoteContentSpecAlias`; `getNoteContentSpec`/`parseNoteContent`/`getNoteType` are alias-aware, direct registrations win). 错题 moved core as contentType `"mistake"` with alias `"textbook.mistake"` → it (zero data migration; new saves write `"mistake"`; core seeds it private-by-default in policy.ts). The review PLUGIN dissolved into core: prompts (`review.generate-check`/`review.grade-answer`/`review.explain`, ids unchanged) live in `src/core/review/prompts/` and seed the prompt registry on load; the hidden `review.grade` spec + the mistake/grade client renders register with the built-ins; `src/kits/review/` deleted, no catalog entry, stale installed ids inert. | The review loop IS the mission loop (product-kernel §0) — it must not be uninstallable, and it must not import kit files (the adaptive-note lesson: capability declared per spec, engine reads the registry). The alias map is the cheapest zero-migration path for persisted contentType ids and is generic for future renames. `textbook.review-pack` declaring `review:{reviewable:true}` in its OWN file proves kits extend the loop by declaration alone. | Keep review as a default-installed plugin; hardcode both mistake ids in the queue; migrate vault records `textbook.mistake`→`mistake` on load; alias resolution hardcoded in the getter instead of a registry. | FLAT (kit-flatten-and-core-review.md §2) models capability groups WITHOUT the review plugin; future SRS = swap the one queue policy fn; reuse `registerNoteContentSpecAlias` for any future contentType rename. |
| 2026-07-04 | UI-CONTROLS-001: checked UI now has two shared primitives only: `.sv-check` for selection/inclusion rows and `.sv-switch` for persistent on/off settings. Layers use `.sv-check` because they are filter membership rows; Profile/Settings memory capture, Plugin enable toggles, and Operation enable/hide toggles use `.sv-switch`. The same rule is now written into `design.md`. | The UI had several visually unrelated checked states: raw panel checkboxes, large one-off blue checks, plugin toggles, memory switches, and note editor checkboxes. Splitting by semantics keeps the control language predictable while preserving the user's single accent-blue requirement. | Make every checkbox a switch; leave raw browser inputs and tune each pane separately; create business-specific Layer/Plugin/Memory controls. | When adding a new checked control, pick `.sv-check` or `.sv-switch` first; do not add a new checkbox style unless the interaction is genuinely a different primitive. |
| 2026-07-04 | F3-D1-001: every reader realm implements the one D1 `ReaderAnnotationAdapter` (`rectsFor` first/last/all · `onLayoutChange` · `injectRealmCss` · `paint` · `reveal`, `src/client/surfaces/readerAnnotationAdapter.ts`); `MarkerOverlay` and the note-card wiring consume the adapter instead of reaching into reader internals, and the webview guest's inline `<mark>`-embedded marker chip is REMOVED in favor of a real body-mounted `MarkerOverlay` (clicks bridge over the existing `sv:marker-action` channel). For the webview surfaces the adapter lives in the GUEST realm (the preload), not the host reader component — rects/paint/reveal are only measurable in that realm. | The paint/marker/card pieces were already shared and realm-safe; what diverged was hosting and measurement: each reader hand-rolled overlay mounting/reposition wiring, `MarkerOverlay` measured only the first `querySelector` hit (a multi-span PDF anchor was measured by its first span alone), and the guest rendered a different inline chip. One named contract makes N1's two-slot markers (`first`/`last`) and P-A2 per-pane paint pure consumers, and lets a future mobile webview build against the same shape from day one. | Keep per-reader wiring and give N1 its own rect helper; host-side adapter proxying IPC round-trips for guest rects; threading the adapter through `SurfaceReaderProps` now (deferred — no host consumer exists yet). | N1 (D2 two-slot markers + suppression + clustering) consumes `rectsFor(...).first/.last`; P-A2 reuses the adapter per pane; extend `injectRealmCss` with D3 style vars when N2 lands. |
| 2026-07-02 | ANNOT-FOCUS-001: keep historical note-less anchors hidden server-side, but merge the current focused/materialized anchor into the client paint projection. | The user still does not want old anchors without notes to clutter the document, but a just-created anchor is active working state and must be visible immediately. The client already owns focus, so it can safely include that one anchor without changing server cleanup semantics. DOM annotation rendering must also paint zero-note anchors so marker positioning has a `data-sv-key`. | Re-enable all note-less anchors from the server; persist a special draft-anchor entity; require a note before any anchor marker appears. | If draft anchors become a first-class workflow, add an explicit transient-anchor state instead of widening the persisted anchor list. |
| 2026-07-02 | ANNOT-MARKER-001: Source Viewer marker chips use explicit anchor/note button roles; the note marker reuses the highlighted passage's shared annotation card instead of opening a separate one-note window. | The same anchor can own multiple visible notes, so marker clicks must go through the existing grouped annotation card path rather than a per-note ad hoc popup. Keeping the overlay marker layer as a small host bridge lets PDF/image/DOM and Electron webview readers share the same behavior while still letting the host focus the anchor. | Keep markers as static glyphs; open a custom marker popover that duplicates note-card rendering; focus only the right sidebar and never open the source-side note card. | If marker actions grow, extend the same role-marked button contract instead of adding surface-specific click handlers. |
| 2026-07-01 | ANNOT-PIN-001: click-pinned annotation note cards are anchored to the source text, not to viewport coordinates. | A pinned note is meant to stay related to the passage the user clicked. Saving/restoring absolute `left/top` makes the card look detached after scrolling; storing an anchor-relative offset after explicit drag preserves user adjustment while still following the source text. | Keep absolute viewport placement for all pinned cards; hide pinned cards on scroll; make pinned cards non-draggable. | If multiple pinned cards are added later, reuse the same anchor-relative placement model per card. |
| 2026-07-01 | LINKED-NOTE-001: Linked Notes icons are navigation/focus controls, not local preview toggles. | The Anchor panel already identifies the current passage; clicking a linked note should move the user to the note-viewing surface and re-reveal the source anchor, avoiding a second small popover competing with the Notes viewer. Using `FocusTarget: note` lets RightSidebarTabs and NoteListPanel coordinate through the existing shared focus contract. | Keep the inline `ArtifactCard` under the Anchor panel; open the centered FocusOverlay directly; add a separate linked-note modal. | If note focus later needs deep linking, reuse the same `{type:"note", noteId}` target. |
| 2026-07-01 | ANNOT-REF-001: Source Viewer annotation popovers consume host-built preview-card HTML plus note counts, while `annotationLayer` remains framework-free. | The note card visual system already lives in React (`ArtifactCard`/NoteType registry), but the annotation layer also runs inside iframes and Electron webview guests. Passing inert preview HTML through `PaintAnchor.notePreviews` keeps visual reuse without importing React into cross-realm painting code, and lets DOM/PDF/image/webview surfaces share the same marker/popover behavior. | Re-render note cards independently in `annotationLayer`; import React into the low-level painting layer; keep showing merged markdown/raw JSON in Source Viewer. | If preview cards become interactive inside popovers, add a host-mediated open action instead of relying on static HTML event handlers. |
| 2026-07-01 | NOTE-REF-001: keep the existing NoteType registry and make the reference match through the shared `ArtifactCard` preview shell, `FocusOverlay` expanded shell, and mode-aware note renderers. | The mismatch was caused by each note type surfacing too much of its own UI in the small card, while the reference has one common card/window chrome and only type-specific bodies. Centralizing title/type/footer metadata and using `mode: "card"` for compact previews makes current and future note types inherit the same visual system. | Build a separate static Note reference page; hardcode every note type in the right panel; fork the note registry into preview-specific components. | If another note type is added, require a compact `mode: "card"` body and derive title/extra through `noteCardMeta` instead of custom shell markup. |
| 2026-06-29 | UI-REF-014: do a late whole-shell calibration layer plus one default layout split adjustment, not a component rewrite. | The mismatch was mostly shared visual metrics: topbar sizing, wordmark weight, rail color, panel margins, borders, muted text, chat composer, and the right Anchor/AI split. A late CSS layer and the preset's Anchor height move the real app closer to the reference while preserving the user's earlier removals and the dock/view architecture. | Rebuild the screen as static reference markup; refactor all pane components before the visual target stabilizes; force all sizes through inline styles. | If later requests require exact reference fixture content, add a clean demo-vault visual regression setup so screenshots compare content and chrome together. |
| 2026-06-29 | UI-REF-013: unify the left rail panes with a late shared CSS normalization layer instead of rewriting each pane component. | Library, Bookmarks, Concepts, Actions, and Layers already have stable view boundaries and working data logic, but their visual rules were scattered across pane-specific selectors. A late CSS layer keeps behavior and selectors intact while aligning shell, header, list rows, controls, empty states, and checkbox sizing to the current Growte reference style. | Refactor each pane to a new shared React component; duplicate fixes in every `.concept-*`, `.layer-*`, `.operation-*`, and `.bookmark-*` selector block. | If the UI keeps growing, extract a small `LeftPane`/`PaneSection` component after the visual target stabilizes. |
| 2026-06-29 | Recreate the supplied Growte UI reference by polishing the existing workspace shell instead of introducing a separate mock app. | The current architecture already has TopBar, IconRail, dock panes, reader, layers, and right-side study panels; using those preserves real functionality and keeps the screenshot look tied to the product surface. | Build a standalone static page; replace the data-driven workspace with fixed mock markup. | If exact content-level pixel matching is required, run against a clean vault seeded with the physics demo or add a dedicated visual regression fixture. |
| 2026-06-23 | Use Markdown files under `docs/implementation/` to control larger implementation work. | The user wants explicit research, task splitting, implementation plans, verification methods, progress tracking, and subagent handoffs. Markdown keeps the process visible in the repository. | Chat-only tracking; single large planning file. | Exercise this workflow on the next real feature or fix. |
| 2026-06-23 | Treat Obsidian as an architectural reference through public APIs and open plugins, not as source code to copy. | Obsidian app internals are not a public open-source implementation; the safe reusable material is its public API shape, local-first model, plugin lifecycle, and community plugin patterns. | Attempt to reverse-engineer app internals; ignore Obsidian and design from scratch. | Use the discussion to choose which patterns to implement first. |
| 2026-06-23 | Greenfield rewrite of `src/` rather than evolving GrowHTML. | GrowHTML's GrapesJS component-tree/`project.json` editor model and the Vault's content+Patch data model are two incompatible truth-sources; stitching them is more work than a clean rewrite. | Evolutionary migration on top of GrapesJS. | Rewrite in-place, keep git history + toolchain. |
| 2026-06-23 | "Editing" is not a core abstraction; it decomposes into `Source.content` + `Patch` (only mutation primitive) + `Anchor`. Rendered view is a projection `materialize(content, appliedPatches)` + overlays. | All source modifications must go through reviewable Patches (PRD §3.4/§14); a separate editor state would be a second truth-source. Drop GrapesJS WYSIWYG for MVP. | Keep free-form WYSIWYG editor state alongside patches. | Free editing later = a Source Adapter capability emitting user-authored Patches. |
| 2026-06-23 | AI provider is a plugin; first provider is a Claude Agent SDK plugin behind a capability-based `ModelProvider` interface. | Resolves the chat-vs-agent conflict: the agent's internal tool use (web search) is a provider-internal detail; the runtime only needs prompt+context+schema → structured result. Preserves existing Claude/Codex investment and PRD's pluggable-model goal. | Drop CLI agents and go pure HTTP chat (OpenAI/Ollama) only; or keep Claude/Codex hardcoded. | Add OpenAI-Compatible + Ollama providers later behind the same interface. |
| 2026-06-23 | Build the Vault data layer first (Slice 1, no AI), then plug AI in. | Lets the full Source→Anchor→Note→Patch→render loop be proven with manual actions before any model is involved; de-risks the core. | AI write-back loop first; full minimum closed loop (incl. Concept/Relation/Graph) in one slice. | AI Runtime is Slice 2. |
| 2026-06-23 | Keep TS/Vite/React/Express stack; add Zod + Vitest; JSONL behind a `Store` interface. | Framework switch solves no architectural problem. Zod gives one-source validation for DTOs, AI output, JSON-Schema generation, and JSONL integrity. JSONL matches PRD (readable/local-first), `Store` interface keeps SQLite swap open. | Switch frontend framework; use SQLite from day one; use Hono/Fastify. | `core/`+`adapters/` stay framework-agnostic for desktop later. |
| 2026-06-23 | C1 defines the full Zod-first contract (record envelope + entity schemas + id rule + golden fixtures), not just TS types. | A single Zod source prevents C2 validation, C6 DTO, and AI structured-output from each re-deriving the shape and drifting. Envelope `{id,type,schemaVersion,createdAt,updatedAt,createdBy}`; ids `<type>_<ulid>` (time-sortable). (Codex review.) | TS types in C1, validation bolted on later. | Fixtures double as the C4/C5 parallel contract and as test data. |
| 2026-06-23 | C2 stores are snapshot-upsert (last-write-wins per id) via atomic temp+fsync+rename; pure-log files append-only; event-sourcing deferred. | Patch status (pending→applied→reverted) and Note/Patch CRUD need updates, which append-only JSONL handles poorly. Snapshot upsert is the simplest correct MVP; atomic rename gives crash safety (handle Windows replace). (Codex review.) | Append-only-only; SQLite now. | Add audit/event log when history/undo-tree is needed. |
| 2026-06-23 | Freeze HTML parser = `linkedom` in core; split C5 into C5a (parse/materialize/applyPatch) + C5b (anchor resolve + conflict guard); selection capture moves to C7. | "core pure TS" forbids browser DOM, so the parser must be chosen up front or patch-apply leaks to the frontend. Anchor stability + patch conflict detection are the hard parts and deserve isolation. (Codex review.) | jsdom; keep C5 monolithic; capture selection in the adapter. | jsdom fallback only if linkedom serialization fidelity fails. |
| 2026-06-23 | C8 is a non-optional Seed/Migration; C7 acceptance requires "reload still correct". | C7 (no-AI manual loop) needs deterministic source material, and persistence must be proven across reload, not just in-memory. (Codex review.) | C8 optional; verify only in-session. | Seed imports old `content.html` if present, else generates a demo source. |
| 2026-06-23 | AI Action has two tiers under one `AIActionPlugin` interface: code actions (compiled TS) and declarative slash commands (vault `.md`, safe-templated, no eval). Declarative commands are runtime-loadable without a sandbox (tier 1.5). | Lets users author/share unlimited lightweight "generation plugins" as data; same precedent as Claude Code / Obsidian Copilot markdown commands. Being data not code, they are safe to runtime-load now, unlike untrusted JS. | Only compiled code actions; defer all user plugins to sandboxed tier-3. | Slice 2 (M4): design the interface for both tiers, ship built-in code actions + a declarative loader; commands in `.study/commands/*.md`, shareable as packs, permission-declared. Slice 1 unaffected. |
| 2026-06-23 | Core entity schema is stable after the extension discussions; only two additions remain (left for Codex): `Note.contentType` as an **open string** (default `markdown`, not enum) and **promote `metadata` to the envelope** (every entity gets a plugin escape hatch; dedupe Source's existing field). | All discussed features map to reserved fields or plugin/runtime/config/log layers; the `metadata` escape hatch is what keeps core stable against future plugins (plugins extend via metadata, not new columns). `contentType` must be open or each note plugin would force a core schema change. | Closed `contentType` enum; per-feature schema columns; no escape hatch. | **Done 2026-06-23:** `noteContentTypeSchema` (open string, default `markdown`) added to `note.ts`; `metadata` promoted into `recordEnvelopeSchema` (`common.ts`) and deduped from `source.ts`; API passes optional `contentType`; schema tests added; `check`+`test` green (45). |
| 2026-06-23 | Rich note content is a first-class plugin extension point ("note plugin" form): `NoteContentRenderer` registered in `plugins/registry.ts` keyed by `Note.contentType`, like `SourceRenderer`/`ViewPlugin`/`ModelProvider`. Tier A text/markdown (MVP, built-in), tier B structured data — mindmap/diagram/flashcard (data-safe, runtime-loadable like tier-1.5 commands, fast follow), tier C interactive artifact — animation/mini-game (sandboxed iframe, deferred code-plugin path). Reserve `Note.contentType` (default `markdown`) now. | Note's own render layer makes rich content cheap; new note types ship as plugins, not core changes. Real differentiator vs Obsidian's text-only annotation; proven pattern (Claude Artifacts). Same data-vs-code→sandbox rule keeps it safe. | Text-only notes; build artifacts into MVP; treat note types as core not plugins. | Discipline: MVP tier A only; tier B first; tier C after B proves used. |
| 2026-06-23 | Recorded a scope-risk / competitive red line: core loop (anchored ask + reviewable Patch on local materials, own model) is real but the wedge is narrow; Concept/Relation/Graph + universal formats + marketplace are the pseudo-requirement-risk zones. | Each feature exists elsewhere (NotebookLM/Obsidian+Copilot/Cursor/MarginNote); must avoid building "my own Obsidian". Keeps focus on the HTML→PDF ask+anchor+patch+note loop. | Pursue graph/all-formats early; assume full differentiation. | Re-read before adding features; do not pull graph/new formats forward. |
| 2026-06-23 | The Note layer can evolve into a shared annotation layer (WeChat-Read-style) by swapping the `NoteService` backend (local→synced) + identity/visibility; no source/renderer change, no CRDT. Reserve `Note.authorId`/`Note.visibility`/`Source.contentHash` now; MVP stays single-user/private. | Decoupled Note layer makes sharing a backend swap; annotations are an additive author-keyed set (union/sync), so CRDT (a PRD §17 non-goal) is unneeded. Reserving 3 cheap fields avoids a later schema migration. | Bake sharing into MVP; add fields later via migration; use CRDT. | Prereqs deferred: contentHash source identity, quote/context portable anchors, sync + opt-in privacy via permission model. |
| 2026-06-23 | Note is an independent layer (own data `notes.jsonl` + logic `NoteService` + render `NoteLayer`) composited over the Source layer, coupled only via `locateAnchor`. Note never enters source content; "content into the doc" is a Patch, not a Note. | Decouples annotation from editing: source stays read-only/any-format, re-render never loses notes, PDF/image annotate fine, and all writers (user/AI/embedded CLI) just write data → reactive render ("feels like editing" without DOM surgery). | Materialize notes into source HTML; per-format note rendering. | Embedded CLI is just another writer, split by permission: note→`write_note` direct; source change→`write_patch`/`modify_source` (reviewed), never edit source file directly. |
| 2026-06-23 | Plugin freedom is tiered: UI extension (granted via Workspace regions + contributions), capability (granted but permission-gated), runtime third-party install (deferred post-M8). MVP plugins are first-party/compiled-in. | Gives Obsidian-style freedom to add views and customize the library/nav without core changes, while keeping content-editing/AI tools safe via the permission model. Untrusted runtime loading needs a sandbox — too costly for MVP. | Full-trust Obsidian-style runtime loading now; hardwired UI. | Build the client shell with regions/registry in Slice 1 (built-in views only) so View plugins need no refactor later. |
| 2026-06-23 | Agentic providers are embedded as local CLI subprocesses (`claude-agent-sdk`/`claude-cli`/`codex-cli`) using the local login's subscription OAuth. | Same mechanism as OpenClaw/Hermes and as existing GrowHTML; lets the local tool ride Claude Max / ChatGPT subscriptions instead of paying per-token. Requires same-host login. | Pure API-key providers only (loses subscription); reverse-proxy hacks. | Provider plugin must: strip `ANTHROPIC_API_KEY` in subscription mode (it silently overrides OAuth → API billing); note post-2026-06-15 separate Agent-SDK credit pool; individual-use ToS (switch to API key before multi-user); support `CLAUDE_CODE_OAUTH_TOKEN` for headless. |
| 2026-06-23 | Viewers are plugin-ized as a per-`type` pair: `SourceAdapter` (core data ops) + `SourceRenderer` (client view). Note/Patch/Concept stay format-agnostic; the format plugin only maps Anchor→position and draws overlay decorations. | Keeps the generic core from re-implementing Note display per format and lets non-editable formats (PDF/image) degrade to annotation-only via `capabilities.editable`. Anchor is the only contract → it is a discriminated union by `anchorKind`. | One monolithic adapter mixing UI+data; per-format Note rendering. | Slice 1 = HTML pair only; PDF/Word/code/image = Slice 4. Word MVP = docx→HTML via mammoth. |
| 2026-06-23 | Vault root defaults to `./data/vault/`, env-overridable; single vault for MVP. | In-repo path is git-friendly and matches local-first; one vault keeps C3 interface simple. Multi-vault / folder-picker deferred. | Force user folder-pick on first run; multi-vault now. | Revisit when desktop (Tauri/Electron) filesystem access lands. |

| 2026-06-23 | C7 reader capture prefers real selected text, but also supports clicking a `data-study-id` block to create an Anchor draft from that block's text. | Browser automation could not reliably create native text selection inside the iframe, and a click fallback is also a useful MVP interaction. The same handler still uses selected text when present, preserving sub-paragraph precision for real users. | Require drag-selection only; add a separate debug/test-only control; make anchors API-only. | Human spot check native drag-selection before relying on fine-grained sub-paragraph anchors. |
| 2026-06-23 | Pivot to a desktop app — **Electron (confirmed)**, superseding the earlier web-only decision (Q-001). | A pure web app cannot embed arbitrary live webpages with an annotation overlay (X-Frame-Options + same-origin); a desktop webview (Electron `BrowserView`/`<webview>` + preload injection) can, and also unlocks direct local-filesystem access. Electron reuses the TS/Node/Express/React stack (core/adapters already pure TS) with least friction; Tauri rejected (Rust shell + Node sidecar + constrained webview injection). | Browser extension (web-only, no local files UI); server proxy/snapshot (frozen copy); Tauri. | Sequence: wrap S1 in Electron shell → webview web-annotation → shared NoteService → PDF/Word. |
| 2026-06-23 | Two source modes (local files: HTML/MD editable, PDF/Word annotation-only; web pages: annotation-only) under one `capabilities` model. Editing is deferred but KEPT: `Patch` stays a core entity (dormant) + adapter `applyPatch`; the editing *experience* = a Patch-Review ViewPlugin + Rewrite/Insert AIAction plugins, gated off in MVP via a feature flag + `capabilities.editable`. | User wants both annotation-only reading and (later) source editing; capabilities model already supports per-adapter editability. Gating keeps Slice 1's patch code dormant, not wasted; turning editing on later = register the plugins, no code change. | Delete the patch pipeline; bake editing into MVP. | MVP runs read + annotate only. |
| 2026-06-23 | Sharing is pull-based (link / published artifact), NOT an ambient web-wide annotation network. (a) internal deep link or shared note file → opens the layer in-app; (b) publish a note/layer as a self-contained public webpage (reuses the standalone-HTML export) that renders rich note forms. | Ambient overlay sharing needs scale to be valuable → cold-start death (Hypothesis's decade-long wall). Pull-based gives immediate 1→N value (Glasp/Readwise public pages, Claude Artifacts publish). Rich forms travelling on the public page is the differentiator vs Glasp's text-only pages. | Bet on a whole-web ambient annotation network; text-only shares. | Public page = optional hosting + opt-in/default-private (publishing is public/indexable); tier-C artifact notes render in a sandboxed iframe. Ambient overlay = later optional bet. |
| 2026-06-23 | Adopt a Playwright click self-test as the standard per-step UI verification, alongside Vitest for data/logic. `npm run e2e` boots the real server+client on an isolated vault and drives Chromium through the full no-AI loop with assertions. | The user asked for every step to be self-verifying via clicking the real web app. Click-driven E2E proves the actual UI loop (iframe selection, apply/revert, reload persistence), which unit tests cannot. | Manual QA only; component tests without a real browser. | `vitest.config.ts` excludes `e2e/**`; extend `e2e/loop.spec.ts` per new UI step; `npm run e2e` green 2026-06-23 (4 passed). |
| 2026-06-23 | NoteLayer overlay renders stored notes onto the reader document via a client-side `decorateNotes(doc)` that maps anchor→studyId→element and adds a highlight + title tooltip; recomputed on notes/anchors change. | Keeps the Note layer coupled to the source only via the existing study-id locator (the SourceRenderer overlay model), needs no AI/PDF, and is fully click-E2E verifiable. | A true margin gutter (layout-fragile in an iframe); injecting markers as child nodes (breaks arbitrary element layout). | PDF/web overlays will implement their own `locateAnchor`/`decorate`; richer popovers + click-to-focus-panel later. |
| 2026-06-23 | Local PDF is stored as bytes via a generic binary-source path (`ingestBinarySource`/`computeBufferHash`/`readSourceFile`) and served raw at `GET /api/sources/:id/file`; the reader embeds the native browser PDF viewer. | Delivers "open/persist a local PDF" with a fully Vitest-verifiable data path (byte-equality + magic-byte guard) without a heavy PDF.js dependency; the `/file` endpoint is reused later by Electron and by PDF.js. | PDF.js text-layer reader now (heavy, flaky headless); store PDFs as base64 in JSONL (bloats snapshots). | PDF anchoring/notes-on-PDF needs a PDF.js text layer (deferred); reader is display-only for now. |
| 2026-06-23 | Rich note content is a pure, dependency-free `NoteContentRenderer` registry (`contentType` → HTML string) with built-in `markdown`/`mindmap`/`flashcard`, HTML-escaping first, link-scheme allowlist, and inert-plain-text fallback for unknown/parse-fail. | Pure functions are unit-testable without a DOM and safe for `dangerouslySetInnerHTML`; the registry is the plugin seam for future forms. Escape-first + allowlist makes user/AI content XSS-safe. | A markdown lib + DOMPurify (needs a DOM to test the pure path); rendering raw HTML notes (XSS). | mermaid/markmap (heavy SVG/canvas libs) and tier-C sandboxed-iframe artifact notes register as additional renderers later. |
| 2026-06-23 | AI is a capability-based `ModelProvider` boundary with a deterministic offline `MockModelProvider` as the verified default; the real `ClaudeCliProvider` (subprocess `claude -p`, subscription OAuth) is env-selected and not the test path. The API-key-stripping is isolated in a pure, unit-tested `buildSubprocessEnv`. | Mocking at the provider boundary makes the whole chat loop deterministic/offline-testable; isolating the safety-critical env handling as a pure function lets the "never silently bill the metered API in subscription mode" invariant be asserted without spawning a subprocess. | Test against the real CLI (non-deterministic, requires auth, bills API); hardcode Claude/Codex. | Wire/verify the real `claude-cli` provider against an authenticated CLI; add hosted-API + Ollama providers behind the same interface; switch subscription-OAuth → API key before multi-user release. |
| 2026-06-23 | Source readers go behind a client `SourceViewer` registry (`getSourceViewer`/`registerSourceViewer`), each type mapping to `{kind, htmlPipeline}`; this is the third plugin registry (with `noteRenderers` and `ModelProvider`). | The earlier MVP hardcoded `sourceType === "pdf"` branches; making viewers a registry realizes the long-planned "viewers = SourceAdapter+SourceRenderer plugin pair" and lets webview / PDF.js viewers register without touching the reader's control flow. | Keep adding per-type `if` branches; a heavier full SourceRenderer interface (render/capture/locate/decorate) now (deferred until a viewer actually needs it). | Webview registers a `webview` viewer; PDF.js text-layer registers an enhanced `pdf` viewer; promote to a full SourceRenderer interface when overlay/capture diverge per type. |
| 2026-06-24 | The PTY bridge (main process) uses an INJECTABLE spawner: real node-pty (lazy-imported, API-key-stripped) by default, or an in-process fake echo when `STUDY_VAULT_PTY_FAKE=1`. The visible xterm terminal + bridge are verified end-to-end via the fake spawner in `_electron`. | node-pty is a native module that can't be built for Electron in the headless sandbox (Windows winpty source build needs MSVC tools), and a real CLI is non-deterministic — the fake spawner makes the full main↔IPC↔preload↔xterm pipeline deterministically testable here, while real claude/codex use the identical path on a machine that ran `npm run electron:rebuild`. | Skip the terminal e2e (no pipeline coverage); mock at the renderer (misses the IPC/main path); require native node-pty in CI (can't build here). | Run `electron:rebuild` + verify live claude/codex on a display machine; consider `node-pty-prebuilt-multiarch` if a matching prebuild becomes available; watch the Windows ConPTY `AttachConsole` teardown quirk. |
| 2026-06-24 | Add a PTY-backed `claude-pty` ModelProvider that holds ONE persistent interactive `claude` session across turns; prefer it on desktop, keep `claude-cli` (`-p`) and the Agent SDK as fallbacks. Both PTY integration modes (transport-into-chat now; visible xterm terminal next) are plugins behind the same `PtySession` seam. | A persistent session reuses the CLI's context + prompt cache instead of paying a cold start every turn (`claude -p`), so multi-turn chat consumes far fewer effective tokens / more subscription headroom. The real win is session+cache reuse, NOT dropping role markers (marginal). `PtySession` seam (node-pty real / fake) keeps the provider deterministically unit-testable without the native module. | Keep only `-p` one-shot (token-heavy multi-turn); raw subprocess pipes without a PTY (CLI TUI needs a tty); make it default (non-deterministic, needs auth → mock stays default). | Visible xterm terminal plugin + Electron main node-pty bridge over IPC (`@electron/rebuild` for the ABI); tune `extractAnswer` against the real CLI TUI; watch the Windows ConPTY `AttachConsole` teardown quirk; PTY is desktop-only (mobile can't run node-pty → falls back to a hosted-API provider). |
| 2026-06-23 | Content hashing uses a pure-TS `sha256Hex` (not `node:crypto`) and the source-path traversal guard is a pure `assertSafeRelativePath` (not `path.sep` prefix matching). | Completes the de-Node'ing of the core data layer so its only hard Node dependency is `NodeStorageAdapter`; a pure sha256 works identically on Node/browser/RN and stays synchronous (Web Crypto is async and would ripple). Verified against FIPS vectors + node:crypto parity. | Inject a `Hasher` like storage (defers the real impl, adds threading); use async Web Crypto (ripples async through ingest); keep node:crypto (blocks mobile). | Mobile port now = one Capacitor `StorageAdapter` + client `api` → in-process data layer; `node:path` (portable) is still used for composition. |
| 2026-06-23 | All vault filesystem access goes through a `StorageAdapter` interface (`NodeStorageAdapter` default, `MemoryStorageAdapter` for tests/reference), injected as an optional `storage` param defaulting to Node and exposed as `vault.storage`. | Removes the hard `node:fs` dependency from the data layer so a mobile backend (Capacitor Filesystem / SQLite) can swap in — the single biggest blocker to a Capacitor mobile port. Optional-with-Node-default keeps it a zero-churn pure refactor (existing tests are the safety net); the in-memory adapter proves the seam runs the whole vault off-Node. | Leave `node:fs` inline (blocks mobile); rewrite for mobile later (riskier, no seam); full repository pattern (heavier than needed). | Mobile follow-ups: abstract `node:crypto` content hashing and the Node `path` traversal-guard; add a Capacitor adapter; point the client `api` at an in-process data layer instead of HTTP. |
| 2026-06-23 | PDFs are anchored by page + quote (TextQuoteSelector), not by rect; the reader is a PDF.js (`pdfjs-dist` v6) canvas + text-layer component, replacing the native-viewer iframe. `rect` on `pdf_selection` is now optional (positional hint only). | Rect anchoring is zoom/version-fragile; quote-per-page is robust and reuses the same TextQuoteSelector model as web. The native PDF viewer exposes no selection/text to anchor on; PDF.js gives a real selectable text layer in the renderer DOM, which is also Playwright-testable (unlike `<webview>`). | Keep the native viewer (no anchoring); anchor by rect coordinates (fragile); a server-side PDF text-extraction approach (loses visual fidelity). | Lazy/virtualized page rendering for large PDFs; multi-span highlight precision; persist rect as a fast-scroll hint. |
| 2026-06-23 | Live web annotation uses a `web_live` Source (URL kept live, stored content = the URL, real URL in metadata) + `web_text_quote` anchors (W3C TextQuoteSelector) rendered in an Electron `<webview>` with a guest preload that reports selections and re-highlights anchors over IPC. Window preload switched to `sandbox:false` (contextIsolation stays on) to resolve the guest preload path. | Reuses the whole Source→Anchor→Note pipeline (one new source type + one new anchor kind) instead of a parallel URL-keyed store; TextQuoteSelector needs no injected ids so it works on third-party DOM and survives revisits; `<webview>` embeds cross-origin live pages an iframe can't (X-Frame-Options). The pure selector logic (`textQuote`) is unit-tested; the embed is `_electron`-tested. | A separate URL-keyed annotation store; snapshot-only (loses live interactivity); `BrowserView`/`WebContentsView` (harder to lay out in React); keep sandbox on + ship guest preload path another way. | Drive guest select→highlight in CI is unreliable → manual check; consider `WebContentsView` + a richer multi-node highlighter; per-author filtering of shared web notes; W3C TextPositionSelector as a fallback. |
| 2026-06-23 | Electron main boots the Express+vault server **in-process** and serves the built client from one origin (`createApp({clientDir})` + `startServer`); the renderer is the same React app loaded over HTTP. esbuild bundles `electron/main.ts`+`preload.ts` to `dist-electron/*.cjs` with `--packages=external`. Dev (`--dev`) loads the Vite server instead. | Reuses the entire web stack unchanged (no duplicate UI), keeps the renderer privilege-free (talks HTTP, not Node), and makes the prod path fully unit-testable via `startServer` + static-serving tests. esbuild (vs a TS main build pipeline) is fast and bundles our source while leaving node_modules external for electron-builder later. | Load client via `file://` + IPC for data (renderer needs privileged Node, harder to test); compile the whole server with `tsc` to JS (slower, config churn); electron-vite (extra framework). | Launch the GUI on a display machine; add webview-based live web-page annotation (the Electron-specific capability); package with electron-builder; preload currently exposes only `{desktop, platform}`. |

| 2026-06-25 | Unify the `webpage` (snapshot) and `web_live` (live) viewers into ONE Web viewer: a new `web` viewer kind routes webpage into the shared `WebviewReader` tabbed shell, whose tabs now carry a `snapshot`/`live` sub-mode. Each sub-mode keeps its OWN anchor path — snapshot tab = `DomReader` study-id `html_selection` (offline), live tab = `<webview>` guest `web_text_quote` (Electron). A snapshot can "Open Live" and in-snapshot link clicks open live tabs (new optional `DomReader onOpenUrl`). | To the user a saved snapshot and the live page are the same "web" surface; splitting them into two viewers meant the snapshot lacked the tab strip / back-forward / address bar that live web had ("why aren't these the same viewer?"). One shell unifies the UX while preserving the two irreducibly-different anchor mechanisms (a contentDocument we own vs. an IPC bridge to a separate WebContents). | Route webpage into the webview reader and drop the study-id snapshot path (loses offline-stable `html_selection` anchors + breaks existing webpage notes); leave them split (the status-quo gap); invent a third merged anchor kind (needless schema churn). | Carry the source identity across snapshot→live so live-tab notes fold back onto the snapshot; a webview-captured higher-fidelity snapshot (rendered+authenticated DOM); shared back/forward history for the snapshot tab. |
| 2026-06-25 | Study Layer V1 foundation: a new `layer` entity (`StudyLayer` = a shareable bundle of anchors+notes over one `SourceFingerprint`, with `importMode: owned/imported/subscribed` + `enabled` toggle). `anchor`/`note` gain an OPTIONAL `layerId` (+ anchor `matchStatus`, note `origin`); every source gets one lazily-created `owned` layer; a boot migration backfills pre-layer records. Importing re-locates anchors by a pure **rematch resolver** on the portable W3C TextQuoteSelector (`quote`+context), NOT by the author's `studyId`/`selector`/`sourceId` (stripped on `.studypack` export, rebuilt locally). `GET /sources/:id/anchors` hides disabled-layer anchors. | The portable locator already lived in the anchor envelope; the real work is re-realization + ownership, not a new selector (the reviewer's `domPath`/offset idea is the LEAST portable part). Optional `layerId` + backfill means existing vault data loads unchanged (the snapshot store drops schema-invalid rows, so a required field would have silently deleted every old anchor/note). Landing schema now avoids a painful retrofit once rich notes accumulate. | Make `layerId` required (drops legacy data on load); add a new portable `AnchorSelector` with domPath/offsets (non-portable across copies); only `note.visibility` with no layer entity (can't distinguish owned/imported/copied, can't toggle); build full sharing UX now (V2/V3). | V2: `.studypack` file UX — layer switcher, import-preview three-state (matched/fuzzy/unmatched), enable/disable toggle, copy-imported-note-to-mine, `anchor.rematch` for un-located anchors (portable info already stashed in `note.metadata.unmatchedAnchors`). V3: cloud/subscribed transport. code_range import + per-page PDF text rematch are best-effort/deferred. |
| 2026-06-25 | Study Layer V2 (local `.studypack` UX): a new `layer.switcher` workspace view (additive 5th pane in `studyVaultLayout`, like `concept.list`) lists a source's layers with an `enabled` toggle, per-layer Export (downloads `.studypack`), and an Import flow (file pick → non-persisting `import/preview` → three-state matched/fuzzy/unmatched summary → confirm → `import/commit`). Toggle is a registered `layer.toggle` command; export + the two-step file import live in the view (file IO + interactive preview), mirroring how URL-import/file-dialogs are view/context actions. A new context `refreshLayers()` (+ `layersVersion` token + `onLayersChanged` action) reloads the list AND repaints the reader (enabled layers drive which anchors the server returns). | The V1 server already backed list/patch/export/preview/commit; V2 is the UI seam. Doing it through the existing ViewRegistry/CommandRegistry/EntityClient keeps it additive (existing pane DOM + e2e untouched) and consistent with the concept pane. Keeping export/import in the view (not commands) matches the codebase's treatment of file/IO flows and avoids forcing two-step interactive state through the command payload. | Force the file import + preview through a fire-and-forget command (awkward two-step + file IO); embed the switcher inside the study pane (cross-view coupling, breaks the iron law); make the checkbox optimistic/local (drifts from the server's enabled state). | `copy-imported-note-to-mine` (needs `POST /notes/:id/copy-to-layer`), `anchor.rematch` + a match-inspector view for un-located anchors (portable info already in `note.metadata.unmatchedAnchors`), and a `layer` FocusTarget + note-list-by-layer filter — all deferred. The 5-pane layout bumped `body min-width` to 1740px. |
| 2026-06-25 | Product Kit = a register-only vertical-product plugin pack (`ProductKit` + `KitInstallContext` in `src/kits/`). It changes NO core data structure; it registers domain-prefixed note `contentType`s (e.g. `textbook.explanation`) over the existing `Note.content=unknown` + `NoteContentSpec`, plus (later phases) commands/views/layouts/surface contributions, and a language map renaming core concepts. Server (`createApp`→`installServerKits`) registers a kit's React-free `contentSpecs` for API validation; client (`views.tsx`→`clientKits`) installs the full kit. **Phase 1** ships the Textbook Learning Kit's 3 Study Blocks (Explanation/Practice/Mistake: spec §4 schemas) with render cards + edit forms, appearing in the composer picker + note list with zero App/Workspace edits. | The base already has the four registries the kit needs (NoteType/Command/View/Layout); a vertical product is "batch-register into them", not a fork. Domain-prefixed contentTypes over `content: unknown` give rich product objects with no core schema churn. Keeping core kit-free (composition roots wire kits) preserves the iron law "a kit owns the interpretation, not the data". | New core entities per block type (schema churn, breaks portability/sharing); writing the textbook flow straight into App (un-pluggable, pollutes core); a parallel kit registry instead of reusing NoteTypeRegistry. | Phase 2: AI commands (explain/practice/mark-mistake) needing a STRUCTURED-output capability (the provider only does free-text markdown today) + a selection-toolbar surface (none exists yet — selection fills the chat chip). Phase 3: Review Pack + teacher-layer export/student import (reuse Study Layer V1/V2) + mistake-layer-private policy + student layout preset. |
| 2026-06-25 | Product Kit phase 2 (Textbook AI loop): add an OPTIONAL `ModelProvider.completeStructured(req)→{json}` (mock echoes a host-supplied schema-valid `sample` → deterministic), a React-free `generateStructuredContent()` (build kit prompt → JSON → `extractJson` → validate against the contentType's `NoteContentSpec.schema` → retry; unknown/invalid → `StructuredGenerationError`→400), endpoint `POST /api/kits/generate`, a React-free prompt registry (`ProductKit.prompts[]` registered by `installServerKits`), 3 kit `Command`s (explain/practice/mark-mistake) wired via `KitInstallContext.commands`→`CommandRegistry`, and a host `SelectionToolbar` reading priority-sorted `surfaces.contribute('selection-toolbar',…)`, mounted under the chat Source chip. | The structured path is the one genuinely missing capability (provider was free-text only); putting parse+validate+retry in a host helper (not each command) and the deterministic sample in the kit prompt keeps the mock generic (no layering inversion: `src/ai` never imports a kit) AND e2e deterministic. Commands reuse the exact `anchor.add-note` shape (materializeAnchor→createNote). The toolbar is the surface the kit's contributions needed; empty/absent with no kit installed (zero base impact). | Hardcode textbook fixtures in the mock (inverts layering); a function-call/tool API on the provider (the CLI providers don't expose one yet); make every command own its parse/retry (duplication); put the toolbar in core (kit-agnostic but then unused). | Phase 3: Review Pack (`textbook.review-pack`) from a chapter's Explanation+Mistake; teacher-layer export / student import (reuse Study Layer V1/V2); mistake-layer-private policy; student layout preset. Real `claude-cli`/`claude-pty` use the text→JSON fallback (no `completeStructured`) — tune the JSON-only prompt + retry against the live CLI. |

| 2026-06-25 | Product Kit phase 3 (Textbook MVP close): add `textbook.review-pack` (a SOURCE-level 4th Study Block, spec §4) + a `textbook.generate-review-pack` command that gathers the source's Explanation+Mistake notes → structured review pack, surfaced via a NEW `source-actions` surface slot (host `SourceActionsToolbar`, top of the study panel) — not the selection toolbar, since it's chapter-level. Add a React-free kit **layer-propagation policy** registry (`src/kits/policy.ts` + `textbook-learning/policy.ts`): the export filter in `buildStudyPack` drops notes whose contentType is `privateByDefault` (textbook.**mistake**) + any anchor referenced only by dropped notes, so a teacher's exported `.studypack` never ships student mistakes. Register a `textbook.student-learning` column layout preset (`KitInstallContext.layouts`). | Closes the §16 loop register-only. Review Pack is source- not passage-scoped, so it needs a source-level surface (none existed → add the generic `source-actions` slot, kit-agnostic). The propagation guarantee must be a HARD server-side export filter (visibility default already makes mistakes private locally, but the layer is the export unit, so contentType filtering is what actually protects them); keying off `privateByDefault` keeps generic/non-kit notes exportable. | Put Review Pack in the selection toolbar (wrong scope); enforce privacy via `note.visibility` only (doesn't gate layer export); exclude-only-`exportableContentTypes` (would drop generic markdown notes); build the full §8 dock layout engine now (out of scope/risky). | Full dock layout (left nav / center / right tutor / BOTTOM practice+mistakes) + a layout-switcher UI to activate the preset (base shell renders columns only); `copy-note-to-mine` + `anchor.rematch` UI (Study Layer V2 backlog); `code_range`/per-page PDF rematch on import. |

| 2026-06-25 | Per-source Product Kit activation: a source picks its kit(s) instead of kits being globally on. Data is an array `source.metadata.activeKitIds` (core metadata, no schema change); UI is single-select (reader-header "Kit" dropdown: Core + installed kits). Effective kits = the source's array (empty = Core) else a workspace default (`localStorage["sv-default-kit"]`, initial `textbook-learning`). Activation gates ONLY creation/interaction entry-points (selection toolbar, source-actions, composer type picker, kit commands, language) by set membership `itemKitId ∈ activeKitIds`; note-type RENDER + content specs stay globally registered. `installClientKits` tags each registration with its `kitId`; `kitSurfaceItems(slot, kitIds?)` / `noteTypeOwnerKit` apply the gate; `setActiveKit` persists via a new generic `PATCH /api/sources/:id` metadata-merge endpoint then reloads sources. | Lets one base app host multiple verticals per-document without forking the shell. Data-as-array keeps multi-kit a UI-only change later while shipping a simple single-select now. The render-stays-global rule is essential: shared/imported kit Study Blocks (Study Layer) and blocks created before switching a doc to Core must still display — so only creation is gated. Workspace default preserves the current "open a doc, tools are there" behaviour. | Global-only activation (status quo — can't mix verticals per doc); a real `activeKitId` scalar (blocks future multi-kit, forces a later migration); gating rendering too (would hide imported/shared kit notes — breaks Study Layer); a dedicated activation entity (metadata escape hatch is enough, avoids a core schema change). | Workspace-default-kit settings UI (default hardcoded for now); multi-kit activation UI (data ready); per-kit language term substitution across the wider UI (`kitTerm` is gate-aware but not yet consumed in the shell). |

## Template

| Date | Decision | Rationale | Alternatives Considered | Follow-up |
| --- | --- | --- | --- | --- |
| 2026-06-25 | Layout engine (phase 1): `WorkspaceShell` recursively renders a DOCK TREE (`layout.layout` = nestable `split`/`leaf`, `src/client/workspace/dock.ts`) instead of a fixed row of grid columns. Unified onto one engine — existing presets re-expressed as a single-row dock tree (not dual-track). Root split is the `.app-shell` flex container; leaves keep each view's own class/DOM. | A real workspace needs nesting (reader + bottom panel, left/center/right student layout); the grid shell couldn't express it and `layout.mode`/`layout` were unconsumed. A leaf only references a view by `nodeId`, so ViewRegistry/view plugins are untouched. | Dual-track (keep grid, add dock for new layouts); a third-party dock lib. | Phase 2: pane collapse, layout switcher, student layout preset, minimal responsiveness (drop `body min-width:1740`). Later: drag-to-dock, canvas. |
| 2026-06-25 | Layout engine (phase 2): pane collapse/expand + layout switcher + student preset + minimal responsiveness. Collapse is pure (`isCollapsibleLeaf`/`isPaneCollapsed`, `dock.ts`): a fixed-size non-reader leaf collapses to a `RAIL_PX` rail; effective state = explicit toggle (persist `sv-pane-collapsed`) OR secondary pane (`library`/`concept.list`/`layer.switcher`) auto-collapsing below `RESPONSIVE_BREAKPOINT_PX=1280` (`<`, so the 1280 test viewport stays expanded). The collapse chevron is an ABSOLUTE overlay (no DOM shift → view classes/order preserved). Active layout lives in `WorkspaceContext` (`activeLayoutId`, persist `sv-active-layout`); `App.tsx` renders `getLayoutPreset(activeLayoutId)`; picker = `.layout-select` in the reader header. `studentLearningLayout` is the first nested preset (`column` split → reader over a `practice` panel). Responsive overflow: replaced `body min-width:1740px` with `.app-shell{overflow-x:auto}` + reader pane `min-width:360px`. | Real workspaces collapse/switch panes; doing it as pure predicates keeps it unit-testable and keeps rendering global (only chrome changes). Absolute overlay button avoids shifting the view DOM (kept e2e selectors stable). The overflow swap is what fixes the regression the naive `min-width:0` caused — five expanded panes (1300px fixed) crushed the reader to 0 at 1280; now the shell scrolls and the reader keeps a usable min, while narrow screens collapse secondaries to fit with no scroll. | Drop the body min-width with nothing else (crushes the reader at 1280 — caused 10 e2e failures); a fixed wider body min-width (responsive collapse then saves no space — still scrolls); gating render on collapse (would hide content). | Drag-to-dock / pane reorder; workspace-default-layout settings UI; multi-pane student presets. |
| 2026-06-25 | AI orchestration base v1 (foundation + streaming): sink the structured-generation engine into a KIT-AGNOSTIC base layer (`src/ai/structured.ts`: `generateStructured(provider,{messages,schema,sample?,contentType?,context?},maxAttempts)` + `extractJson` + `StructuredGenerationError`); `src/kits/structured.ts` becomes a THIN wrapper (resolves kit prompt + the contentType's `NoteContentSpec.schema` + deterministic sample, delegates to the engine; re-exports the error/extract helpers so import paths are unchanged). Add `ProviderCapabilities.streaming:boolean` + optional `ModelProvider.stream?():AsyncIterable<string>` (mock `streaming:true`, `complete()`/`stream()` share one `answer()` → byte-identical; `claude-cli`/`claude-pty` `streaming:false`). Add `POST /api/chat/stream` (SSE `chunk`/`done`/`error`, validates before streaming so a bad body still 400s, single-chunk `complete()` fallback when the provider has no `stream()`); `/api/chat` + `/api/kits/generate` untouched. Client `entityClient.chatStream(input,onDelta)` consumes SSE with non-streaming `chat()` fallback; `anchor.ask-ai` feature-detects streaming + progressive `onAssistantChunk` render. | The structured engine and the streaming transport are both pure mechanism — they belong below the kit boundary so any caller (kits now, agent loops later) reuses them, and so the iron law "`src/ai` never imports a kit" is enforced by the dependency direction (kit passes a generic zod schema + sample IN). Streaming is additive: a NEW endpoint + an optional capability + feature-detection means `/api/chat` and every existing test/flow are bit-for-bit unchanged, and a non-streaming provider degrades through one server-side single-chunk path rather than each caller special-casing it. Sharing the mock's `answer()` across `complete`/`stream` is what makes "progressive arrival" e2e-assertable while keeping the final string deterministic. | Keep the structured engine inside `src/kits` (inverts layering — `src/ai` couldn't reuse it, agent loops would re-implement parse/retry); a function-call/tool API on the provider for structured output (CLI providers expose none); stream over WebSocket (heavier than SSE for one-way token push); make every caller wrap non-streaming providers (duplication — the server does it once); reuse `/api/chat` with a `stream` flag (risks the existing contract its tests depend on). | Real-provider native token streaming (`claude-cli`/`claude-pty` are `streaming:false` → server single-chunk fallback; tune a token stream off the CLI/PTY later); tool-calling / agentic loops; embeddings / semantic retrieval; `buildChatContext` refactor — all explicitly out of scope for v1. Verify the streaming + structured paths against an authenticated `claude-cli` on a display machine (subscription mode strips `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN`). |
| YYYY-MM-DD | TBD | TBD | TBD | TBD |
| 2026-06-30 | Library now treats opened folders and reading history as separate sections: `Open Folder` owns a non-persisted multi-root file tree list, while `Recent Read` is an always-visible source list ordered by locally persisted recent source ids. | The user wants folders to be browseable without hiding Recent Read, and wants multiple folders with per-folder close. Keeping folder roots session-only avoids silently persisting local disk paths; persisting only source ids gives a useful recent-read ordering over the existing Source records without a schema/server migration. | Keep the old single `folderRoot` mode (hides Recent Read after opening a folder); persist folder roots in localStorage (more privacy-sensitive); add a server-side recent-read entity (too heavy for a sidebar UI pass). | Later: promote recent-read timestamps to source metadata or a workspace preference if cross-device/history semantics become important. |
| 2026-07-04 | SPEECH-3 注音 ships as a UNIVERSAL text capability riding the SPEECH-1b surfaces: pure `pinyin.ts` engine (pinyin-pro `type:"all"` — word-level segmentation resolves 多音字 from context) + a CENTERED `PinyinPopover` ruby dialog + one shared `PinyinButton` (the CJK gate lives inside it) mounted on the GlobalSpeakSelection chip and the reader SelectionFloatingToolbar via a new `SelectionToolbar.onPinyin` opt-in; popover STATE hoisted at host level in `usePinyinPopover`. | Same law as 朗读: 注音 is a capability of TEXT — one implementation behind a shared contract, not per-view bolt-ons (abstract-recurring-capabilities). Centered dialog over selection-anchored: selections live in scrolling panes/iframes, and a centered card is the robust kid-friendly V1. Hoisted state: any click (including inside the popover) collapses the selection, which unmounts the chip/toolbar — the dialog must outlive its opener. Gate inside PinyinButton: every host suppresses latin-only selections identically for free. | A selection-anchored popover (stale rects in scrolling panes); inline ruby over reader bodies now (reader files = the contended D-track batch; deferred as the N-batch enhancement); per-surface popover state (dies with its opener); naive per-char pinyin lookup (wrong 多音字 — defeats the point of pinyin-pro). | Reader-body inline ruby (注音模式 toggle over the render path, N-batch); Settings Hub 注音 toggle + kit `young:{pinyin:true}` defaults (F5 config slot); the chip currently inherits the SPEECH-1b speech-status gate — consider decoupling 注音 (pure client-side) from TTS availability for offline use. |
| 2026-07-04 | X1-001: the packaged desktop app ships an esbuild-FULLY-BUNDLED main (`dist-electron/main.cjs` = electron main + Express server + all pure-JS deps; `--external` only electron/node-pty/@anthropic-ai/claude-agent-sdk/@openai/codex-sdk/bufferutil/utf-8-validate), so `package.json dependencies` is reduced to the two on-disk runtime modules (node-pty, claude-agent-sdk — both `asarUnpack`ed; node-pty trimmed to its win32-x64 N-API prebuilds with `npmRebuild:false`) and everything else becomes a devDependency — electron-builder then ships exactly prod deps + transitives (the standard single-package electron layout). `@openai/codex-sdk` is NOT shipped (its `@openai/codex` vendors a 308MB codex.exe; the provider degrades to a lazy-import error only when selected). Packaged vault root = `app.getPath("userData")/vault` via the pure `resolveVaultRoot` (STUDY_VAULT_ROOT wins; dev keeps `cwd/data/vault`). Updater = electron-updater `checkForUpdatesAndNotify()` on packaged boot against GitHub Releases (tingxia1028/growHTML), errors logged (electron-log → `<userData>/logs/main.log`), never boot-blocking; publishing manual. Builder packages from a STAGED clean electron dist (`electronDist` + `scripts/prep-electron-dist.cjs`) instead of zip-extract. | Shipping full node_modules would drag every client-only dep (grapesjs/mermaid/pdfjs/…, hundreds of MB) into the installer for nothing — the deps are all consumed by bundlers; the two exceptions are exactly the packages that need real files at runtime (native .node binaries; a spawned vendored cli.js). N-API prebuilds make node-pty rebuild-free across Electron versions, and its existing lazy `await import` means a broken native never blocks boot (terminal is hidden/deferred anyway). The zip extract→rename path EPERMed persistently on this machine (a directory-handle holder); staging a local copy also keeps the dev launcher's branded Growte.exe (locked by the live dev client) out of the package without killing that session. | Ship pruned-but-full node_modules with `--packages=external` (300MB+ of client-only deps, plus electron-rebuild needed at package time); bundle the claude SDK too (breaks its spawn-by-real-path of cli.js); ship codex-sdk (+308MB for one optional provider); kill the running dev client to delete its branded exe in-place (hostile to the parallel session); `asar:false` (slower loads, no integrity resource). | macOS target when the Apple account lands (multi-platform.md §3 spec ready); CI release job = X4; installer size trim option = exclude `claude-agent-sdk-win32-x64` (215MB claude.exe) if a user-installed CLI mode is added; a Settings 关于 "检查更新" button riding the same autoUpdater seam (SettingsHub contended this window); Windows code signing (SmartScreen) later. |
| 2026-07-04 | LIB-2-001: the Library pane is rebuilt as REGISTRY-RENDERED sections behind ONE `+`. Contribution points `LibrarySection {id,title,order,count,emptyText?,render}` / `LibraryAddAction {id,group:导入|新建,title,run|render,disabled?/hint}` in `librarySections.tsx`; core seeds its own 最近/文档/文件夹 sections + import/create actions through the SAME registries via a `libraryBuiltins.tsx` side-effect import (shell-primitive rule — core-registered, not uninstallable, no bypass). Sections partition by CONTAINER; types are filter chips on 文档's header DERIVED from the sourceTypes present (webpage/web_live → one 网页 chip); kit sections = views over the same sources, never containers. **新建 resolved to a minimal wire, not a deferral**: 文档… calls the EXISTING `entityClient.ingestHtml` (`POST /api/sources/html`, the onboarding/W3 route) with a seeded HTML doc and opens it — markdown blank-create templates + entityClient bindings + 纯编辑模式 stay SRC-1/2 and will register into this same 新建 group. **.xmind folded into the ONE 文件… picker** by extension routing inside `WorkspaceContext.openFileDialog` (extracted `importXmindFromPath`; `importXmindFile` kept delegating for compat). Minimal typed I18N seed pulled forward (`src/client/i18n`: zh default + complete en, `defineMessages`/`t`/`LocalizedText` — missing translation = compile error); ALL Library strings routed through it. New styles live in scoped `library.css`; header search = client-side title filter over section items with the SEARCH-1 mount seam commented. | The blessed layout demands kit-extensible sections + a single add entry; reusing the repo's registry idiom (builtinNoteTypes side-effect seeding, disposer-returning registers) keeps kits additive and fixtures clean. `ingestHtml` is the only client-reachable create seam today (entityClient is contended this window — no new binding possible) and source-authoring.md §2 already ruled creation-through-existing-ingest conflict-free. Extension routing beats a second menu item because the native dialog has no filter — one gesture, one picker, no duplicate dialog code. | Per-type sections (shreds the list — rejected in the design); keeping a separate Import .xmind item (two pickers for one gesture); deferring 新建 entirely (the seam exists and is proven); building the I18N provider/persistence now (I18N-1 scope, not needed for static labels); putting Library styles in styles.css (contended by the parallel UI session). | SRC-1: markdown blank-create + editor register as 新建 actions; SEARCH-1 replaces/feeds the header input; Textbook Kit's 教材 section rides FLAT-1 capability groups; re-point any Library-specific e2e selectors (kebab menu + `.library-body-split` are gone) in the next e2e pass. |
| 2026-07-04 | TRUST-3-001: soft delete = `deletedAt` on the SHARED record envelope + tombstone-aware SnapshotStore (`list()`/`get()` live-only; new `listTrashed()`/`getAny()`; `delete()` stays REAL for purge; rewrites keep tombstones). Cascades carry `metadata.trash={cascadeOf:parentId}` (source→its notes/anchors/patches; note→its orphaned anchors); a source soft delete KEEPS the stored file. Trash surfaces in `src/server/trash.ts` (list/restore/永久删除/清空回收站?confirm=清空回收站 + 30d auto-purge scheduler, clock-injected, armed under dataTrust.scheduleAuto with STUDY_VAULT_TRASH_AUTO_PURGE=0 kill switch; retention via STUDY_VAULT_TRASH_RETENTION_DAYS). Restore conflicts: note whose source is not live → 409 (source-in-trash / source-missing) — restore the document first; stale layer memberships pruned on restore. Explicit export choice: tombstones RIDE the verbatim full-vault backups/exports; outward reads (svpack/report/search/queue/digest) exclude via live-only list(). UI = registered `trash.panel` view + UserMenu 数据 回收站 entry. | Store-level filtering is the ONE guard every consumer inherits (zero per-callsite edits, impossible to forget a surface — the doc's "excluded from every list/search/queue/digest" literally); tombstone-in-place keeps every relationship intact so restore = clear the flag (no re-linking); cascade markers make "restore resurrects exactly the cascade" derivable without guessing; keeping files on disk is what makes source restore lossless; verbatim exports must not lose recoverable data and TRUST-2's byte-identity/count validation already counts raw lines. 409-on-dead-source beats silently detaching a note into an unreachable state. | A separate trash jsonl/dir (loses relationships, duplicates store machinery, and the doc explicitly chose the envelope flag); filtering at each read site (guaranteed to miss one); hard-deleting orphan anchors as before (restore couldn't repaint highlights); auto-restoring a trashed source when restoring its note (surprising blast radius); per-item typed confirm (friction — typed phrase reserved for 清空回收站, the TRUST-2 idiom). | Settings-hub 数据 section placement (retention config UI) stays deferred with the TRUST-1 hub work; direct-transport (mobile X0b) trash routes when that adapter grows a delete surface; consider surfacing cascade children individually in the trash view if users ask. |
| 2026-07-04 | N1A-D2-001: user amended D2 — anchor-chip click = per-anchor notes TOGGLE (session-scoped, realm memory, no persistence), NOT open-card; plus a global 显示锚点标记 switch in the Anchor panel (persisted via the annotation-mode localStorage idiom, hides anchor glyphs only). Both slots still emit onMarkerAction (host focus bridge / guest sv:marker-action unchanged ⇒ anchor click = toggle + focus). Toggle state lives in annotationLayer's per-document store (ONE store for chip+card+margin — wireNoteCard gate + dismissIfKey + paintMarginNotes memo/filter); global value lives in a markerOverlay module store reaching guests as an additive sv:anchors third payload. | Anchor glyph as a notes on/off switch matches the user's mental model (anchor = place, notes = attachments); one per-document store avoids three parallel visibility states drifting; additive payload keeps older hosts/guests compatible. | Open-card on anchor click (the doc's original wording — superseded by user decision); per-reader visibility forks (violates the F3 contract); persisting per-anchor toggles (session noise, not a durable preference); new IPC channel for the toggle (guest already bundles the shared modules). | N1b: same-line clustering + card-open suppression + D5 floating editor; Codex i18n sweep will bilingualize the switch label; .sv-switch unification when the parallel session lands styles.css. |
| 2026-07-04 | SRC-12-001: markdown sources store RAW markdown; the reader/anchor pipeline sees a DETERMINISTIC projection (existing markdown renderer + injectStudyIds at render time) — the editor round-trips clean markdown while anchors bind to rendered-DOM text, which is also why save re-projects by quote+context via the import machinery (toPortable → rematchAnchor → resolveLocalStudyId), never by studyId. Blank-create = untitled + auto-编辑 (no title prompt; inline title; title-only rename doesn't bump revision). Live-preview render lives in src/client/sourcePreview.ts OUTSIDE the §0.5-B-guarded workspace dir: it renders a source BODY, not note content — contract guard intact. "Shared" for the edit warning = publish-ledger entries OR plaintext imported layers. | Raw-md storage keeps the editor lossless and the projection deterministic; quote+context re-projection is the ONLY strategy that survives studyId shifts on every edit; auto-编辑 gets a kid typing in seconds; imported guard is server-side (400) so read-only isn't just UI. | Storing rendered HTML (lossy round-trip); rematch by studyId (order-based, shifts on any edit); title prompt before create (friction); TipTap WYSIWYG now (dep + schema-lossy for arbitrary HTML; V1.1). | SRC-3 patch-apply + imported fork (+ import-path markdown rematch nuance noted in-doc); SRC-4 GrapesJS; SRC-2b HTML 所见即改 queued; sealed-pack anchors not re-projected (V1 accepted). |
| 2026-07-04 | SRC-2B-001: in-place editing = contenteditable iframe + pure DOM style engine, zero new deps; sanitize is a BLACKLIST (strip editing artifacts/on*/js-URLs, keep everything else incl. user scripts + data-study-id); output HTML kept simple (b/i, real heading tags, span inline styles, text-align on block); 源码 raw editor stays as a sub-toggle. | It's the user's OWN document (whitelists destroy user markup); execCommand is deprecated + untestable in jsdom; sandbox without allow-scripts already neutralizes scripts during editing; keeping scripts matches the raw-editor behavior (no regression). | ContentTools (dep); GrapesJS (sealed for SRC-4); TipTap (schema-lossy); execCommand; whitelist sanitizer. | Style-bar active-format indication; e2e spec rides the e2e suite; multi-block inline wraps kept simple-by-design. |
| 2026-07-04 | ACTION-2B-001: auto-output run contract client-side — operation.run drops the outputType requirement; the RESPONSE's contentType (form-router verdict) becomes the draft's type, flagged autoForm so Regenerate re-omits contentType (server hard-rejects pinned types for auto ops and may re-route). Simple save sends outputContentType only when pinned ("" = AUTO = key omitted). Convert simple→template is one-way with confirm (template seeded from instruction + {{anchorText}}). V1 chrome demoted to 高级. | Two fields is the entire required surface (user law: 交互要简单); one-way convert avoids a lossy template→sentence back-mapping; autoForm prevents Regenerate from pinning a routed type. | Keeping the V1 builder as the default (complexity the user explicitly rejected); two-way convert (lossy); client-side form guessing (router owns it). | Server nullable/clear convention so PATCH can un-pin output type; SURFACE_TABS Bookmark title bilingualization (shared BOOKMARK_ACTION const); real-provider auto-form e2e. |
| 2026-07-04 | FLAT-1-001: (a) capability-group toggles shipped in manager V1 (doc §2 wins over a read-only descope — without toggles the opt-in subject groups become unreachable since the market no longer sells subject kits); (b) standalone plugins (flashcard/quiz/bookmark/diagrams/table-viewer) demoted to hidden always-on infrastructure — cataloged internally, never listed/managed; (c) subject ProductKits kept as RUNTIME registration vehicles (per-subject language/detection) — the flatten is presentation + install-state only; (d) migration detection is structural (legacy kit ids / group-owned direct holds trigger it; output contains neither ⇒ idempotent, no version field). | One user-facing unit is the blessed law; group toggles are the minimum viable replacement for subject-kit opt-in; structural detection avoids a schema version dance; keeping registration vehicles avoids a contribution rewrite. | Read-only groups V1 (unreachable subject opt-ins); version-field migration (needless schema churn); rewriting contributions to one kit id (breaks pins/detection/uninstall-data law). | WorkspaceShell modal title 套件 (LEFT-COLLAPSE territory); marketplace M1 rides the kit-only catalog; user-defined kit builder UI later. |
| 2026-07-04 | REV-3 调度持久化 = review-schedule.json 独立行文档,而非 note.review 事件重放 | MEM-2 整合 14 天后剪除原始事件(memory.ts §2),重放派生的 schedule 两周即崩;vault JSON 同 memory-settings 惯用法,零迁移(无行=到期)。附带语义变化:SRS 下 pass 不再永久退休错题(按间隔复现;弱项拉回不越过未到期间隔),legacy 模式(不传 now)逐字节保留 | note.review 事件重放(14 天后崩);另立 SRS 库/DB(重复存储机制);发明 4 档 again/hard/good/easy(载荷只有 pass/fail/skip) | 老师可见排期; D7/N4 交互全模式复习 |
| 2026-07-04 | CG-123 + N1b 作一个联合波次提交,不拆分 | 两并行代理在 WorkspaceContext.tsx + commands/registry.ts 交错改(9 concept vs 7 floating hunks),按 hunk 拆会造出各自不独立编译的假 commit——违背外科提交"每个 commit 单独可编译"的初衷;真实最终树 tsc 0 + 2097 绿是比隔离验证更强的正确性信号。关联图=core 引擎(用户法则),插件经 graphLens 只改样式不改结构;力导向手写零重依赖 | 强拆 hunk(假 commit 风险);引 d3-force/cytoscape(重依赖) | CG/N1b 的 e2e spec(代理未及写);老师视角图谱 |
| 2026-07-04 | SMALLFIX-001: (a) 会话 resume 防覆盖用独立 userActedRef 闩锁而非复用 token/count(startNew 会把后者重置成 fresh 态,与"从没动过"不可区分);(b) 输出锁定用 null=清除约定(update-only;create 仍拒 null),客户端编辑态发显式 null 因 undefined 被 JSON 丢弃;(c) markmap SVG 加绝对尺寸属性让 d3-zoom 可解析长度;(d) 样式条激活态用纯 queryFormatState 复用 apply 的同款检测,保证"按下"永远预测 toggle | 复用可变 ref 判用户意图会误判;undefined 静默不达服务端;% 宽度 d3 不可解析;镜像检测保证 UI 与引擎一致 | 复用 token/count 判定(误判);发 undefined un-pin(被丢弃);execCommand 查询状态(废弃且 jsdom 不可测) | Codex CLI 后台需 stdin 重定向(< /dev/null)才不卡;kit-activation/generation-preview spec 现代化候选仍挂 #46 记录 | 
| 2026-07-04 | SRC-3-001: apply 把编辑烘焙进存储(SRC-2 管道)而非旧的 render 时 materializeHtml;renderSource 排除 applied 补丁避免双重应用;revert 把应用前字节暂存于 patch.metadata.srcPriorContent(信封逃生舱,无 schema 改动);rewriteSourceContent 从 updateAuthoredSource 抽出共享(补丁绕过 authored-only 守卫:导入源是补丁的法定目标 §3);fork IO 落 sourceAuthoringIo.ts 而非受争用的 entityClient | 兑现 §4"经同一保存→重算→重投影管道重写";保留单一 matcher;把 entityClient/chat-attachments 留给各自 owning 代理 | render 时应用(双重应用风险);另发明 SRS 库;两向 template 转换 | 导入 rematch 对 markdown 匹配 RAW markdown 文本的后续处理 |
| 2026-07-04 | N5-001: D10 实时持久化留 localStorage CardGeom 惯用法(设备本地),不走 vault note.display 写路径 | 设计文档"buildStudyPack 已带笔记记录、可搭车"不准——实测 buildStudyPack 把笔记剥成 portableNoteSchema,display 要改 pack.ts + create/update 服务 + app.ts + entityClient(全 sibling-hot/SRC-3 邻接);N5 干净搭 N1 的 localStorage 底子零服务端改动;note.display 作为 additive 未来钩子加入 schema,导出是自含客户端包 | vault 写路径(sibling-hot 冲突);服务端导出路由(app.ts 争用) | note.display 随 .svpack 携带(pack.ts + note 服务 follow-up) |
| 2026-07-04 | SEARCH-2-001: 拼音复用已发布的 pinyin-pro(SPEECH-3),不手搓表也不引新依赖 | 零新增依赖/零增量包体;纯离线 JS 服务端可跑;词级词典处理多音字;隔离在 src/core/search/pinyin.ts 使共享 rank core 保持无依赖。路线图"pinyin lib decision rides SC-3"就此解决 | 手搓紧凑拼音表(多音字覆盖不全+维护负担);引新拼音依赖(包体) | 面板 type/source/date 过滤 UI(服务端已就绪);SEARCH-2 fuzzy 的 CJK 扩展 |
| 2026-07-04 | 收尾波次三路 hunk 拆分入库(app.ts/directTransport.ts/views.tsx 共享,按 disjoint 行段拆),SRC-3 独立子树经隔离 worktree tsc 验证 | 三特性文件集不相交、共享文件 hunk 位置分离(与 CG+N1b 的交错不同),可干净拆成三个各自可编译的 commit;SRC-3 是唯一"新颖子集"故隔离验证,commit 2/3 逼近已知绿的合并树 | 单一联合波次(CG+N1b 式,牺牲 bisect);全量隔离验证三 commit(用量墙下过费) | W2(chat 附件)、SC-1/2/3、M-B 下一波 |
| 2026-07-04 | SC-1-001: slash palette 经 scoped `.chat-slash-palette`(position:absolute 抬出作曲器 flex 行)向上弹出,而非内联 | 内联时它成 flex 兄弟顶下 textarea;absolute 保持作曲器条稳定、palette 在自己 264px 滚动盒内向上生长。SC-1 真实未完工的只有此定位 CSS + e2e,接线本身 N1b/02c0476 已落 | 内联 flex(顶下输入框);重造第二作曲器(违背单一 slash 惯用法) | SC-2(工具栏表面:选区/anchor/底栏);SC-3(palette 里的 operations、拼音、kit/memory 排序、市场有效安装列表源) |
| 2026-07-04 | M-B 核实:KaTeX ADOPTED(实测非记忆):77.46kB gzip 懒 chunk、SSR-safe renderToString、trust/throwOnError 硬化;renderMath 保持同步(string→dangerouslySetInnerHTML)而非 diagrams.ts 的 async await-import 形——打包器已对静态 import 懒拆(已证),async 化会破坏同步 render({mode}) 契约+测试。§8.1 有意权衡 | 兑现"懒加载不膨胀主包"的 outcome 同时不破坏同步渲染契约;避免回归一个已通过的已 ship 特性 | 强行 async 化 Latex.tsx(破坏同步契约与测试) | M-C(剩余 8 类型 + 语文/理化生组 + 导入提示,市场 M3) |
| 2026-07-05 | M-C-001: 共享学科插件(subject-formula∈数学∩理化生, excerpt∈英语∩语文, figure∈史地∩语文)只列入其 home 能力组,不进每个学科的组 | 否则迁移 legacy subject-math 安装会经共享 subject-formula 把 subject-experiment 漏进 effective-installed;跨学科可达用全局渲染 + kit language + catalog provides 保证 | 每组都列共享插件(过度激活);按组成员表达跨学科(泄漏) | M 轨全 11 类型 5 kit 完成;市场售卖 M3 仍 defer |
| 2026-07-05 | N6-001: D12 板开态用模块 store(anchorFocusBoardStore, useSyncExternalStore)而非 WorkspaceContext 字段 | 本波 WorkspaceContext 是 N3 的受争文件;且开态是瞬时视图态非持久 workspace 数据;纯模型拆 anchorBoardModel.ts 避 Windows 大小写撞 AnchorFocusBoard.tsx | 放 WorkspaceContext 字段(撞 N3);持久化开态(非必要) | V1 单文档;多文档下的板归属随 F1 |
| 2026-07-05 | N3-001: D6 auto-materialize 为 OPT-IN(默认关),非直接按钮的一律默认 | 翻转 operation.run/kit-generate 默认会破未跳过的 operation-authoring e2e,且无 per-surface prefs 系统无法scoped;gate 在 additive GeneratedDraft.autoMaterialize(默认 off)使所有现存生成路径逐字节不变,只接路线图点名的锚点 chat 回复路径 | 全局默认 auto(破 e2e);改 M-C kit 命令(跨域) | 直接锚点/选区按钮 opt-in(需 per-surface preview prefs) |
| 2026-07-05 | F1-D1: source.tabs 作宿主 VIEW 而非新 `tabs` DockNode | 仿 RightSidebarTabs/rightSplit.ts 加标签容器,布局引擎(dock.ts split|leaf)零改动零爆炸面;tabs DockNode 原语 defer | 新 DockNode 变体(大爆炸面);固定列 | tabs DockNode 原语 |
| 2026-07-05 | F1-D2: host-realm 分屏门槛(pdfjs/image = 同时仅一并发面板) | 两 host-realm body 共享唯一 host #sv-note-card + 模块 notesHiddenAll/anchorGlyphsVisible 单例 + HideAllNotesToggle per-source 播种效果;canSplitConcurrently 拒两 host-realm,分屏仅 iframe/webview | 允许(上色冲突);按 paneId 拆单例(热文件重构) | 按 paneId 拆 annotationLayer/markerOverlay 单例(含 F-1) |
| 2026-07-05 | F1-D3: 库点击=切换聚焦面板,Ctrl/Cmd-点击=新面板 | 每次点击加标签会在共享 vault 上破几乎所有单标签 e2e;切换保单文档字节不变(垫片一致),显式 openSourceInNewPane 是多文档入口 | 每次点击加标签(破 e2e) | — |
| 2026-07-05 | F1-D4: 跨源共享笔记两面都画 V1 仅客户端 | 服务端 listNotes 按 note.sourceId 过滤;端到端跨文档上色需 listNotes 也匹配 anchor.sourceId = P-B;客户端选择器已在 paneSelectors.test.ts 证明 | — | P-B: listNotes-by-anchor.sourceId |
| 2026-07-05 | F1 实现审查 F-1(SHOULD-FIX,非 blocker):两 iframe 分屏共享全局 notesHiddenAll,一面板全隐藏影响两面板 | 有界纯视图态无数据丢失,仅分屏新路径;正解=按 paneId 拆单例(规格已 defer 的热文件重构),排后续 | 现在改热文件(阻塞 F1 落地) | F-1 follow-up 任务 |
| 2026-07-05 | SRC-4 编辑器选型(实测):不采用任何重型 WYSIWYG 库 | GrapesJS(1.1 MB min JS + 60 KB CSS)是页面构建器,经自己的组件模型重序列化→对任意 study HTML + data-study-id 有损,破坏锚点重投影契约;TipTap/Lexical schema 有损(SRC-2b 已否)。SRC-4 = 扩展 SRC-2b 零依赖原地编辑 + 纯 DOM 块工具栏 + 预设模板。懒边界以"缺席"达成(bundle 守卫测试 + grapesjs 永不入 dist/) | 采用 GrapesJS(1.1MB+破契约);TipTap/Lexical(有损) | grapesjs/grapesjs-preset-webpage 保留但故意不接线 |
| 2026-07-05 | W2 CHAT-ONLY:仅 anchor.ask-ai 解析并发送 ChatContext.sources[](feature-detected resolveAttachmentBundles) | operation.run/kit 生成/note.generate-block 经 composeAutoContext 读扁平单段上下文(未动),永不收附件——避免与 ACTION-2a 双注入 | 让所有生成路径都吃附件(scope 膨胀+双注入) | — |
| 2026-07-05 | Sealed 笔记过滤;managed 暴露声明 | sealed:true(受保护导入,写锁)笔记从 bundle 过滤,绝不入 prompt;附件源正文确会发往 managed 网关(context verbatim)=有意(用户自选挂自己的源) | 把 sealed 也灌进 prompt | — |
| 2026-07-05 | claudeCli DRY + pty 上下文盲(既有) | claudeCliProvider 私有 sourceBlock/passageBlock/flatten/turn 删除,委托共享 cliAgent/spec weaver → 5 个 weave provider 共用一套;claudePtyProvider 完全不读 request.context(既有限制,W2 未动) | — | pty follow-up:把 contextPreamble(含附件)weave 进 PTY 首写 |
| 2026-07-05 | MEM-3 判"吸收式完成":AI 接线半(profileContext→prompt + managed 硬剥离 + per-kind 策略)已由 REV-2/ACTION-2a 落地(profileContext.ts / autoContext.ts:91);仅剩 registerBehaviorTaxonomy 行为分类学 | 无当前坏掉的消费者(digest subject 维已从 subject.kitId 兜底,弱项匹配优雅降级),现建=低回报 | 现在建分类学 | 行为分类学挪"按需再建"(见 f1-followup 同类纪律) |
| 2026-07-05 | SC-3 memory-recency 排序延后 | 无廉价客户端 per-entry 使用信号(searchRecents=近期查询非条目使用);真 recency 需向 src/client/memory 加 MEM 管道,超 SC-3 爆炸面。SC-3 排序=active-kit 优先+稳定序,recency 待 MEM digest 信号可用再上 | 现在硬塞 recency(需 MEM 改动) | recency 待 MEM 信号 |
| 2026-07-05 | operation 条目携 SlashOperationLike(非 entityClient 的 OperationRecord),SlashEntry.scope 线内穿 operation scope | slash 模块与 W2 的 entityClient 解耦(SC-3 建时 views/entityClient 归 W2);OperationRecord 结构兼容故挂载时直接传 ctx.operations | 让 slash import entityClient(与 W2 争用) | — |
| 2026-07-05 | F-1 host-control→realm-doc 桥 = 新 per-source `sourceRealmDoc` 注册表(reader 登记 realm Document),非 views.tsx/WorkspaceContext/paneId 穿线 | per-realm(keyed by Document)爆炸面最小、单面板字节等旧、guest 免费隔离;per-paneId 会碰 WorkspaceContext | per-paneId(碰 WorkspaceContext) | DELTA-3 剩余:host-card-per-pane + host-realm per-source seeding |
| 2026-07-05 | F-1 webview host-push 读持久化 prefs(全局字形 + per-source hide-all)走 realm-agnostic marker-prefs bus,不订阅现已 per-realm 的 live store | 单例改 per-realm 后 host-push 无 realm doc 句柄;持久化 prefs 是 per-source 真值 | 让 host-push 抓某个 realm 的 live 值(错面板) | — |
| 2026-07-05 | W3 HTTP-only,无 directTransport 平价 | 直传适配器无 provider 且设计上排除所有 AI 路由(测试断言 /api/chat→unsupported);合成与所有 AI 路由一致 | 加 directTransport 平价(不可建) | — |
| 2026-07-05 | W3 服务只对 synthesisDocSchema 校验 + 直接 ingest markdown,contentType "" | 无 synthesis-doc 注册类型(那是 generateBlock 的路径);.min(1)×2 挡空源;isAvailable 只挡按钮不挡输出 | 走 getNoteContentSpec(炸) / 传 "synthesis-doc" | — |
| 2026-07-05 | W3 cli-agent 结构化降级路径守卫 | completeStructured 降级可能把文件路径当 markdown(过 min(1));looksLikeBarePath→SynthesisPathResultError→400;mock/sample 不触发 | 直接 ingest 路径当正文(存垃圾) | 后续可扩 JSON_ONLY 命名 markdown |
| 2026-07-05 | W3 mock 在合成 prompt 标记上返回确定性 {title,markdown} 默认(无 sample 时) | 生产按钮派 {} 无 sample,照 form-router-default 先例保离线确定性,contentType "" 不变;供了 sample 仍胜 | 生产加 test-hook 传 sample | — |
| 2026-07-05 | A4b 工具卡片 = render-only agentTurn 态,非持久化 ChatMessage;仅 done.message 持久(appendAssistant) | ChatMessage 是 {role,content} 持久化形状,工具卡片是循环 plumbing,塞进去会污染持久化+分类+Add-as-note;render-only 爆炸面最小 | 把工具事件塞进 ChatMessage | 持久化工具 transcript(defer) |
| 2026-07-05 | A4b agent turn 从 WorkspaceContext 直接发,绕过命令 registry;mock-agent 用组合(非调私有 answer());全局 e2e pin 翻 mock-agent | agent turn 到 done 前无持久化契约;组合保三方法字节等价(证:两 pin 同 2 个既有 flake);registry 不动=与 W3 chat.synthesize 零冲突 | 走 registry / 子类改 answer 为 protected / 单独 webServer | write 工具 + timeout/abort(defer) |
| 2026-07-05 | reducer 文件 agentTranscript.ts→agentTurnReducer.ts | 与 AgentTranscript.tsx 仅大小写之差,Windows 大小写不敏感 FS 会解析错模块(RTL import 失败抓到) | 保留同名 | — |
| 2026-07-05 | SC-2 `/` 面板用 **keydown 驱动、无可聚焦输入框**;ToolbarSlashButton 经 `useWorkspaceOptional` 读 context(provider 外渲染空,不抛) | 可聚焦输入框会抢焦点+塌陷 reader DOM 选区→丢 focus.draft;keydown 从根消除失焦类;optional 保护无 provider 的视图测试 | 面板内放 filter input / useWorkspace 硬依赖 | 工具栏带指令的 AI 生成(chat-only,defer) |
| 2026-07-05 | SC-2 挂 `SelectionFloatingToolbar.tsx`(非 spec 说的 dumb `SelectionToolbar.tsx`) | SelectionToolbar 是底栏/锚点共用的哑渲染器,挂那会把 `/` 泄漏到非浮动表面;浮动 host 才是表面隔离的正确家且已读 useWorkspace | 挂 dumb 渲染器 | — |
| 2026-07-05 | D4a 区域选择 MODELESS:Alt+drag 主显式触发,off-text 尽力而为(isRealRegion 守卫) | pdf.js 文本层稀疏,纯非文本启发会误判;Alt 是无歧义显式路径 | 保留 mode-tab / 纯 off-text 判定 | D4b HTML/web rect(defer) |
| 2026-07-05 | 转为区域 非 registry 命令——经 runAction 特判到 `convertSelectionToRegion` 回调 | 需活选区几何(DOM/pageEl.getBoundingClientRect),命令层拿不到 | 走命令 | — |
| 2026-07-05 | D3a 解析按 anchor 的 NOTES' layerIds(非弃用 anchor.layerId)∩ enabled,按 order 排序;precedence style.color>layer.color>#3474e6;paint 加 var 无 !important | note.layerIds 是现行绘制过滤源;order 排序保确定;无 !important 让 focus-blue(.sv-selected/.sv-active)仍胜 | 用 anchor.layerId / 硬编码色 | — |
| 2026-07-05 | **已知 D3 债 → D3b:** buildStudyPack(studyLayer.ts:147-152)只写 {title,description,author,visibility},丢 style.color/decoration(同 N5 display 缺口)——违反 D3 §3"到处渲染一致",N2 范围外 | .svpack 便携层色需 D3b 补 | 本期做 style 导出 | D3b |
| 2026-07-05 | N2 D3a paint 经 LayerSwitcherView 可达(Option B,内联);LayerLensManage 留孤儿(不挂) | LayerLensManage 是 LayerSwitcherView 的严格子集+1(缺过滤复选+svpack 分享),挂它=更弱的重复管理器;restyleLayer 须 load()+refreshLayers() 双调(chip 色不喂 paint 管道,paint 喂) | 挂 LayerLensManage / 提取 LayerPaintControl(单消费者,不值) | 完整 R7 Lens-pill+层级;LayerLensManage 采纳或删除 |
| 2026-07-05 | 交互全屏渲染机制 = 每类型一个有状态 React 子组件(非 HTML 串/host 缝),因 render():ReactNode 且 useState 已是契约 | 契约就是 React,子组件是最干净且零新路径;状态短暂(无 store/schema),重挂=重来 | HTML 串+wireup / 加 host 分支 | 持久化/SRS |
| 2026-07-05 | vocab deck 上/下一张 DEFERRED;review-pack exercises[]=清单非逐题 Reveal;exercise 揭答只揭不选;textbook-kit-review e2e 保持 skip | NoteRenderInput 是单 content+单 note(无兄弟);exercises schema 是纯 string[] 无答案;答案串未必对齐 4 类选项;e2e 是 pre-IA 死标记 | 现在做 deck/逐题揭答/un-skip | ctx 兄弟缝(deck) |
| 2026-07-05 | **已知 UX 皱褶:** review 自评"显示答案"浮出翻卡正面,需再翻一次见背面 | 翻卡统一 mode:full 后 review 流多一步;功能对、契约纯,V1 先留 | — | FlipCard 加 initialFlipped(review 答案位传入)去双揭 |
| 2026-07-05 | PRO-1 客户端主导:无 server scheduler,客户端 app 运行时 tick,纯评估器客户端调用用真本地时钟 | 3 个时间本地 delta 逻辑上指向它,且甩掉 server 时区/分层违规/fire-on-post 三坑;背景/关闭态推送=PRO-3 | server scheduler + 提升 reviewDueStats 到 core | PRO-3 背景推送 |
| 2026-07-05 | trigger = core(1 schema + 1 纯评估器);review-push = 代码注册内置(非 seeded 实体) | operation 的时间类比;内置触发器像内置 operation 是代码,用户触发器将是实体(PRO-1 不 seed) | 复习推送硬编码(PRO-2 得撕出重来) | 用户触发器 UI(PRO-2/3) |
| 2026-07-05 | PRO-1 只 schedule+navigate;event/operation schema 定义但评估器拒(unsupported-in-pro1) | event 要没建的 memory-event hook;operation.run 要 focus(registry.ts:746)headless 跑不了 | 现在 ship 两个空跑分支 | PRO-2 |
| 2026-07-05 | 教回 = register-only KIT;唯一 core 触点 = 4 个一行(WorkspaceShell import + presets 节点 + commandEntries + searchMessages);面板直 registerView(views kit sink 是死收集器) | 守内核 AI-native=上层 kit;命令表 NAV_COMMANDS 静态(commandEntries:32)非注册表派生,不加就不进全局搜索 | 经死的 views sink 挂 / 加 rail 图标 | 注册表派生命令表=SEARCH-2 |
| 2026-07-05 | 教回 trigger ship **enabled:false**(opt-in)——**已知缺口**:复习推送+教回同时启用会对同一 reviewDue 双重 nudge(evaluate.ts:139-140 硬编码 reviewDue,per-trigger cap 无共享预算) | 守克制(disabled 默认不触发);不动 core 评估器 | 现在加 teachDue 信号(动 core) | PRO-3 加独立 teachDue 信号(教今天学的≠复习到期的)后默认启用 |
| 2026-07-05 | AI 走结构化 seam(非自由 chat);人格在 KitPrompt.build();profileContext 追加自动经 applyProfileContextGate 门控;wrap-up 走 getNoteType().render;note.review mode:"teach"=payload | 结构化=确定可测(mock 回显);人格在 build 内=零新隐私面(复用现有门);payload 扩展=零 schema 变更 | 自由 chat 多轮 / 新 verb / 新隐私路径 | 语音(PRO-3) |
| 2026-07-05 | V1-001: TWO ContentPart shapes — WIRE/persisted `{type:image,assetId}` (ref) vs provider-facing RESOLVED `{type:image,data}` (bytes); server resolves wire→resolved JIT for vision providers ONLY in ai.ts, so src/ai never imports the store | Satisfies "refs not base64" (JSONL/request carry a bounded ULID) AND the iron rule (src/ai vault-free); bytes are ephemeral, in-process | Base64 in the message (vault bloat + iron-rule break) / provider imports the store | audio part reserved; V-2 拍错题 reuses the same seam |
| 2026-07-05 | V1-001: widen content to a UNION as the completeness strategy (tsc-as-enumerator) — commit-1 not done until tsc=0; reply-side split into `AssistantReplyMessage` to keep those readers string-typed | The compiler enumerates every unguarded `.content`-as-string reader (caught ~6 the plan missed incl. a deriveSessionTitle crash) — a proof, not a hand-list | Hand-audit readers (the review showed that misses crashers) | any future content-shape change reruns the same tsc enumeration |
| 2026-07-05 | V1-001: mock-agent = vision:true (deviation from spec's false) | It delegates chat byte-identically to the vision MockModelProvider, and the e2e webServer pins mock-agent globally → vision demos offline without a 2nd server; A4b agent e2e still green | ship mock-agent vision:false (then no e2e vision demo under the default pin) | — |
| 2026-07-05 | V1-001: use AI SDK v7 **FilePart** (not deprecated ImagePart); vision cap is transport-honest, model-vision-fit (deepseek/GLM ids) flagged in code to verify at V-2 | FilePart is the non-deprecated v7 shape; the mock proves the seam independent of any real VLM | ImagePart (deprecated) / block V-1 on per-preset model-id verification | V-2 verifies real per-preset vision model ids |
| 2026-07-05 | V2-001: `images?` is a SIBLING of GenerateStructuredRequest (+kitGenerateSchema+client), threaded end-to-end, NEVER inside `input` | Inside `input` it spreads into template render values (resolvePrompt) + composeAutoContext reads input.* → double-brace images renders [object Object]; zod also silently strips an un-widened field | fold images into input / widen only one leg (silent drop) | audio part rides the same sibling later |
| 2026-07-05 | V2-001: CONDITIONAL array message (bare string when no images) + image-present-ONLY gate in generateKitContent | ai.test.ts capturingProvider reads .content as a string (.startsWith) → always-array crashes it; unconditional gate would 400 every existing text kit call (capturingProvider is vision:false) → text-only must be an untouched no-op | always-array (cleaner code) / unconditional vision gate | — |
| 2026-07-05 | V2-001: resolveVisionRequest DELEGATES to a shared resolveImageParts(vault,parts) — byte-resolution only, the gate stays each caller's shell | One resolver for chat + kit (DRY, iron-rule-safe); the gate single-sourced per call site is the truest "delegate not duplicate"; chat regression tests stay byte-green | duplicate the gate in the kit path (two sources of truth) | V-3/managed-vision reuse the same resolver |
| 2026-07-05 | V2-001: reuse the CORE `mistake` contentType (no fork); photo mistake is anchor-less via onGenerated(anchorId:undefined,sourceId) preview-then-Save | mistake fits photo extraction exactly (question/wrongAnswer/correctAnswer + optional reason/correction); Review-Pack proves the anchor-less preview path; autoMaterialize needs a real anchorId so it's the wrong path | new mistake-photo contentType / require a reader anchor | mistakeSchema.sourceImageAssetId? for a card thumbnail (deferred, additive) |
| 2026-07-05 | V2-001: DEFER PaddleOCR local lane + threshold cost-routing entirely (§4 "optional") | Real scope creep (local-tool probe + per-op lane choice) for zero killer-flow value; the preview gate IS the human-confirm 阈值路由 for V-2 | build the 三车道 cost-router now | V-3+/a later cost track if managed-vision economics demand it |
| 2026-07-05 | ALPHA-1-001: detect the offline mock via the EXISTING `aiProviders().active.kind === "mock"` (no new route); banner in the chat surface links to settings.hub (the onboarding connect-ai nav) | Zero server change — the field is already client-visible; reuses the exact settings nav; degrade-not-disappear (read failure → no banner) | a new /api/ provider-status route / a server flag | codex-hide needs isPackaged on /api/about (deferred) |
| 2026-07-05 | ALPHA readiness: X1 is DONE (installer exists), NOT the gap the backlog implied — trust artifacts over the roadmap | The assessment found `release/*.exe` + a complete electron-builder.yml + embedded-server prod path already shipped (2026-07-04); the honest 收尾 is small (one banner + a smoke-test) | build X1 from scratch (wasted work) | clean-profile install smoke-test + code-signing are the remaining alpha gates (verification/account, not code) |
| 2026-07-05 | POLISH-1-001: fix review double-reveal by seeding the FlipCard face via a typed render `ctx` (NoteRenderCtx.initialFace), NOT a new render path or schema field | Keeps the single getNoteType().render contract (host escape hatch); review reveal shows the answer face directly; standalone flip behavior unchanged | a new "revealed" contentType / fork the flashcard render / a schema flip-state field | any host that wants to seed a note's initial interactive state reuses ctx |
| 2026-07-05 | POLISH-1-001: surface `isPackaged` read-only on /api/about (threaded Electron main→startServer→createApp); provider UI disables (not hides) codex when packaged | Smallest honest signal; degrade-not-disappear (row stays listed + labeled); reuses fetchAbout seam + existing managed-note CSS (no codex styles.css touch) | hide codex entirely / a client-only packaged heuristic / a new provider-status route | future packaged-only gates read the same isPackaged |
| 2026-07-05 | POLISH-1-001: DEFER the N1 off-reader "note created" nudge | auto-materialize is structurally reader-gated (off-reader → parkDraft, not auto-materialize) + DraftNoteToast already surfaces every auto-materialize; the "reader not active" condition needs a NEW off-reader trigger + a readable active-surface signal that doesn't exist (shellNav is one-way; WorkspaceShell state is private) → new-subsystem, bigger-than-small | force it through shellNav + WorkspaceShell state exposure now | pair with a future proactive/off-reader materialization path |
| 2026-07-05 | MISTAKE-BOOK-001: the 拍错题→复习 "loop" needs NO scheduling code — a saved mistake is review-eligible by construction (capability queue selection + no-record⇒due) | Verified end-to-end + pinned by queue.test; adding a note-create→schedule side-effect would break the zero-migration invariant + the mistake-new reason | build a scheduling enrolment step (unneeded, harmful) | any new review-capable contentType is auto-queued the same way |
| 2026-07-05 | MISTAKE-BOOK-001: 错题本 = a CORE cross-source browse view (allNotes + capability filter), NOT a kit surface; 复习错题 = a pending-scope seam + a panel-local mistakesOnly filter, NO queue-policy change | mistake+review are core; the view is pure composition (bookmark.list precedent); the queue already orders mistakes first so scoping is a display filter | put the view in the mistake-photo kit / change queue policy for scoping | REPORT-1 report.list reuses the same cross-source-allNotes-list pattern |
| 2026-07-05 | MISTAKE-BOOK-001: 错题本 reachable via a RAIL entry (rail → 5 items) — FLAGGED vs the SHELL-4 "rail 只留4项" decision | Rail is the simplest reachable surface for a high-frequency K12 surface; append-only | command-launched (keeps rail at 4, the REPORT-1 choice) — pending user's UX call | if user prefers rail-minimal, move 错题本 to a command like teachback/report |
| 2026-07-05 | REPORT-1-001: 学习报告 = a register-only KIT (a contentType + an AI operation over core data), NOT a new organ | Reuses digests/weakBuckets/profile/structured/preview; footprint = 3 aggregation edits + launch one-liners; the review VERIFIED every MEM-2 data-shape read (the stale zone survived) | add a report entity/route/server-schema | any "summarize my data" product = assemble-over-reads + structured prose + preview, register-only |
| 2026-07-05 | REPORT-1-001: deterministic stats are the SOURCE OF TRUTH — re-merge them OVER the AI output before save; the AI owns ONLY prose | A schema-valid-but-wrong AI stat must never leak into a report the learner/teacher trusts; pinned by a lying-client test | trust the AI's stats fields | every generated artifact with factual numbers should re-merge deterministic values post-generation |
| 2026-07-05 | REPORT-1-001 DEVIATION: a truly vault-level (source-less) note can't save through the shell's savePendingDraft→anchor.add-note loop — it gates isAvailable on ctx.sourceId||focus | The report.list VIEW owns its own inline preview loop (same .generation-preview contract) with Save via createNote({anchorIds:[]}) — source-less, ungated; still register-only, zero core edit | force a source onto the report / add a core ungated-save path | any vault-level (no-source) generated note needs a view-owned save, not the anchor-gated shell loop |
| 2026-07-05 | REPORT-1-001: the server GET /api/notes does NOT filter by contentType (honors conceptId/anchorId/sourceId only) — surfaces filter contentType CLIENT-SIDE (report.list + 错题本 both do allNotes()+filter) | Confirmed via the co-run e2e failure (an unfiltered raw-API read returned another spec's note); the view's client-side filter is correct + the established pattern | rely on ?contentType= server filtering (silently ignored) | a future server-side contentType filter (or a cross-source note-browser core) would let vault-level surfaces skip the client filter — deferred |
| 2026-07-05 | G-A3b-001: the managed gateway's LOCAL half (provider + mock gateway + ledger + pricing + gates) was ALREADY SHIPPED (G-A1–A3a, 90 tests) — G shrank to client-wiring only | Plan/review verified src/ai/managed.ts + src/gateway/* + the V-1/REV-2 gates all present + green; roadmap:93 confirmed | rebuild the provider/gateway from scratch (wasteful; the review's #1 flagged risk) | before planning a "big" track, grep for a shipped foundation — much may be done |
| 2026-07-05 | G-A3b-001: the managed session token is a bearer SECRET — store it ENCRYPTED via keyStore (managed_session_<id> blob, BYOK-key path), never plaintext in ai-providers.json or any JSON response; EnvOnlyKeyStore mode → read null → show 登录, no crash | A leaked bearer token = account takeover; the safeStorage path is the shipped precedent for BYOK keys | store the token in the plaintext provider-config JSON / crash in env-only mode | any bearer/session credential rides the keyStore, never config JSON |
| 2026-07-05 | G-A3b-001: a provider CONFIG entry may not shadow a registry provider id (managed) → auto-provision a distinct `managed-account` config entry (hidden) to host the session/panel; activation still resolves to the "managed" registry provider | The config-vs-registry id spaces are distinct; a shadowing id would collide in providerForEntry resolution | reuse the "managed" id for the config entry (id collision) | any built-in provider needing per-vault config gets a distinct hidden config-entry id |
| 2026-07-05 | G-A3b-001: managed = CORE-ai (a ModelProvider under the shared seam, same V-1/REV-2 gates), credits/ledger = EXTERNAL gateway (never touches the JSONL vault, not a vault entity), settings = CORE-client | The kernel is 5 entities+memoryEvent; a "credits" entity would violate the design law; multi-user money = ACID server-side, not local JSONL | model credits as a local vault entity/prefs | any multi-user/billing state lives behind the external gateway, mocked locally for CI |
| 2026-07-05 | M-001: F4/F5/M1/MH-0 were ALREADY SHIPPED — the backlog's "M needs F4/F5 refactors first" was stale (docs-vs-code drift); the kit system evolved PAST the spec into a FLAT capability-group model (local CatalogSource = kits only, members = internal groups) | Verified verbatim in code (activation.ts:7-9, catalogAgreement.test.tsx) + reconciled architecture-review.md/roadmap.md | plan the phantom F4/F5 refactors | THIRD track this session (REPORT/G/M) where the foundation was more complete than the backlog claimed — grep for a shipped foundation before planning a "big" track |
| 2026-07-05 | M-001: marketplace preview data flows through the CatalogSource seam (CatalogListing.previews), NEVER a direct catalog.ts import in the view (MH-0) | The manager UI must stay source-agnostic so a remote registry slots in unchanged; guarded by pluginManagerViews.test.tsx:363-370 | render previews by importing catalog.ts in the view | any market UI addition rides CatalogListing/CatalogSource; the local↔remote boundary stays a one-line source-merge |
| 2026-07-05 | M-001: install is KIT-GRANULAR (FLAT model) — the InertNote "install to view" affordance installs the provider's OWNING KIT via groupOwnerOf, not a bare plugin | Members are internal capability groups; a user installs a kit, enables groups within it | install a bare member plugin (not a user-facing unit) | any "install the thing that provides type X" affordance resolves to the owning kit |
| 2026-07-05 | M-001: preview sampleContent MUST validate against its NoteContentSpec (else it silently renders as InertNote) — fixtures reuse the kits' shipped KitPrompt.mockContent (one source of truth) | An invalid fixture looks broken with no error; the mockContent shapes are already schema-valid by construction | hand-author preview fixtures divergent from the kit's own mockContent | reuse a kit's own deterministic sample shapes for any preview/demo of that kit |
| 2026-07-06 | ANNO-PREVIEW-001: in-reader note previews use a static mini-card chrome, not the full React `ArtifactCard` | The PDF/HTML reader overlay injects framework-free static HTML into guest/reader DOM, so it needs lightweight type chrome while keeping each note type's `mode:"card"` renderer for the body. This restores type distinction without adding interactive card behavior to the overlay. | Mount full `ArtifactCard` inside the reader overlay / render body-only raw text. | Any future reader preview affordance must be static-safe unless the reader overlay gets an explicit React mount point. |
| 2026-07-06 | FLOAT-NOTE-001: reader note cards stay static, but double-click bridges to the host Notes viewer | The in-reader overlay is framework-free and may run in iframe/webview realms, so full inline editing inside the card would duplicate editor state and card shells. A static `sv:note-card-action` event carries `{noteId, anchorId}` to the host, which focuses the canonical note viewer. Dragging remains local and persists as anchor-relative geometry so pinned cards follow their passage instead of the viewport. | Mount a full editor inside `#sv-note-card` / persist absolute viewport coordinates for pinned cards. | A richer inline editor can be added later only after the reader overlay has an explicit host-owned React mount and edit lifecycle. |
