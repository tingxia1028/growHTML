# N2 — region anchors D4a (modeless gesture) + D3a (configurable paint) — BUILD SPEC (approve-with-changes)

Status: Plan agent → adversarial review → **APPROVE-WITH-CHANGES** (8 deltas folded; 1 was a real blocker).
Region CAPABILITY is shipped (rect schema, rubber-band, region paint, draft→request map) — N2 finishes the UX.
**Build AFTER SC-2 lands** (both add to `WorkspaceContext.selectionActions` memo — delta 6; sequence so N2 sees SC-2's state).
Scope: **D4a** (PDF+image modeless gesture, schema untouched) + **D3a** (per-layer paint color). D4b (HTML/web rect) + D3b (anchor styleOverride envelope, named palette, global default) DEFERRED.

## THE 8 FOLDED DELTAS (from the review — MANDATORY)
1. **BLOCKER — Commit 5 e2e is a REWRITE, not an un-skip.** The 3 `test.skip` specs (regions.spec.ts:97,161,194) skip because their `saveNote()` helper (:51-57) drives the DELETED `.composer-mode .mode-tab`/`.composer-input`/"Save Note" UI, and they assert `.chat-source` "Region selected" which is dead-coded behind `false ?` (views.tsx:637,641). RETARGET all three to the CURRENT save UX: Alt+drag region (or drag on non-text) → floating toolbar / slash `/type` → `.floating-note-editor` "Save this draft as a note" (FloatingNoteEditor.tsx:247-249) → `anchor.add-note` → assert a stored `pdf_selection`/`image_region` with a 4-tuple rect + a `.pdf-region-box`/`.image-region-box` paints. DROP the `saveNote()`-composer + `.chat-source`/"Region selected" assertions.
2. **Thread `enabledLayerIds` into `mapPaintAnchor`.** Today it lives only in `buildPaintPipeline` (paneSelectors.ts:111), NOT in `mapPaintAnchor` (:73-89, which has `sourceLayers` but NOT `enabledLayerIds`). Add it to the signature + both call sites (:117,118) so `resolveAnchorPaintStyle(anchorNotes, sourceLayers, enabledLayerIds)` compiles.
3. **Sort `sourceLayers` by `order` before the "first enabled with a color" pick.** `sourceLayers` flows from `layersResponse.layers` (WorkspaceContext.tsx:1017,1103) with NO client sort; `StudyLayerRecord.order` is optional (study-layer.ts:44). Without a stable sort, two colored enabled layers resolve non-deterministically. Sort on `order` (undefined last) in `resolveAnchorPaintStyle`.
4. **FOUR edit sites for the layer `style` round-trip — ALL in the SAME commit, file = `src/server/services/layers.ts` (NOT core/study-layer):** (a) `updateLayerRequestSchema` add `style` (:27-33); (b) the **`.refine()` guard** (:34-41) add `input.style !== undefined` to the `||` chain — else a style-only PATCH 400s "requires enabled, title, color, or order"; (c) `updateLayer`'s **explicit field construction** (:102-109 — it does NOT spread `input`) add `style: input.style ?? existing.style`; (d) `entityClient.patchLayer` input type (entityClient.ts:988) gain `style?`. Plus the `studyLayerSchema.style` field (study-layer.ts:42-44). Round-trip works ONLY if all land together.
5. **Alt+drag is the PRIMARY/explicit region trigger.** `!target.closest('.textLayer')` is BEST-EFFORT only — pdf.js text layers are sparse (inter-glyph/line gaps + margins are NOT in a `.textLayer` span), so the non-text heuristic mis-fires. Alt held = always region (unambiguous); non-text = convenience, guarded additionally by the existing `isRealRegion`/`MIN_DRAG=6` (overlay.ts:26). Acceptance leans on Alt+drag.
6. **Coordinate with SC-2 on `WorkspaceContext.selectionActions` (:2093-2140).** SC-2 mounts a slash palette on the selection toolbar and MAY touch `selectionActions`; N2 appends 转为区域. Build N2 on the SC-2-landed tree; keep N2's edit an append-only entry in the array literal + one new callback. Don't assume disjoint.
7. **"Omit the `style` key unless defined."** Construct `mapPaintAnchor`'s return with `...(style ? { style } : {})` — the existing byte-lock (paneSelectors.test.ts:94,106) uses `toEqual` (tolerates `style:undefined`) so the EXISTING fixtures stay green, but this construction keeps un-styled `PaintAnchor` genuinely identical and survives a future `toStrictEqual`. NOTE: a NEW styled-anchor test must assert a HAND-ROLLED expectation, NOT `oldPipeline` (which never emits `style`).
8. **Log the `.svpack` `style.color` strip as known D3 debt** (decision log): `buildStudyPack` writes only `{title,description,author,visibility}` (studyLayer.ts:147-152) — color/role/order/style all dropped (same as the N5 `display` gap). Contradicts D3 spec §3's "renders the same everywhere"; scoped out of N2 (→ D3b).

## D3 scope split (locked): D3a = `layer.style {decoration,color}` hex-only via the existing Lens color input; precedence `layer.style.color > layer.color > global #3474e6`. D3b (deferred) = anchor `styleOverride` envelope + per-anchor swatch + named-token palette + global-default Settings.

## Commit sequence (each compiles + tested)
1. **D4a gesture** — PdfReader.tsx (delete `regionMode` state :101-103 + mode-tab buttons :525-542 + `region-mode` class :563; `onMouseDown` computes `isRegionGesture(event, overText)` from the target, Alt→region primary + `!target.closest('.textLayer')` best-effort; keep the marquee/`finishDrag` body verbatim, only the TRIGGER changes) + ImageReader (already modeless — no gesture change) + `surfaces/overlay.ts` pure `isRegionGesture({altKey}, overText)` (+test: alt→region, non-text→region, text→text) + styles.css (drop `.region-mode .textLayer{pointer-events:none}` :1847-1849; crosshair on `.page[data-alt]`). Remove now-unused ScanLine/TextCursor imports.
2. **转为区域** — new pure `surfaces/pdfSelectionRect.ts` `selectionRectToPageRect(pageRect, selRect)` → normalized 0..1 `Rect` (reuse `normalizeRect` region.ts:34) (+test: center-half→~[.25,.25,.5,.5], full→[0,0,1,1]). Add a 转为区域 `ToolbarAction` to `selectionActions` (WorkspaceContext.tsx:2093), gated to PDF text selection/`pdf_selection` anchor with a page. PREFER the LIGHTER seam: compute the rect IN PdfReader (it owns pageEl.getBoundingClientRect() + the selection bbox) and push a region draft through the EXISTING `onSelect(draft)` — a new `convertSelectionToRegion` context callback is acceptable but not minimal. Flows through `buildAnchorInput` region arm → `regionTargetToRequest` → `pdf_selection{page,rect}`, ZERO schema change.
3. **D3a plumbing** — study-layer.ts `style` field; surfaces/types.ts `PaintAnchor.style?`; paneSelectors.ts `mapPaintAnchor` + `enabledLayerIds` threaded (delta 2) + pure `resolveAnchorPaintStyle(anchorNotes, sourceLayers, enabledLayerIds)` (filter by NOTES' `layerIds` (note.ts:66) NOT deprecated `anchor.layerId`; sort by `order` delta 3; "omit key unless defined" delta 7); readers set `--sv-anchor-color` + `sv-deco-*` on the painted span / region box (PdfReader.highlightAnchors + overlay.placeRegionBox + ImageRegionBox); `annotationLayer.ts` ANNOTATION_CSS add `color-mix(in srgb, var(--sv-anchor-color,#3474e6) 18%, transparent)` + `.sv-deco-{highlight,underline,both}` rules (NET-NEW — today it's literal #3474e6; NO `!important`, keep lower specificity than `.sv-selected`/`.sv-active` so focus-blue wins); styles.css region box reads the var. (+tests: `resolveAnchorPaintStyle` picks enabled layer's style.color / falls back to layer.color / undefined when neither — HAND-ROLLED expectation; `annotationDom.test.ts` element carries `--sv-anchor-color`+`sv-deco-*`.) Does NOT touch F-1's per-realm WeakMaps.
4. **D3a schema round-trip + Lens UI** — the FOUR sites (delta 4) in server/services/layers.ts + entityClient.patchLayer; LayerLensManage.tsx:163 add a 2nd `<input type="color">` bound to `layer.style?.color` (`restyleLayer(layer,{color})`) + a decoration `<select>` (highlight/underline/both), mirroring the existing `recolorLayer` (:51-62); `refreshLayers()` triggers repaint via the `[sourceLayers]` dep (WorkspaceContext.tsx:892).
5. **e2e REWRITE (delta 1)** — `e2e/regions.spec.ts` retargeted to the floating-editor UX: (a) Alt+drag on a PDF → floating toolbar/slash → `.floating-note-editor` Save → assert a `pdf_selection` anchor with a rect + `.pdf-region-box`; (b) text-select → 转为区域 on the floating toolbar → a rect anchor; (c) set a layer's paint color in the Lens → the painted element's `--sv-anchor-color` changed / region box re-tinted. Un-skip only after the rewrite passes.

## Byte-identity note: existing lock (paneSelectors.test.ts:94,106) uses `toEqual`, fixtures use `layers:[]` → stays green. New styled tests: hand-rolled expectations, not `oldPipeline`.

## SC-2 disjointness: file-level disjoint EXCEPT `WorkspaceContext.tsx selectionActions` (delta 6 — coordinate). N2 does NOT touch views.tsx / SelectionToolbar.tsx / anchorViews.tsx / slash files.

## Gates: tsc 0 · full vitest green (baseline post-SC-2) · build · e2e/regions.spec.ts green in isolation. Commit N2.N own sequence, don't push, don't touch docs (report TEXT blocks incl. the .svpack D3 debt for the decision log).
