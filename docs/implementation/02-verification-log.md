# Verification Log

Record verification evidence here as work proceeds. Include both automated and manual checks.

## Verification Matrix

| Task ID | Verification Type | Command Or Method | Expected Result | Actual Result | Status |
| --- | --- | --- | --- | --- | --- |
| E2E-ELECTRON-001 (2026-07-04) | Full Electron e2e | `npm run e2e:electron` (electron:build + suite) | Every spec passes or carries an inline documented skip — no unexplained red | **10 passed / 2 skipped / 0 failed (17.0s after build)** — skips: composer image-pick (manual note-creation UX pending, same decision as the web skips) + deep PTY flow (terminal lives in a transient ⋯ popover today); config-only stability re-run also green (12.6s) | Passed |
| E2E-ELECTRON-001 (2026-07-04) | Vault hygiene proof | `data/vault` inventory (path+size+mtime) diffed before/after all runs; `%TEMP%` scan; port + process checks | Electron e2e never touches `data/vault`; per-run OS-temp vault root created + removed; only its own app instances spawned/killed | `data/vault` UNTOUCHED (64 files, byte+mtime identical); `growte-e2e-electron-vault-<pid>` created + removed every run, zero stale siblings after; dev servers still listening on 4177/5173, e2e ports unbound; zero electron/Growte processes left | Passed |
| E2E-ELECTRON-001 (2026-07-04) | Type check + full unit suite | `npm run check` · `npx vitest run` | tsc 0 errors; ≥184 files / ≥1834 tests green | tsc clean (exit 0); **184 files / 1836 tests, ALL passed** (12.7s) — run with the parallel session's dirty tree (UI-LIBRARY-003 etc.) in place, no external failures | Passed |
| UI-LIBRARY-003 (2026-07-04) | Focused unit tests | `npm test -- src/client/workspace/libraryView.test.tsx` | Library renders compact title-only rows; Documents row `x` closes the row without calling `deleteSourceItem`; Recent row `x` calls `removeRecentSourceId`; collapsed search opens and filters sections | **1 file / 21 tests passed** (jsdom emitted the known canvas `getContext` warning after completion) | Passed |
| UI-LIBRARY-003 (2026-07-04) | Type check | `npm exec tsc -- --noEmit` | 0 TypeScript errors after adding `removeRecentSourceId` to the workspace context and Library section context | Passed (exit 0) | Passed |
| UI-LIBRARY-003 (2026-07-04) | Build | `npm run build` | Vite production build succeeds with the updated Library CSS/JSX | ✓ built (pre-existing chunk-size warnings only) | Passed |
| SEARCH-1-001 (2026-07-04) | Type check | `npm run check` | 0 errors with the rank core (`src/core/search/rank.ts`), the search service + `GET /api/search` route + direct-transport route, and the `src/client/search/` palette modules | Passed (exit 0) | Passed |
| SEARCH-1-001 (2026-07-04) | New unit tests | `npx vitest run src/core/search/rank.test.ts src/server/services/search.test.ts src/client/search/searchEngine.test.ts src/client/search/globalSearch.test.tsx` | Rank tiers pinned (exact>prefix>word-boundary>substring, recency tiebreak, stable ties, cap); CJK substring; snippet extraction; registry-driven note text via a FIXTURE NoteContentSpec; anchor quotes surfacing through notes (quote hit → quote snippet); per-family cap 20; direct-transport parity; command ranking (zh title / en title / aliases, bare query = nav menu); grouped row model + display caps + flat indices; keyboard wrap/Enter; jsdom palette: Ctrl/Cmd+K opens+toggles, Esc closes, 笔记/文档/命令 groups render after the 150ms debounce, Enter dispatches per family over mocked deps (focusAnchor on active source, cross-source openSource → pending reveal completes when anchors arrive, anchorless → focusNote, source → openSource, command → run), empty state, i18n flip (en headers/placeholder) | **4 files / 38 tests, ALL passed** (3.2s) | Passed |
| SEARCH-1-001 (2026-07-04) | Full unit suite | `npx vitest run` | Baseline (≥178 files / ≥1766) + the new SEARCH-1 tests all green | **182 files / 1804 tests, ALL passed** (9.6s; no parallel-session failures observed at run time) | Passed |
| SEARCH-1-001 (2026-07-04) | Build | `npm run build` | Vite production build succeeds with the palette module + scoped `globalSearch.css` bundled | ✓ built (pre-existing chunk-size warnings only) | Passed |
| W1-001 (2026-07-04) | Type check | `npm run check` | 0 errors with the new entity/routes/domain module | Passed (exit 0) | Passed |
| W1-001 (2026-07-04) | Full unit suite | `npx vitest run` | Baseline (≥174 files / ≥1733) + the new W1 tests all green | **178 files / 1766 tests, ALL passed** (9.4s) — includes new `chatSession.test.ts` schema (6), `chatSessions.test.ts` server CRUD+append (11), `useChatSessions.test.tsx` mocked-fetch domain (9), `studyViewSessions.test.tsx` jsdom (5), +2 registry onAssistantDone arms | Passed |
| W1-001 (2026-07-04) | Build | `npm run build` | Client builds with the chat module + scoped css | ✓ built (pre-existing chunk-size warnings only) | Passed |
| W1-001 (2026-07-04) | Chat e2e | `npx playwright test e2e/streaming-chat.spec.ts` | Streaming ask + save-reply flows unaffected by session persistence | **2 passed (9.8s)** on the ephemeral-vault harness (temp vault created + removed) | Passed |
| W1-001 (2026-07-04) | Persistence invariants | Unit assertions | User turn persists on send (lazy create, active source as attachment ref); assistant turn persists on stream end (`onAssistantDone`) or non-streamed reply; chunks NEVER write partials; restart resumes the stored session; explicit 新对话 sentinel survives restart; sessions excluded from `vaultEntitySchema` | All pinned by the four new test files (FIFO create→append order asserted on the mocked fetch call log; union exclusion asserted in the schema test) | Passed |
| E2E-DEBT-001 (2026-07-04) | Harness isolation proof | Single spec run, then full runs; `data/vault` file inventory (path+size+mtime ticks) snapshotted before/after and `Compare-Object`d; temp dir checked | e2e never touches `data/vault`; ephemeral vault created under `os.tmpdir()` and deleted after the run; no repo-side `.e2e-vault` | `data/vault` UNTOUCHED (62 files, byte+mtime identical); `[e2e] ephemeral vault: %TEMP%\growte-e2e-vault-<pid>` created + removed each run; no repo-side vault dir; dev session on 4177/5173 undisturbed (e2e on 14177/15173) | Passed |
| E2E-DEBT-001 (2026-07-04) | Full web e2e | `npm run e2e` | Whole suite green against today's UI (drift reconciled) | **29 passed / 24 skipped / 0 failed (33.0s)** — all 24 skips are pre-existing `test.skip`s with inline reasons (manual note-creation flow removed in the Growte IA rebuild; concepts R7 layout regression) | Passed |
| E2E-DEBT-001 (2026-07-04) | Type check | `npm run check` | 0 errors after the DomReader guard + config/vite changes | Passed (exit 0) | Passed |
| E2E-DEBT-001 (2026-07-04) | Full unit suite | `npx vitest run` | Everything green incl. the DomReader transitional-document guard | **174 files / 1733 tests, ALL passed** (9.2s) | Passed |
| E2E-DEBT-001 (2026-07-04) | Electron e2e (best-effort) | `npm run e2e:electron` | Known-stale: specs target the pre-R1 shell | RED as expected — failure class = obsolete selectors (`.brand-block h1` "Sources", `.reader-header h2`, top-level "Refresh" button); not expanded this pass, logged as standing debt | Failed (pre-existing, documented) |
| ACTION-2A-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after autoContext engine (src/ai + src/server/services), template dotted grammar, operation schema mode/instruction, resolvePrompt/structured simple-mode path, generalized profileContext gate | Passed (0 errors) | Passed |
| ACTION-2A-001 (2026-07-04) | New unit tests | `npx vitest run src/ai/autoContext.test.ts src/server/services/autoContext.test.ts` | Envelope format pinned exactly; sections present-or-omitted per input; 1.5k cap with truncation priority selection→doc→learner (drop-below-minimum pinned); template namespace values + referencesAutoContext; composeAutoContext: selection from anchorText(+locators), doc from stored source (title·sourceType·detectSubject subject·foreground kit), unknown sourceId degrades, hidden facts stripped, managed → no learner, input.profileContext → no double weave, failing profile read degrades | All passed | Passed |
| ACTION-2A-001 (2026-07-04) | Engine + schema tests | `npx vitest run src/ai/template.test.ts src/core/schema/schema.test.ts src/kits/structured.test.ts` | Dotted {{doc.title}} flat-key substitution ADDITIVE (all pre-existing template tests untouched, green); simple-mode schema roundtrip + old records default to template; resolvePrompt: empty envelope → built-in VERBATIM, preamble prepend, values-not-preamble when referenced, runtime wins clashes; simple compile output pinned (preamble+instruction+directive); generateOperationContent: mock → {markdown,""}, real quiz router arm, contentType defaulting, auto-op rejects explicit contentType | All passed | Passed |
| ACTION-2A-001 (2026-07-04) | REV byte-compat + gate parity | `npx vitest run src/core/review/prompts.test.ts src/server/services/ai.test.ts src/client/review/profileContext.test.ts` | REV-1 regression pins byte-identical (no-digest prompts unchanged); managed strip byte-parity preserved AND generalized to all operations (pinned via a {{profileContext}} template op); envelope preamble reaches built-ins with selection input; envelope-free request leaves prompt byte-identical; hand-seeded simple op runs end-to-end at the service level | All passed | Passed |
| ACTION-2A-001 (2026-07-04) | Server route e2e (no UI) | `npx vitest run src/server/app.test.ts` | POST /api/operations accepts mode:"simple"+instruction (201, no template/outputType); POST /api/kits/generate WITHOUT contentType runs it → schema-valid routed {contentType:"markdown",content:""}; simple-without-instruction and template-without-outputType → 400; all V1 operation CRUD/params-merge tests unchanged | All passed | Passed |
| ACTION-2A-001 (2026-07-04) | Full regression + build | `npx vitest run` · `npm run build` | Whole suite green incl. both concurrent agents' work | 174 files / 1733 tests passed · build ✓ | Passed |
| CONCEPT-UX-1-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after the `concept.mark-selection` command (+`onConceptMarked` action, optional `concepts` client capability), the WorkspaceContext toast state/action, `ConceptChips`/`ConceptMarkToast`/`conceptName`/`conceptMessages`/`conceptUx.css`, the FocusOverlay chips row, the conceptViews sort/filter/badges, and the ConceptInspector 关联到… + jump rework | Passed (0 errors) | Passed |
| CONCEPT-UX-1-001 (2026-07-04) | New unit tests | `npx vitest run src/client/commands/conceptMark.test.ts src/client/workspace/conceptChips.test.tsx src/client/workspace/conceptViews.test.tsx src/client/inspectors/conceptInspector.test.tsx` | 选中即建: creates + links ONE marker note (anchorIds+conceptIds), dedupes by normalized name against an existing-concept fixture, 40-char name cap (marker keeps the full quote), region draft/no-passage unavailable, no-`concepts`-client fallback still creates. Chips: render from a fixture note, chip click navigates (mock focus callback), ＋ autocomplete prefix-suggests + links, Enter creates on no-match / links on exact match, NoteConceptChips appends via `updateNote` standalone, FocusOverlay mounts the row only for saved-note blocks. 关联到: autocomplete pick dispatches `relation.create` with `relationKind:"related"`, self excluded; linked-note row reveals the anchor (`setAnchor`) + focuses the note. List: count-desc sort + badges, filter + 没有匹配的概念 line, empty-vault guidance 选中文字 → 标为概念, pure helper coverage | 4 files / 23 tests, all passed | Passed |
| CONCEPT-UX-1-001 (2026-07-04) | Full unit suite | `npx vitest run` | Everything green on top of the parallel sessions' work | **172 files / 1684 tests, ALL passed** (no parallel-session failures observed) | Passed |
| CONCEPT-UX-1-001 (2026-07-04) | Build | `npm run build` | tsc + vite build succeed with the new modules/CSS | Passed (exit 0, built in ~1s vite step) | Passed |
| TRUST-12-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after `src/server/dataTrust.ts` (service + rotation + scheduler + routes), the `createApp.dataTrust`/`startServer.backups` wiring, `src/client/workspace/dataTrust.ts`, and the UserMenu 数据 entries | Passed (0 errors) | Passed |
| TRUST-12-001 (2026-07-04) | Full unit suite | `npx vitest run` | New `src/server/dataTrust.test.ts` (rotation math on an injected clock incl. the 24h protection window + same-day collapse; manual trigger + status shape; backups-dir exclusion even when nested; day-by-day pruning; fake-timer scheduler stale/fresh/interval re-check; pre-清除记忆 hook armed only with the dataTrust option; export headers/manifest/every-store + validator roundtrip; import rejected on missing confirm phrase / non-zip / missing manifest / counts mismatch / zip-slip WITHOUT touching the vault or creating a pre-import backup; REPLACE A→B verified on fixture vaults with the pre-import backup carrying B's old state, undo via `POST /api/backup/restore`, no staging/replaced residue; restore name/phrase guards) + new `src/client/workspace/dataTrust.test.ts` (default IO endpoints via mock fetch; local `peekVaultZip` accept/refuse; import flow: same-vault, DIFFERENT-VAULT ⚠️ prompt, wrong-phrase/cancel never upload, non-pack refused locally, restartRequired honesty, failure framing; backup/export actions + download plumbing) + `UserMenu.test.tsx` extended (entry ids incl. backup-now/export-vault/import-vault; 数据 entries call through the IO seam) | **168 files / 1661 tests, ALL passed** (no parallel-session failures observed) | Passed |
| TRUST-12-001 (2026-07-04) | Production build | `npm run build` | Vite production build succeeds with the dataTrust client module + fflate on the client path | Passed (pre-existing chunk-size warnings only) | Passed |
| TRUST-12-001 (2026-07-04) | REAL backup + export + import (temp fixture vault, actual service) | Temp tsx script: `startServer({vaultRoot: <tmp>/vault, backups:{dir:<tmp>/backups, auto:false}})` → seed 2 sources + 1 anchor + 1 note over real HTTP → `POST /api/backup/now` → open the zip → `GET /api/vault/export` → open the zip → `POST /api/vault/import?confirm=替换全库` roundtrip | Backup zip opens, manifest kind/reason correct, counts match the seed; status lists it with a real size; export streams `application/zip` + `attachment; filename="vault-YYYYMMDD-HHMMSS.growte-vault.zip"`, counts {sources:2, anchors:1, notes:1} match, `sources/` files in, `backups/` out; import roundtrip ok with sameVault:true + a pre-import backup; temp dirs + script deleted | Passed — "REAL CHECK PASSED · backup vault-backup-20260704-030222-manual.zip (4465 bytes, 17 files) · export 4455 bytes · pre-import backup created" | Passed |
| SPEECH-3-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after the pinyin engine (`pinyin.ts`), `PinyinPopover` + `usePinyinPopover`/`PinyinButton`, the GlobalSpeakSelection chip-row restructure, and `SelectionToolbar.onPinyin`/`SelectionFloatingToolbar` wiring | Passed (0 errors) | Passed |
| SPEECH-3-001 (2026-07-04) | Engine correctness | `npx vitest run src/client/speech` + in-task probe | `annotate()` context-aware 多音字 (长大→zhǎng vs 长度→cháng — both readings resolved in ONE sentence "他长大了，测量长度。"; 银行→háng vs 行走→xíng), tone-marked output (你好→nǐ hǎo), verbatim per-char mapping over mixed zh/latin/punct (null for non-CJK, concat reproduces input), containsCjk gate (CJK punctuation alone → false) | All assertions green; live probe output archived in the progress log | Passed |
| SPEECH-3-001 (2026-07-04) | Full unit suite | `npx vitest run` | New `pinyin.test.ts` (pure) + `pinyinPopover.test.tsx` (jsdom, the globalSpeakSelection.test.tsx harness: per-CJK-char `<ruby>字<rt>zì</rt></ruby>` with context-resolved 多音字; consecutive non-CJK merged into ONE plain run; header 朗读 POSTs the shown text to `/api/speech/tts` (mock fetch/Audio); Escape/✕/backdrop dismiss, inside-click doesn't; 200-char cap → 200 rubies + truncation note + capped 朗读 text; chip: CJK selection shows 拼 beside 朗读, popover OUTLIVES the cleared selection, Escape closes; latin selection → 朗读 only; SelectionToolbar: 拼 shown for CJK passage + `onPinyin(text)` called, hidden for latin, hidden without opt-in); all existing suites green against the restructured chip row | 164 files / 1605 tests, ALL passed (no parallel-session failures observed) | Passed |
| SPEECH-3-001 (2026-07-04) | Production build | `npm run build` | Vite production build succeeds with pinyin-pro + the popover module + scoped css bundled | Passed; existing chunk-size warnings only | Passed |
| SPEECH-1B-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after `GlobalSpeakSelection` (+ WorkspaceShell chrome mount), the ChatMessageBody assistant-row 朗读, and the FocusOverlay header 朗读 via `getNoteContentSpec().toSearchText` | Passed (0 errors) | Passed |
| SPEECH-1B-001 (2026-07-04) | Full unit suite | `npx vitest run` | New `src/client/speech/globalSpeakSelection.test.tsx` (jsdom, speech.test.tsx harness + module-level `Range.prototype.getBoundingClientRect` stub — jsdom has none: host selection → chip appears and speaks `selection.toString()` (POST body asserted) flipping 朗读↔停止; selection inside a `.reader-panel` container → hidden; 停止 keep-alive after the selection clears, gone when playback ends; Escape stops + dismisses; unavailable status renders nothing; scroll hides an idle chip; assistant bubble 朗读 wired to the reply text / user bubble none; FocusOverlay header speaks `toSearchText(content)` for a code-snippet fixture — the code, NOT JSON; unknown contentType → disabled, no crash) + `artifactCard.test.tsx` updated (assistant actions row now ALWAYS carries 朗读; fetch stubbed to reject so the status probe resolves unavailable deterministically); everything existing stays green | 162 files / 1585 tests, ALL passed (incl. the parallel session's layer/plugin suites at run time) | Passed |
| SPEECH-1B-001 (2026-07-04) | Production build | `npm run build` | Vite production build succeeds with the GlobalSpeakSelection module + scoped css bundled | Passed; existing chunk-size warnings only | Passed |
| SPEECH-2-001 (2026-07-04) | Type check | `npm run check` | TypeScript passes after `SpeechService.transcribe`/stt status + the `/api/speech/stt` route, the shared `speechStatus` module refactor, `useVoiceInput`, `VoiceInputButton`, and the composer mount | Passed (0 errors) | Passed |
| SPEECH-2-001 (2026-07-04) | Full unit suite | `npx vitest run` | Extended `src/server/speech.test.ts` (mocked probe/proxy seams: status up with sidecar model / down with setupHint; base-URL override; service-level probe cache 30s TTL + failed-transcribe flips availability; proxy roundtrip `{text, language, durationMs}` with byte-identical audio + mimeType; `?language=` passthrough; malformed language → 400 before the lane; sidecar down → 502 `{error, code:"stt_unavailable"}` naming stt-sidecar; empty body → 400 `stt_bad_audio`; >15MB → 413 `stt_audio_too_large`) + new `src/client/speech/voiceInput.test.tsx` (mocked MediaRecorder/getUserMedia/fetch: record→stop→transcribing→transcript with mic released; cancel-during-recording sends nothing; 502 → error not transcript; unavailable start is a no-op; confirm popover inserts ONLY on 确认插入, 取消 discards, 重录 records again; unavailable click opens the setup guide naming the README; ONE `/api/speech/status` fetch serves useSpeakText+useVoiceInput); everything existing stays green | 161 files / 1574 tests, ALL passed (incl. SPEECH-1's 20 and the previously-parallel layer suites) | Passed |
| SPEECH-2-001 (2026-07-04) | Production build | `npm run build` | Vite production build succeeds with the voice-input client module + scoped css bundled | Passed; existing chunk-size warnings only | Passed |
| SPEECH-2-001 (2026-07-04) | Real sidecar + real proxy (manual, LOCAL) | Started `scripts/stt-sidecar/server.py` with the system Python 3.10 (faster-whisper 1.2.1, CUDA torch present, HF cache already had large-v3); temp tsx script drove the REAL `createSpeechService()` defaults (no mocks): live edge-TTS synthesized the test utterance, then the real probe + proxy hit the running sidecar; script deleted and sidecar stopped after | `/health` answers with the model; `status().stt.available = true`; the mp3 comes back as the same sentence | `/health` → `{ok:true, model:"large-v3"}`; `status().stt` → `{available:true, lane:"local", model:"large-v3"}`; edge lane → 20880 bytes audio/mpeg for "今天天气很好，我们一起去公园吧。"; transcribe → "今天天气很好,我们一起去公园吧。" (verbatim, language=zh, audio 3480ms, roundtrip 1147ms on GPU) | Passed |
| SPEECH-1-001 | Type check | `npm run check` | TypeScript passes after the speech service, the createApp speech seam/routes, the useSpeakText hook, SpeakButton, and the three mount points | Passed (0 errors) | Passed |
| SPEECH-1-001 | Full unit suite | `npx vitest run` | New `src/server/speech.test.ts` (mocked edge lane: audio/mpeg byte pass-through + default/override voice; empty/whitespace/too-long text → 400 before the lane; unknown voice → 400 友好 message; lane failure and empty stream → 502 `{error, code:"tts_unavailable"}`; status shape with curated voices) + new `src/client/speech/speech.test.tsx` (mocked fetch/Audio/blob-URL: speak→speaking→ended; second speak pauses the first; stop() pauses + revokes; 502 clears speaking; unavailable status; SpeakButton 朗读↔停止 flip, disabled without text/when unavailable; SelectionToolbar opt-in/opt-out of the 朗读 slot); everything existing stays green | 160 files / 1551 tests: 1549 passed incl. all 20 speech tests; the 2 failures are in `src/client/workspace/layerTree.test.ts` against the parallel LAYER-TREE-001 session's in-flight dirty `layerTree.ts` (pre-existing in that session, unrelated to speech) | Passed (speech scope) |
| SPEECH-1-001 | Production build | `npm run build` | Vite production build succeeds with the speech client module + scoped css bundled | Passed; existing chunk-size warnings only | Passed |
| SPEECH-1-001 | Real synthesis (manual, ONLINE) | Temp tsx script importing `createSpeechService()` from `src/server/services/speech.ts` (real msedge-tts lane, no mocks); script deleted after the check | Real mp3 bytes back from Microsoft's wss endpoint for a zh and an en utterance | `zh-CN-XiaoxiaoNeural` → 30528 bytes, header `fff364c4` (MPEG frame sync); `en-US-JennyNeural` → 18720 bytes; status().tts.available = true | Passed |
| LAYER-TREE-001 | Focused layer tests | `npm test -- src/core/study-layer/layers.test.ts src/server/studyLayer.test.ts src/client/workspace/layerTree.test.ts src/client/workspace/layerViews.test.tsx` | Preset/custom layers hang under Mine, imported hierarchy remains intact, Layer tree helper covers parent + descendants, and Layer UI no longer renders a fixed Stages group | 4 files, 33 tests passed | Passed |
| LAYER-TREE-001 | Type check | `npm exec tsc -- --noEmit` | TypeScript passes after Layer tree rendering and parentId seeding changes | Passed | Passed |
| LAYER-TREE-001 | Production build | `npm run build` | Vite production build succeeds after Layer tree model/UI changes | Passed; existing chunk-size warnings only | Passed |
| UI-SETTINGS-001 | Type check | `npm exec tsc -- --noEmit` | TypeScript passes after Settings provider row structure and CSS changes | Passed | Passed |
| UI-SETTINGS-001 | Focused settings tests | `npm test -- src/client/settings/SettingsHub.test.tsx src/client/settings/AiProvidersSection.test.tsx` | Settings registry/order, memory switch, provider picker, BYOK add/edit/delete, detect, test connection, and env-only flows remain green after DOM/style refactor | 2 files, 19 tests passed | Passed |
| UI-SETTINGS-001 | Production build | `npm run build` | Vite production build succeeds after Settings CSS and component changes | Passed; existing chunk-size warnings only | Passed |
| UI-SETTINGS-001 | DOM/style smoke | Playwright against `http://127.0.0.1:5173`; screenshot saved to `C:/Users/Jump/AppData/Local/Temp/growte-settings-provider-unified.png` | Settings uses the shared pane shell; section cards are not nested; provider rows are small cards with hidden native radios, 16px custom markers, neutral chips, and accent-blue selected state | Hub radius 10px, white bg, border `rgb(230, 234, 240)`; first section border none; provider row radius 8px, selected bg `rgb(238, 245, 255)`, border `rgb(198, 217, 255)`; native radio hidden at 1x1 opacity 0; custom marker 16x16 accent-blue border; capability chip neutral gray | Passed |
| SHELL-PRIM-001 | Type check | `npm run check` | TypeScript passes after the core `file-link` spec, the client note type, and the `openPath` bridge additions | Passed (0 errors) | Passed |
| SHELL-PRIM-001 | Full unit suite | `npx vitest run` | New `fileLinkNoteType.test.tsx` (registration at core seed + palette entry + `/链接`/`/file` alias resolution; card = title/basename + dimmed path + one-line note gist + 打开; full = whole note; desktop click dispatches `studyVault.openPath` and surfaces shell.openPath's error string; web click copies the path via clipboard and reads 复制路径; declared form seeds from `createDefault` and collects a schema-valid `{path,title,note}` draft; desktop 选择文件… fills the path via the existing `dialog:openFile` bridge) + extended `contentTypes.test.ts` (schema accept/reject, incomplete seed, toSearchText) + extended `adapters.test.tsx` (visible core entry, no provider badge, `/链接 我的讲义` resolves first); everything existing stays green | 158 files, 1531 tests passed (full combined tree, incl. the parallel UI session's in-flight edits) | Passed |
| SHELL-PRIM-001 | Production build | `npm run build` | Vite production build succeeds with the file-link module + scoped css bundled | Passed; existing chunk-size warnings only | Passed |
| SHELL-PRIM-001 | Electron bundles | `npm run electron:build:preload` + `npm run electron:build:main` | Preload rebuilds with `openPath`; main rebuilds with the guarded `shell:openPath` handler | Both rebuilt, exit 0 | Passed |
| UI-PLUGIN-001 | Type check | `npm exec tsc -- --noEmit` | TypeScript passes after Kit & Plugin header/search refactor and installed-list search filtering | Passed | Passed |
| UI-PLUGIN-001 | Focused plugin manager tests | `npm test -- src/client/workspace/pluginManagerViews.test.tsx` | Top tabs render, collapsed search expands below the tabs, installed/market search behavior stays wired, and existing install/filter flows stay green | 1 file, 12 tests passed | Passed |
| UI-PLUGIN-001 | Production build | `npm run build` | Vite production build succeeds after CSS and component changes | Passed; existing chunk-size warnings only | Passed |
| UI-PLUGIN-001 | DOM/style smoke | Playwright against `http://127.0.0.1:5173`; screenshot saved to `C:/Users/Jump/AppData/Local/Temp/growte-plugin-manager-tabs-search.png` | Kit & Plugin panel has top tabs, search hidden by default, magnifier expands an input below tabs, market tab still shows its type filter below search | Before click: tabs `已安装/市场`, active underline `rgb(52, 116, 230)`, no search row. After click: `搜索已安装…` row appears below tabs. Market tab switches placeholder to `搜索市场…` and keeps All/Plugins/Kits below the search row | Passed |
| REV-CORE-001 | Type check | `npm run check` | TypeScript passes after the registry capabilities/alias, the core mistake+grade types, the queue/panel generalization, and the review-plugin deletion | Passed (0 errors) | Passed |
| REV-CORE-001 | Full unit suite | `npx vitest run` | Queue rule 1/2 eligibility comes from `spec.mistake` / `spec.review.reviewable` (alias-aware; a test-only FIXTURE spec enters rule 2 with zero queue-code changes; both mistake ids enter rule 1); ReviewPanel suite green with 存为错题 writing `"mistake"` and grading via `spec.review.expectedAnswer`; core registration of the 3 prompts (ids unchanged) + hidden `review.grade` (excluded from the slash palette, quiz stays in) + grade schema roundtrip + explain profileContext weave; `textbook.mistake` renders through the core plugin via the alias; installState tolerates a stale `"review"` plugin id; no plugin record/catalog entry for the review loop | 157 files, 1510 tests passed | Passed |
| REV-CORE-001 | Production build | `npm run build` | Vite production build succeeds with `src/kits/review/` deleted and the core review modules bundled | Passed; existing chunk-size warnings only | Passed |
| UI-CONTROLS-001 | Type check | `npm run check` | TypeScript passes after JSX structure changes for shared check/switch controls | Passed | Passed |
| UI-CONTROLS-001 | Focused panel tests | `npm test -- src/client/profile/ProfilePanel.test.tsx src/client/settings/SettingsHub.test.tsx src/client/workspace/layerViews.test.tsx src/client/workspace/pluginManagerViews.test.tsx src/client/workspace/operationViews.test.tsx src/client/review/ReviewPanel.test.tsx` | Profile/settings switch clicks still persist, layer toggles still dispatch, plugin/operation toggles remain functional, and review panel stays green after hidden note-editor checkbox styling | 6 files, 47 tests passed | Passed |
| UI-CONTROLS-001 | Production build | `npm run build` | Vite production build succeeds after shared CSS primitives and `design.md` update | Passed; existing chunk-size warnings only | Passed |
| UI-CONTROLS-001 | DOM/style smoke | Playwright against `http://127.0.0.1:5173`; screenshot saved to `C:/Users/Jump/AppData/Local/Temp/growte-ui-controls-001.png` | Layers rows use `.sv-check`, active layer cards use accent-blue background/border, persistent switches use the Codex-like pill geometry and accent-blue on state | Layers: 5 rows, 5 `.sv-check-box`, checked box `16x16` with `rgb(52, 116, 230)`; Profile switch `34x20` with `rgb(52, 116, 230)`; Plugin Manager rendered 32 `.sv-switch` controls | Passed |
| UI-CONTROLS-001 | Desktop restart | Stop project `Growte.exe` processes and start `npm run electron:dev`; verify Vite server | Running desktop client picks up the shared control styling and rebuilt Electron bundles | New project `Growte.exe` main/gpu/utility/renderer processes running; dev server returned 200 on `http://127.0.0.1:5173/`; main/preload/webview-preload bundles rebuilt | Passed |
| F3-D1-001 | Full unit suite (adapter contract + guest overlay) | `npx vitest run` | `collectAnchorRects` returns first/last/all over every `[data-sv-key]` element; `MarkerOverlay` positions chips from `adapter.rectsFor(...).first` and repositions on `adapter.onLayoutChange`; the webview guest preload paints anchors, hosts a real body-mounted overlay chip (no inline chip in the `<mark>`), and bridges clicks via `sv:marker-action`; all existing tests stay green | 157 files, 1494 tests passed | Passed |
| F3-D1-001 | Type check | `npm run check` | TypeScript passes after the adapter contract, MarkerOverlay routing, and per-reader adapter changes | Passed (0 errors) | Passed |
| F3-D1-001 | Production build | `npm run build` | Vite production build succeeds with the adapter module bundled | Passed; existing chunk-size warnings only | Passed |
| F3-D1-001 | Webview preload build | `npm run electron:build:webview-preload` | Electron webview guest bundle rebuilds with the overlay-hosted markers and stays framework-free | `dist-electron/webview-preload.cjs` rebuilt (31.1kb); grep for "react" in the bundle: 0 hits | Passed |
| ANNOT-FOCUS-001 | Focused anchor/annotation regression tests | `npx vitest run src/client/workspace/anchorProjection.test.ts src/client/markerOverlay.test.ts src/client/annotationMarkers.test.ts src/client/annotations.test.ts src/client/annotationDom.test.ts src/client/surfaces/DomReader.test.ts` | Current focused anchor is merged into the paint/reveal projection; no-note DOM anchors still get highlight/key for marker positioning; no-note markers show only anchor glyph | 6 files, 70 tests passed | Passed |
| ANNOT-FOCUS-001 | Type check | `npm run check` | TypeScript passes after workspace projection and annotation grouping changes | Passed | Passed |
| ANNOT-FOCUS-001 | Production build | `npm run build` | Vite production build succeeds after focused-anchor visibility changes | Passed; existing chunk-size warnings only | Passed |
| ANNOT-FOCUS-001 | Webview preload build | `npm run electron:build:webview-preload` | Electron webview guest bundle remains current after marker fallback change | `dist-electron/webview-preload.cjs` rebuilt | Passed |
| ANNOT-FOCUS-001 | Desktop restart | Restart project `Growte.exe` with `npm run electron:dev`; verify Vite server | Running desktop client picks up focused-anchor visibility changes | Dev server returned 200 on `http://127.0.0.1:5173/`; project `Growte.exe` main/gpu/utility/renderer processes running | Passed |
| ANNOT-MARKER-001 | Focused marker/annotation regression tests | `npx vitest run src/client/markerOverlay.test.ts src/client/annotationMarkers.test.ts src/client/annotationDom.test.ts src/client/surfaces/DomReader.test.ts src/client/selection/webviewSelection.test.ts` | Marker HTML exposes anchor/note roles; overlay marker clicks focus anchors and note marker clicks open the shared multi-note card path; DOM/webview annotation plumbing remains green | 5 files, 91 tests passed | Passed |
| ANNOT-MARKER-001 | Type check | `npm run check` | TypeScript passes after adding marker action callbacks to the reader contract | Passed | Passed |
| ANNOT-MARKER-001 | Production build | `npm run build` | Vite production build succeeds with interactive marker plumbing bundled | Passed; existing chunk-size warnings only | Passed |
| ANNOT-MARKER-001 | Webview preload build | `npm run electron:build:webview-preload` | Electron webview guest bundle includes marker action forwarding | `dist-electron/webview-preload.cjs` rebuilt | Passed |
| ANNOT-MARKER-001 | Desktop restart | Start `npm run dev`, then `npm run electron:dev`; verify project `Growte.exe` processes | Running desktop client picks up marker action and webview preload changes | Dev server returned 200 on `http://127.0.0.1:5173/`; project `Growte.exe` main/gpu/utility/renderer processes running | Passed |
| ANNOT-PIN-001 | Focused annotation card regression tests | `npx vitest run src/client/annotationDom.test.ts` | Pinned cards ignore stale viewport placement, restore size only, and follow the highlighted text rect on scroll | 1 file, 31 tests passed | Passed |
| ANNOT-PIN-001 | Type check | `npm run check` | TypeScript passes after anchor-relative pinned-card positioning changes | Passed | Passed |
| ANNOT-PIN-001 | Production build | `npm run build` | Vite production build succeeds after shared annotation-layer changes | Passed; existing chunk-size warnings only | Passed |
| ANNOT-PIN-001 | Webview preload build | `npm run electron:build:webview-preload` | Electron webview guest bundle picks up the shared annotation-layer positioning fix | `dist-electron/webview-preload.cjs` rebuilt | Passed |
| LINKED-NOTE-001 | Type check | `npm run check` | TypeScript passes after linked-note focus and note-viewer changes | Passed | Passed |
| LINKED-NOTE-001 | Focused workspace tests | `npx vitest run src/client/workspace/anchorViews.test.tsx src/client/workspace/NoteListPanel.test.tsx src/client/workspace/RightSidebarTabs.test.tsx` | Linked-note click focuses anchor+note and does not render inline popover; Notes tab activates; focused note card highlights/scrolls | 3 files, 4 tests passed | Passed |
| LINKED-NOTE-001 | Production build | `npm run build` | Vite production build succeeds after right-sidebar/focus changes | Passed; existing chunk-size warnings only | Passed |
| ANNOT-REF-001 | Type check | `npm run check` | TypeScript passes after preview payload, annotation marker, and viewer plumbing changes | Passed | Passed |
| ANNOT-REF-001 | Focused annotation/surface tests | `npx vitest run src/client/annotationDom.test.ts src/client/surfaces/DomReader.test.ts src/client/selection/webviewSelection.test.ts src/client/surfaces/overlay.test.ts src/client/surfaces/DomReader.reveal.test.tsx` | Floating cards render preview HTML instead of raw text/JSON; note-count badges are stamped; DOM/webview/region plumbing remains green | 5 files, 78 tests passed | Passed |
| ANNOT-REF-001 | Production build | `npm run build` | Vite production build succeeds with the client-side static preview renderer bundled | Passed; existing chunk-size warnings only | Passed |
| ANNOT-REF-001 | Browser smoke | In-app browser against `http://127.0.0.1:5173` | App mounts and Source Viewer areas are present before desktop restart | Frontend returned 200; root mounted; Source Viewer-related containers detected; current browser vault had no painted `.sv-annotated` notes to visually hover | Passed with data caveat |
| ANNOT-REF-001 | Desktop restart | `npm run electron:build:webview-preload`; restart current Growte process with `npm run electron:dev` | Webview preload and running desktop client pick up annotation payload changes | `dist-electron/webview-preload.cjs` rebuilt; new Growte process group running | Passed |
| NOTE-REF-001 | Type check | `npm run check` | TypeScript passes after card metadata, shared shell, CSS, and kit preview changes | Passed | Passed |
| NOTE-REF-001 | Focused component/unit tests | `npx vitest run src/client/workspace/artifactCard.test.tsx src/client/notes/noteTypeRegistry.test.tsx src/client/workspace/views.test.tsx src/client/workspace/BookmarkIndex.test.tsx`; then `npx vitest run src/client/workspace/artifactCard.test.tsx src/client/notes/noteTypeRegistry.test.tsx src/kits/textbook-learning/noteTypes.test.tsx` | Artifact card labels/previews, overlay rendering, note registry full renders, workspace surfaces, bookmark changes, and textbook kit card mode remain green | First run: 4 files, 47 tests passed. Second run: 3 files, 48 tests passed. | Passed |
| NOTE-REF-001 | Browser screenshot + computed-style smoke | Playwright against the running `http://127.0.0.1:5173`; screenshots saved to `C:/Users/Jump/AppData/Local/Temp/note-ref-001-smoke-kit-card.png` and `C:/Users/Jump/AppData/Local/Temp/note-ref-001-overlay-final.png` | Preview cards use the reference compact card dimensions and expanded overlay uses the reference centered shell | Preview card measured `260x138`; first card showed icon/type/title/two-line body/footer. Expanded overlay measured `980px` wide, title `亿以上数的认识`, layer `拓展`, four header actions, body font `14px`. | Passed |
| UI-REF-014 | Type check | `npm run check` | TypeScript passes after CSS calibration and right-column preset adjustment | Passed | Passed |
| UI-REF-014 | Browser screenshot + computed-style smoke | Playwright against `http://127.0.0.1:5173`, viewport 1576x890; screenshots saved to `C:/Users/Jump/AppData/Local/Temp/growte-current-full-after-ui-ref-014c.png` | Whole shell aligns closer to the supplied reference: panel bottom/right margins, right Anchor/AI split, topbar tab sizing/no wrapping, lighter borders/gray scale, and compact controls | Panels end at y=870 with a 20px bottom gap; right column x=1222 and right edge=1562; Anchor/AI heights 372/418; topbar center 398x40; three topbar tabs 131x34 with nowrap and no overflow | Passed |
| UI-REF-014 | Production build | `npm run build` | Vite production build succeeds after calibration pass | Passed; existing chunk-size warnings only | Passed |
| UI-REF-013 | Type check | `npm run check` | TypeScript passes after adding the left-pane CSS normalization layer | Passed | Passed |
| UI-REF-013 | Browser computed-style + screenshot smoke | Playwright against `http://127.0.0.1:5173`; switched Library, Anchors/Bookmarks, Concepts, Operations, and Layers from the left icon rail; screenshots saved to `C:/Users/Jump/AppData/Local/Temp/growte-left-operation-ui-ref-013b.png` and `C:/Users/Jump/AppData/Local/Temp/growte-left-layer-ui-ref-013c.png` | Left rail panes share the same shell/header metrics and checkbox controls are compact | All five panes measured `250x818`, 16px panel padding, 10px radius, `#e9edf3` border, 13px panel text, 14px/600 headers; Operations/Layers checkbox controls measured 16x16 | Passed |
| UI-REF-013 | Production build | `npm run build` | Vite production build succeeds after CSS-only left-pane changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-012 | Type check | `npm run check` | TypeScript passes after adding window-control IPC bridge and TopBar buttons | Passed | Passed |
| UI-REF-012 | Browser DOM smoke | Playwright against `http://127.0.0.1:5173` | Window controls are absent in plain browser mode | `windowControls:0`, `desktopClass:0` | Passed |
| UI-REF-012 | Electron bundles | `npm run electron:build:main`; `npm run electron:build:preload` | Main and preload bundles compile with the new IPC bridge | Both passed | Passed |
| UI-REF-012 | Electron DOM smoke | Temporary Electron dev launch against Vite | Desktop bridge exists and topbar renders three controls | `desktop:true`, `hasWindowControlsBridge:true`, one `.topbar-window-controls`, three buttons: Minimize, Maximize/Restore, Close | Passed |
| UI-REF-012 | Production build | `npm run build` | Vite production build succeeds after TopBar/CSS changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-012 | Desktop restart | Restart current project Electron dev client | Running window picks up new main/preload | Old project Electron main stopped; new main process started as PID 17564 | Passed |
| UI-REF-011 | Type check | `npm run check` | TypeScript passes after swapping the reader tab icon and CSS overrides | Passed | Passed |
| UI-REF-011 | Browser computed-style + screenshot smoke | Playwright against `http://127.0.0.1:5173`; screenshot saved to `C:/Users/Jump/AppData/Local/Temp/growte-reader-tab-after.png` | Active reader tab is white, neutral gray, file-tab shaped, and no longer blue-selected | `.reader-tab.active` height 40px, white bg, `#e9edf3` side/top border, white bottom border, 13px/500 gray title, gray document icon/close | Passed |
| UI-REF-011 | Production build | `npm run build` | Vite production build succeeds after reader tab changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-010 | Type check | `npm run check` | TypeScript passes after typography/token calibration | Passed | Passed |
| UI-REF-010 | Focused theme test | `npm test -- src/client/theme/registry.test.ts` | Default theme tokens match the updated lighter base palette | 1 file, 7 tests passed | Passed |
| UI-REF-010 | Browser computed-style + screenshot smoke | In-app browser at `http://127.0.0.1:5173`, viewport 1600x900, screenshot saved to `C:/Users/Jump/AppData/Local/Temp/growte-current-ui-after-typography.png` | UI controls use lighter body text/background, softer active blue, neutral PDF tab border, and lighter control weights | Body `#252b34/#f7f8fa`; topbar active `#2f67d7` on `#f1f6ff`; reader tab border `#e9edf3`; source item text 13px/500 or active 600 | Passed |
| UI-REF-010 | Production build | `npm run build` | Vite production build succeeds after CSS/theme changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-009 | Type check | `npm run check` | TypeScript passes after simplifying AI Chat | Passed | Passed |
| UI-REF-009 | DOM + interaction smoke | Playwright against `http://127.0.0.1:5173` | Note controls absent; bottom input uses `Type / for commands`; history is above input; Enter sends to chat history and clears input | Note control counts all 0; bar height 46, input height 42; `userMessages:1`, `inputValue:""` | Passed |
| UI-REF-009 | Production build | `npm run build` | Vite production build succeeds after chat UI changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-008 | Type check | `npm run check` | TypeScript passes after removing the Layer Lens toggle and changing anchor cleanup | Passed | Passed |
| UI-REF-008 | Focused server tests | `npx vitest run src/server/app.test.ts` | Anchor list is note-derived; note-less anchors prune; delete/edit cleanup and patch-reference protection pass | 1 file, 45 tests passed | Passed |
| UI-REF-008 | DOM smoke | Playwright against `http://127.0.0.1:5173` | `Show anchors with no visible notes` text and `.layer-lens-toggle-row` are absent | `{"textCount":0,"rowCount":0}` | Passed |
| UI-REF-008 | Production build | `npm run build` | Vite production build succeeds after UI/server changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-007 | Type check | `npm run check` | TypeScript passes after responsive containment CSS changes | Passed | Passed |
| UI-REF-007 | DOM width check | Playwright against `http://127.0.0.1:5173` in Note composer mode | Major panels and `.composer-actions` stay within their border width | `.library-panel`, `.reader-panel`, `.anchor-panel`, `.study-panel`, `.composer-actions` all reported `scrollWidth <= clientWidth` | Passed |
| UI-REF-007 | Production build | `npm run build` | Vite production build succeeds after responsive CSS changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-006 | Type check | `npm run check` | TypeScript passes after brand mark sizing/font changes | Passed | Passed |
| UI-REF-006 | Browser crop | Playwright screenshot of `http://127.0.0.1:5173` top-left area | Top-left brand uses larger black anchor icon and serif `Growte` wordmark | Saved `C:/Users/Jump/AppData/Local/Temp/growte-top-left-after.png`; visually matches reference more closely | Passed |
| UI-REF-006 | Production build | `npm run build` | Vite production build succeeds after brand mark changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-005 | Type check | `npm run check` | TypeScript passes after frameless-window and drag-region changes | Passed | Passed |
| UI-REF-005 | Electron main bundle | `npm run electron:build:main` | Main process bundle includes the frameless BrowserWindow config | Passed | Passed |
| UI-REF-005 | Electron preload bundle | `npm run electron:build:preload` | Preload bundle still builds before relaunch | Passed | Passed |
| UI-REF-005 | Desktop restart | Relaunch `npx electron dist-electron/main.cjs --dev` | Electron dev client restarts with one process group | Running: node parent plus Electron main/gpu/utility/renderer children | Passed |
| UI-REF-004 | Type check | `npm run check` | TypeScript passes after removing the open-folder wrapper shell | Passed | Passed |
| UI-REF-004 | Production build | `npm run build` | Vite production build succeeds after folder-tree styling changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-003 | Type check | `npm run check` | TypeScript passes after disabling collapsed dock rails | Passed | Passed |
| UI-REF-003 | Focused unit test | `npx vitest run src/client/workspace/dock.test.ts` | Dock collapse helpers report no collapsible/collapsed panes | 1 file, 21 tests passed | Passed |
| UI-REF-003 | DOM smoke | Playwright against the already-running `http://127.0.0.1:5173` | `.dock-rail`, `.dock-collapse-btn`, Settings, and right note-list counts are all 0 | Counts all 0 | Passed |
| UI-REF-003 | Targeted Playwright spec attempt | `npx playwright test e2e/layout-engine.spec.ts e2e/theme-switch.spec.ts` | Simplified UI specs run | Blocked before tests by existing dev server using API port 4177 (`EADDRINUSE`); covered by DOM smoke above | Blocked |
| UI-REF-003 | Production build | `npm run build` | Production build succeeds after dock changes | Passed; existing chunk-size warnings only | Passed |
| UI-REF-002 | Type check | `npm run check` | TypeScript passes after removing desktop menu/topbar buttons/note-list rendering | Passed | Passed |
| UI-REF-002 | DOM smoke | Playwright against `http://127.0.0.1:5173` | Settings button, Reader/library button, and `.study-panel .note-list` are absent | Counts all 0 | Passed |
| UI-REF-002 | Desktop restart | `npm run electron:dev` | Desktop client relaunches with rebuilt Electron main/preload | Electron process running (`dist-electron/main.cjs --dev`) | Passed |
| UI-REF-002 | Production build | `npm run build` | Production client build succeeds after cleanup | Passed; existing chunk-size warnings only | Passed |
| UI-REF-001 | Type check | `npm run check` | TypeScript passes after TopBar/CSS/seed changes | Passed | Passed |
| UI-REF-001 | Focused unit test | `npx vitest run scripts/seed.test.ts` | Empty-vault demo seed still creates exactly one deterministic source | 1 file, 3 tests passed | Passed |
| UI-REF-001 | Full unit suite | `npm run test` | Existing unit/component tests pass after UI changes | 74 files, 645 tests passed; jsdom canvas warning unchanged | Passed |
| UI-REF-001 | Visual browser check | Playwright screenshot at 1600x900 against `http://127.0.0.1:5173` | Growte shell shows the reference-style topbar, rail, Layer Lens popover, rounded panels, and right chat panel | Passed; current local content differs because the active vault opens the user's existing Chinese PDF and notes | Passed |
| UI-REF-001 | Production build | `npm run build` | Vite production build succeeds | Passed; Vite emitted existing chunk-size warnings only | Passed |
| MECH-001 | File review | Confirm `docs/implementation/` files exist | Workflow files are present | Six workflow files are present. | Passed |
| MECH-001 | Git status | `git status --short --branch` | New Markdown files are visible as untracked changes | `?? docs/` is visible on `main...origin/main`. | Passed |
| PLAN-001 | Git branch | `git status --short --branch` | Current branch is `codex/ai-study-vault` | Passed | Passed |
| PLAN-002 | Reference research | Web research against Obsidian public docs/repos | Borrowable public architecture references identified | Passed | Passed |
| PLAN-003 | Codebase inspection | Read shared types, storage, server routes, agent modules, and client app structure | Current GrowHTML migration points identified | Passed | Passed |
| PLAN-004 | Slice planning | Review `00-research-and-plan.md` and `02-verification-log.md` | C1-C8 sequence and verification plan are defined before coding | Passed | Passed |
| C1 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C1 | Unit tests | `npm run test` | Schema/id fixture tests pass | 2 files, 8 tests passed | Passed |
| C2 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C2 | Unit tests | `npm run test` | JSONL snapshot store tests pass | 3 files, 14 tests passed | Passed |
| C3 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C3 | Unit tests | `npm run test` | Vault layout tests pass | 4 files, 16 tests passed | Passed |
| C4 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C4 | Unit tests | `npm run test` | Source ingest tests pass | 5 files, 19 tests passed | Passed |
| C5a | Type check | `npm run check` | TypeScript passes after worker integration | Passed | Passed |
| C5a | Unit tests | `npm run test` | HTML core tests pass | 6 files, 27 tests passed | Passed |
| C5b | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C5b | Unit tests | `npm run test` | Anchor guard tests pass | 7 files, 34 tests passed | Passed |
| C6 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C6 | API tests | `npm run test` | Supertest API coverage passes | 8 files, 38 tests passed | Passed |
| C8 | Type check | `npm run check` | TypeScript passes | Passed | Passed |
| C8 | Unit/script tests | `npm run test` and `npm run seed` | Seed creates deterministic source | 9 files, 41 tests passed; default vault seeded | Passed |
| C7 | Type check | `npm run check` | TypeScript passes after client implementation | Passed | Passed |
| C7 | Unit/API regression | `npm run test` | Existing data/API tests still pass | 9 files, 41 tests passed | Passed |
| C7 | Production build | `npm run build` | Vite production build succeeds | Passed: client bundle generated under `dist/` | Passed |
| C7 | Browser QA | In-app browser against `http://127.0.0.1:5173` | Content capture -> anchor -> note -> patch -> apply -> revert -> reload works | Passed. Click fallback on `data-study-id` block created anchor draft; note persisted; patch applied to iframe; revert restored original after reload. | Passed |
| Step① NoteLayer | Unit + E2E | `npm run test` / `npm run e2e` | Saved note paints onto the document (`.sv-annotated` + title tooltip), survives reload | 49 unit; e2e asserts highlight class + title regex on `[data-study-id="e2e-p"]` and after reload | Passed |
| Step② Open PDF | API + E2E | `npm run test` / `npm run e2e` | PDF base64 ingest (magic guard), `/file` serves exact bytes (`application/pdf`), reader switches to native viewer | +2 API tests (byte-equal + non-PDF 400); e2e imports a PDF and re-fetches served bytes | Passed |
| Step③ Rich forms | Unit + E2E | `npm run test` / `npm run e2e` | markdown/mindmap/flashcard render; XSS escaped; bad input → inert text; client renders via registry | +7 renderer unit tests; e2e: markdown→`<p>`, flashcard→`<summary>` flip card | Passed |
| Step④ AI chat | Unit + API + E2E | `npm run test` / `npm run e2e` | Mock provider deterministic; `buildSubprocessEnv` strips API key in subscription mode; `/api/chat` answers; chat→save-as-note | +5 AI unit, +2 chat API (answer + empty 400); e2e: ask→reply→save note | Passed |
| Steps①–④ regression | Full suite | `npm run check` / `npm run test` / `npm run e2e` | All green | `check` clean; 65 unit (12 files); 4 e2e | Passed |
| Single-origin server | API + integration | `npm run test` | `clientDir` serves index.html + SPA fallback, API not shadowed; `startServer` boots real HTTP on a free port | +1 supertest (static + fallback + `/api/health`); +1 `startServer` test fetches `/api/health` over HTTP | Passed |
| Electron shell | Unit + bundle | `npm run test` / `npm run electron:build:main` + `:preload` | Pure dev/url/clientDir helpers correct; main+preload bundle without unresolved imports | +3 shell unit tests; esbuild produced `dist-electron/main.cjs` (52.8kb) + `preload.cjs` (195b) | Passed |
| Electron desktop loop | E2E (Playwright `_electron`) | `npm run e2e:electron` | Launches the REAL Electron app; renderer loads from the in-process server; import → select → anchor → note → on-document overlay → AI chat all work | `e2e-electron/app.spec.ts` 1 passed (550ms). Drives the packaged shell, not the dev web server. | Passed |
| Electron visible window | Manual (display) | `npm run electron` on a machine with a desktop session | A window visibly opens and is interactive | Functional loop already covered by the `_electron` test; visible-window check deferred (this automation session is non-interactive — `electron.exe` launched and exited cleanly with no error/output, i.e. no attachable desktop) | Pending (cosmetic) |
| Note: Electron binary | Install | `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/ node node_modules/electron/install.js` | Binary downloaded | GitHub release download stalled in this locale; npmmirror mirror succeeded; `node_modules/electron/dist/electron.exe` present | Passed |
| SourceViewer registry | Unit + regression | `npm run test` / `npm run e2e` / `npm run e2e:electron` | Type→viewer mapping + plugin override; html/pdf flows unchanged after refactor | +3 unit; 4 web e2e + 1 electron e2e still pass | Passed |
| Webview annotation backbone | Unit + API | `npm run test` | `web_text_quote` selector create/resolve (disambiguation); `web_live` ingest; web anchor via API | +5 textQuote unit; +2 API (web-live ingest, web anchor) | Passed |
| Webview Electron embed | E2E (`_electron`) | `npm run e2e:electron` | Open Live a URL → web_live source → `<webview>` loads it with the guest preload attached | `e2e-electron/webview.spec.ts` asserts `webview[src]` + `[preload$=webview-preload.cjs]`; 1 passed | Passed |
| Webview guest round-trip | Manual (display) | Select text on the live page in the desktop app | Selection → web anchor → `<mark>` highlight re-appears | Logic covered by textQuote unit tests; full guest-DOM drive deferred (Playwright can't reliably drive `<webview>` guest content) | Pending (manual) |
| PDF anchor model | API | `npm run test` | `pdf_selection` by page+quote (rect optional); page required | +2 API tests (create + missing-page 400) | Passed |
| PDF.js text layer | E2E (web) | `npm run e2e` | Real PDF renders a selectable text layer; select → anchor → note → highlight | loop e2e uses `makeTextPdf` fixture; asserts text layer, selection card, note, `.pdf-anchor-hit` | Passed |
| Diagram note forms | Unit + E2E | `npm run test` / `npm run e2e` | mermaid/markmap dispatch; mermaid note renders an SVG | +2 registry unit tests; mermaid e2e asserts `.note-diagram-mermaid svg` visible | Passed |
| StorageAdapter seam | Unit + regression | `npm run test` | Whole vault (open/ingest/persist/reopen) runs on `MemoryStorageAdapter`; Node path unchanged | +1 in-memory vault test; all prior store/vault/source tests still pass on the Node default | Passed |
| Pure sha256 | Unit | `npm run test` | Matches FIPS vectors + node:crypto across block boundaries; `computeContentHash` output unchanged | +2 sha256 tests; existing source-hash test still passes | Passed |
| Pure path guard | Unit | `npm run test` | Accepts normal relative paths; rejects `..`/absolute | +2 guard tests | Passed |
| PTY provider (transport) | Unit | `npm run test` | ANSI parser; provider returns cleaned answer + reuses one session; factory selects `claude-pty` w/o loading node-pty | +7 tests (FakePtySession-driven, deterministic) | Passed |
| PTY real CLI + xterm | Manual (display) | desktop with authenticated `claude` + `@electron/rebuild` | Persistent live session answers in chat; visible terminal interacts | node-pty load verified under Node (1.1.0); live CLI + Electron ABI rebuild deferred to a display machine | Pending (manual) |
| AI terminal pipeline | E2E (`_electron`, fake PTY) | `npm run e2e:electron` (STUDY_VAULT_PTY_FAKE) | Toggle terminal → start → output reaches xterm; typed input echoes back | `terminal.spec.ts` asserts banner + echo via `.pty-raw` mirror; deterministic, no native node-pty | Passed |
| AI terminal real CLI | Manual (display) | desktop + `npm run electron:rebuild` + authed `claude`/`codex` | Live CLI runs in xterm | node-pty Electron-ABI build deferred to target machine (winpty source build needs MSVC tools) | Pending (manual) |
| Full regression | Full suite | `npm run check` / `npm run test` / `npm run e2e` / `npm run e2e:electron` | All green | `check` clean (incl. `electron/`); 96 unit (22 files); 5 web e2e; 3 electron e2e | Passed |

## Slice 1 Verification Plan (Vault Data Layer)

Design principle: the data layer (`core/`, `adapters/`) is pure TS over files — no AI, no browser — so it is built **test-first**. Each task's "verification method" *is* its companion test file. ~80% of Slice 1 is fully automatable; only C7 (UI) needs manual QA or one E2E.

### Tooling to add (during C1/C2)

- **Vitest** — unit/integration runner (pairs with Vite, zero-config TS). New script `npm run test`.
- **linkedom** (or jsdom) — server-side HTML parse/serialize used by the HTML Adapter in production *and* in tests (same code under test).
- **supertest** — exercise Express endpoints without binding a port.
- **Playwright** (optional, C7) — single happy-path E2E only.
- Isolation: every data-layer test creates a throwaway vault under `os.tmpdir()`; never touches `data/`.

### Per-task verification

| Task | Verification | Automated | Tool |
| --- | --- | --- | --- |
| C1 schema+ids+fixtures | Types compile; Zod accepts valid / rejects invalid (bad envelope, wrong type, missing time/version); ids unique + `<type>_<ulid>` well-formed; golden fixtures validate | Yes | `tsc --noEmit` (`npm run check`) + Vitest |
| C2 store | upsert→read; **status update** (pending→applied→reverted) overwrites same id; delete semantics; malformed-line tolerance; atomic rename is crash-safe (no torn file) | Yes | Vitest (tmp dir) |
| C3 vault | create empty vault → assert `.study/`, `sources/`, `exports/`, `manifest.json`(version) layout | Yes | Vitest |
| C4 source ingest | ingest HTML → file written + one `sources.jsonl` record + list returns it | Yes | Vitest |
| C5a HTML core | `data-study-id` inject idempotent; applyPatch per action (replace/insert/append/rewrite) mutates content; **revert restores original**; serialize round-trips | Yes (highest-value) | Vitest + linkedom |
| C5b anchor + guard | resolve fallback (studyId→selector→quote) survives edits/reorder; **drifted patch returns conflict, is NOT applied** | Yes (highest-value) | Vitest + linkedom |
| C6 server API | list / get rendered / create anchor / Note·Patch CRUD — status codes + response bodies | Yes | Vitest + supertest |
| C8 seed/migration | after run a deterministic source exists (imported `content.html` or generated demo) | Yes | Vitest / script |
| C7 client | browser walk: select → anchor → manual Note → manual Patch (hand-typed) → apply → revert → **reload still correct** | Manual QA, or 1 E2E | manual / Playwright |

### Test pyramid & commands

- Many unit tests (C1–C5) → few API integration tests (C6) → 0–1 E2E (C7). Do not stack E2E.
- Commands: `npm run check` (types) + `npm run test` (Vitest). Both CI / pre-commit friendly.
- The HTML Adapter's `applyPatch` / anchor resolver run server-side in production, so tests call the real production functions — not stubs.

### Slice 2 (AI) verification — recorded early

- Do **not** assert against the live model (non-deterministic, costs money/time). Mock at the **Provider boundary**.
- Action plugins' `buildPrompt` / `parseResult` tested with fixed fixture outputs → deterministic, automatable.
- The Claude Agent SDK provider gets one manually-triggered smoke test, skipped in CI by default.

## Command Log

| Time | Command | Result | Notes |
| --- | --- | --- | --- |
| 2026-06-23 | `rg --files` | Passed | Reviewed initial project file list. |
| 2026-06-23 | `Get-Content README.md` | Passed | Reviewed project usage and workflow notes. |
| 2026-06-23 | `Get-Content package.json` | Passed | Confirmed available scripts. |
| 2026-06-23 | `rg --files docs\implementation` | Passed | Confirmed workflow files are under the repository. |
| 2026-06-23 | `git status --short --branch` | Passed | Confirmed new `docs/` changes are visible to Git. |
| 2026-06-23 | `git switch -c codex/ai-study-vault` | Passed | Created implementation branch. |
| 2026-06-23 | `Get-Content -Raw -Encoding UTF8 pasted-text.txt` | Passed | Read AI Study Vault v0.3 attachment without mojibake. |
| 2026-06-23 | Web research | Passed | Reviewed Obsidian license, API, sample plugin, Dataview, Metadata Extractor, and Local REST API references. |
| 2026-06-23 | `npm install zod ulid linkedom` | Passed | Added runtime dependencies for schema, IDs, and future HTML core. |
| 2026-06-23 | `npm install -D vitest supertest @types/supertest` | Passed | Added test/API verification tools. |
| 2026-06-23 | `npm run check` | Passed | C1 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C1 tests passed: 2 files, 8 tests. |
| 2026-06-23 | `npm run check` | Passed | C2 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C2 tests passed: 3 files, 14 tests. |
| 2026-06-23 | `npm run check` | Passed | C3 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C3 tests passed: 4 files, 16 tests. |
| 2026-06-23 | `npm run check` | Passed | C4 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C4 tests passed: 5 files, 19 tests. |
| 2026-06-23 | Worker C5a verification | Passed | Worker reported `npm run check` and `npm run test` passing. |
| 2026-06-23 | `npm run test` | Passed | Main integration after C5a: 6 files, 27 tests. |
| 2026-06-23 | `npm run check` | Passed | Main integration after C5a type check passed. |
| 2026-06-23 | `npm run check` | Passed | C5b type check passed. |
| 2026-06-23 | `npm run test` | Passed | C5b tests passed: 7 files, 34 tests. |
| 2026-06-23 | `npm run check` | Passed | C6 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C6 tests passed: 8 files, 38 tests. |
| 2026-06-23 | `npm run check` | Passed | C8 type check passed. |
| 2026-06-23 | `npm run test` | Passed | C8 tests passed: 9 files, 41 tests. |
| 2026-06-23 | `npm run seed` | Passed | Seeded default vault with demo source. |
| 2026-06-23 | `npm run check` | Passed | C7 client type check passed after iframe selection/click capture changes. |
| 2026-06-23 | `npm run test` | Passed | C7 regression tests passed: 9 files, 41 tests. |
| 2026-06-23 | Browser QA | Passed | Opened `http://127.0.0.1:5173`; clicked demo paragraph; created anchor; saved note; created patch; applied; reverted; reloaded and verified persistence. |
| 2026-06-23 | `npm run build` | Passed | Production build succeeded after C7. |

## Manual QA Notes

Use this section for browser checks, visual inspection, screenshots, and behavior that automated commands do not cover.

- No manual UI QA is required for `MECH-001` because this task only adds documentation.
- C7 browser QA passed on 2026-06-23. The in-app browser verified: open default source, click the demo paragraph (`data-study-id="demo-paragraph"`) to create a selection draft, create an Anchor, save a Note, create a pending Patch, apply it and confirm the iframe text changes to `Patched paragraph from C7 browser QA.`, revert it and confirm the original paragraph returns, reload and confirm the Note and reverted Patch persist.
- Native drag text-selection inside the iframe was difficult to drive reliably through browser automation, so the client now supports both selected-text capture (`selectionchange` / `mouseup` / `keyup`) and a deterministic click fallback on `data-study-id` blocks. The verified path used the click fallback; real selected text remains the preferred path when present.
- C7 (minimal client) manual QA chain: open library → open a source → select text → an anchor is created → add a manual Note → it appears in history → create + apply a Patch → rendered content changes → revert → original content restored → **reload the page and confirm source/notes/patches persisted correctly**. Capture a screenshot per step.

## Slice 1 Code Review & Fixes (2026-06-23)

Reviewed all Slice 1 code (core/adapters/server/client/seed). Fixed directly; `npm run check` + `npm run test` green (9 files, 43 tests, +2 regression tests).

| Severity | Issue | Fix |
| --- | --- | --- |
| High | `SnapshotStore` read-modify-write had no serialization → lost updates under concurrent API writes | Per-store async write lock (`withLock`) around `upsert`/`delete`; concurrency regression test added |
| Medium | Path-traversal guard was prefix-only (`startsWith(sourcesDir)`) → sibling-dir bypass | Require exact match or `sourcesDir + path.sep` |
| Medium | API `noteKind`/`action` were loose `z.string()` → generic 400s, could create non-HTML actions | Use `noteKindSchema` / `patchActionSchema` in request DTOs |
| Medium | Patch status transitions unguarded (apply without accept, revert pending, …) | `patchTransitions` state machine → 409 on illegal transition; regression test added |
| Low | Dead code: 8 orphaned old GrowHTML modules (`storage/agents/claudeAgent/codexAgent/documentAgent/proposalPrompt/externalImport` + `shared/types`) | Deleted (no references from new code) |
| Low | `writeJsonlAtomic` temp name (`pid+Date.now()`) could collide | Use `randomUUID()` |
| Low | Duplicate import line in `app.ts` | Merged |

Verified non-issues: `vaultEntitySchema` nested discriminated union is valid on zod 4.4.3 (covered by `schema.test.ts`). Projection/materialize (apply does not rewrite the source file) is intended design. `applyHtmlPatch` strictness (studyId→selector, no quote fallback) is intentional — the server apply path gets quote fallback via `applyHtmlPatchWithGuard`→`resolveHtmlAnchor`; materialize trusts already-guarded patches.

## Click Self-Test (E2E) — per-step self-verification

Every UI-affecting step is self-verified by a Playwright click-test that drives the **real running app**: Playwright boots `dev:server` + `dev:client` on an isolated `STUDY_VAULT_ROOT=.e2e-vault`, then drives Chromium. `e2e/loop.spec.ts` walks the full no-AI loop — import → click selection in the reader iframe → create Anchor → save Note → create Patch → **apply (assert rendered text changes)** → **revert (assert original returns)** → reload → reselect source (**assert Note persisted, Patch reverted**).

- Run: `npm run e2e` (or `npm run e2e:headed` to watch). Result 2026-06-23: **1 passed (~1.1s)**.
- Unit/integration stay on `npm run test` (Vitest, 45 passing); `vitest.config.ts` excludes `e2e/**` so the two never collide. Chromium installed via `npx playwright install chromium`.
- **Standard going forward:** data/logic steps → a Vitest companion test; UI/loop steps → extend `e2e/loop.spec.ts` (or a sibling spec) so each step ships with an automated click-test.

## Web URL → local snapshot (verification)

Verified by automated tests (no network flakiness):
- `src/adapters/web/snapshot.test.ts` — `normalizeUrl` (strip tracking params/hash/trailing slash, lowercase host) and `snapshotWebpage` (remove scripts/inline handlers, absolutize URLs, extract title).
- `src/server/app.test.ts` — `POST /api/sources/url` with a **mocked global `fetch`**: asserts a `webpage` Source is created with `metadata.normalizedUrl`, and the served content is sanitized (script stripped) + absolutized.
- Full suite green 2026-06-23: `npm run check`, `npm run test` (10 files, 49 tests), `npm run e2e` (1 passed). The click E2E was kept network-free (URL ingest covered by the mocked-fetch server test, not the browser loop).

## Residual Risk

- Slice 1 has been exercised end-to-end on a real feature task. Remaining risk: native drag-selection inside the iframe was not directly proven by automation; it is supported by the same handler and should receive a quick human spot check before relying on sub-paragraph selection precision.
- **Deferred (documented, not blocking):** (1) apply-time conflict detection checks a single patch vs original content, ignoring previously-applied patches → order-dependent for interacting patches; (2) `replace_selection`/`rewrite_section` drop `data-study-id` in materialized output (harmless now since materialize recomputes from the id-bearing original; matters only if patched content is persisted as source).
- **C1 amendment — DONE 2026-06-23:** `Note.contentType` (open string `noteContentTypeSchema`, default `markdown`) added; `metadata` promoted into `recordEnvelopeSchema` and deduped from `source.ts`; API note DTO passes optional `contentType`; 2 schema tests added (default markdown + metadata escape hatch; open-string contentType). `npm run check` + `npm run test` green (9 files, 45 tests).

## 2026-06-24 — Terminal cwd + placement (verification)

- `electron/ptyCwd.test.ts` — `resolveCwd` unit tests: empty/whitespace → fallback; existing dir → returned; missing path → fallback; file (not dir) → fallback.
- `e2e-electron/terminal.spec.ts` (rewritten) — drives the real Electron app: terminal hidden until **Show** (under AI Chat), fills the **Working directory** field, Start → fake spawner banner echoes the resolved cwd (proves renderer→IPC→bridge `resolveCwd`→spawner), input echoes, **Hide** closes it (regression for "un-closable terminal").
- Full suite green 2026-06-24: `npm run check`, `npm run test` (23 files, 100 tests), `npm run e2e:electron` (3 passed: app + terminal + webview). node-pty rebuilt against Electron ABI (real PTY now available; e2e still uses the deterministic fake via `STUDY_VAULT_PTY_FAKE=1`).

## 2026-06-24 — Persistent-session chat + annotation seam (verification)

- `src/client/annotations.test.ts` (5 tests, node env, no jsdom): `groupForRenderer` groups notes under their anchor and keeps only claimed anchor kinds / ignores orphan+missing-anchor notes; `decorateAnnotations` injects each renderer's stylesheet once across repaints, clears+paints each renderer with only its claimed anchors (spy renderer), and registers plugins ahead of built-ins. Real HTML painting stays covered by the overlay e2e.
- `claude -p` persistent session verified manually at the CLI: `--session-id <uuid>` (turn 1) then `--resume <uuid>` (turn 2) recalled a planted number; stdin prompt + Windows shell launch confirmed (returned `PONG`). `ClaudeCliProvider.complete` is the thin wrapper around exactly this flow.
- Full suite green 2026-06-24: `npm run check`, `npm run test` (24 files, 105 tests), `npm run e2e` (5 web), `npm run e2e:electron` (3 — app/terminal/webview). The overlay assertions in both e2e suites pass unchanged after routing through the AnnotationRenderer registry, proving behavior parity.

## 2026-06-24 — Shared annotation layer + chat context (verification)

- Decoupled layer verified by both overlay e2e suites unchanged in intent: web `loop.spec` html overlay now asserts hover → `#sv-note-card` visible with the note text; the PDF test asserts the SAME shared card floats over a `.pdf-anchor-hit` span with "PDF note." Proves one presentation layer works across HTML + PDF surfaces. Webview guest reuses the identical `applyHighlight`/`ensureAnnotationLayer` (manually verifiable; text-quote resolution unit-tested separately).
- Chat context: `chatContextSchema` gained sourceType/location/contextBefore/contextAfter (all optional → backward compatible); mock + claude-cli prompt builders updated. Existing AI-chat e2e still green (question + quote echoed); reply-save button renamed → e2e updated to "Save full reply".
- Full suite green 2026-06-24: `npm run check`, `npm run test` (24 files, 105 tests), `npm run e2e` (5 web), `npm run e2e:electron` (3). Electron relaunched with `STUDY_VAULT_AI_PROVIDER=claude-cli`.

## 2026-06-24 — Note card window + web tabs (verification)

- Note card: web `loop.spec` (html + PDF) still assert hover → `#sv-note-card` visible with the note text, now through the markdown-rendering body. Drag/resize/pin/close are interaction-heavy and verified manually.
- Web tabs: `e2e-electron/webview.spec.ts` extended — asserts exactly one `.webview-tab` on Open Live (titled with the host) plus the existing src/preload/plugins/nav/address assertions. The guest-side "click a link → new tab" path needs a click inside the guest webContents (not reachable from the host page in `_electron`), so it's a documented manual check.
- Full suite green 2026-06-24: `npm run check`, `npm run test` (24 files, 105 tests), `npm run e2e` (5), `npm run e2e:electron` (3). Electron relaunched with claude-cli.

## 2026-06-24 — Auto-anchor + chat source chip (verification)

- All 5 web e2e + 3 electron e2e rewritten to the no-manual-anchor flow (select → Save Note/Patch auto-creates the anchor; `.chat-source` chip asserted in place of `.selection-card`/`.anchor-chip`) — green.
- Full suite green 2026-06-24: `npm run check`, `npm run test` (24 files, 105 tests), `npm run e2e` (5), `npm run e2e:electron` (3). Electron relaunched with claude-cli.

## 2026-06-24 — Edit-resilient HTML anchoring (verification)

- `src/client/annotationDom.test.ts` (jsdom, 6 tests) covers the resolution logic that an e2e can't reach deterministically (patch materialize preserves study-ids, so "id lost" can't be simulated via the UI): study-id fast path AND text-quote fallback both produce the right DOM.
- Full suite green 2026-06-24: `npm run check`, `npm run test` (25 files, 111 tests), `npm run e2e` (5), `npm run e2e:electron` (3). Electron relaunched with claude-cli.

## 2026-06-25 — Unified Web viewer (snapshot + live) (verification)

- `src/client/viewers.test.ts` — extended: `webpage` now maps to the new `web` viewer kind/id while keeping `htmlPipeline: true`; `html`/`markdown` stay kind `html`; the plugin-priority test is unchanged.
- `e2e/web-snapshot.spec.ts` (NEW, web/chromium) — seeds a `webpage` snapshot from a local fixture HTTP server (`POST /api/sources/url`, offline) and asserts the UNIFIED shell renders in snapshot sub-mode (tab strip + "Snapshot" tab + address bar reflecting the source URL + nav-bar "Open Live") AND the full snapshot study flow still works: select in the snapshot iframe → `.chat-source` chip → Save Note → an **`html_selection`** anchor (not `web_text_quote`) carrying the quote → `.sv-annotated` highlight paints. Proves the study-id path is unchanged by the merge.
- `e2e-electron/web-snapshot.spec.ts` (NEW, Electron) — opens a `webpage` snapshot in the real app: asserts the snapshot tab renders via a `DomReader` iframe with **no** `<webview>` yet, then nav-bar **Open Live** spawns a SECOND tab backed by a real Electron `<webview>` (correct `src` + `webview-preload.cjs` + settled address bar). Covers the cross-mode snapshot→live behavior the host page can't.
- Existing `web_live` coverage unchanged + green: `e2e-electron/webview.spec.ts` (Open Live → `<webview>`) and `e2e-electron/viewer-flows.spec.ts` (live HTML select→chip→note→`web_text_quote` anchor→highlight→hover card).
- Full suite green 2026-06-25: `npm run check` (clean), `npm run test` (38 files, 247 tests), `npm run e2e` (14 web), `npm run e2e:electron` (9). DomReader gained an optional `onOpenUrl` (link interception) used only by the snapshot tab; omitting it preserves the imported-HTML reader's prior behavior (its e2e stays green).

## 2026-06-25 — Study Layer V1 foundation (verification)

- Scope: schema (`SourceFingerprint`/`StudyLayer`/`layerId`/`matchStatus`/`note.origin`) + `layer` id kind + `layers` store; pure rematch resolver; owned-layer + boot migration; export/preview/commit APIs + anchors-filtered-by-enabled-layer. Core + server only — no client/PDF/viewer files touched.
- `src/core/study-layer/rematch.test.ts` — the resolver's three tiers and kind dispatch: exact prefix+quote+suffix → matched; single occurrence → matched; multiple occurrences disambiguated by context → matched (right one); ambiguous → fuzzy; whitespace/case drift → fuzzy; absent/empty → unmatched; pdf page-text quote, pdf/image rect by `sameBinary` (matched vs fuzzy), code_range symbol fallback.
- `src/core/study-layer/layers.test.ts` — `ensureOwnedLayer` creates exactly one owned layer per source and reuses it (fingerprint carries the source contentHash); `migrateStudyLayers` backfills `layerId` onto pre-layer anchors+notes and is idempotent (second run touches nothing). Confirms **backward compat**: a legacy anchor/note written with no `layerId` loads fine (optional field) before the migration assigns one.
- `src/server/studyLayer.test.ts` — new anchors/notes get stamped with the source's owned layer + listed; export produces a portable pack with `studyId`/`selector`/`sourceId` stripped and a `contentHash` fingerprint; preview matches the local source by `contentHash` (stats matched:1); commit re-realizes the html anchor against the importer's OWN injected study-id (not the author's) with `matchStatus:"matched"` + creates the imported note; disabling a layer hides its anchors from `GET /sources/:id/anchors` while enabled layers stay.
- Backward-compat guarantee: `layerId` (anchor+note) is `.optional()`, so `readJsonl`/snapshot-store loading of existing `.study-vault`/`.vault-dev` rows does not fail validation (a required field would have dropped them); `migrateStudyLayers` runs on server boot (`start.ts`) to backfill. Verified by the idempotent migration test and by the full existing suite staying green.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (41 files, 265 tests), `npm run e2e` (14 web — existing viewer/PDF/region/concept/note flows unaffected by the anchor-layer filter). `npm run e2e:electron` NOT run by this task (no Electron-specific surface changed; the layer changes are server/core, covered by unit + web e2e) — flagged, not claimed.

## 2026-06-25 — Study Layer V2 (local .studypack UX) (verification)

- Scope: client UI for the V1 server. New `layer.switcher` view (`src/client/workspace/layerViews.tsx`) — lists a source's layers (owned + imported) with an `enabled` toggle, per-layer Export (downloads `.studypack`), and an Import flow (file pick → `import/preview` three-state summary → confirm → `import/commit`). EntityClient gained `layers/patchLayer/exportLayer/importPreview/importCommit`; a `layer.toggle` command + `onLayersChanged` action; context `refreshLayers()`/`layersVersion`. Added as an additive 5th pane in `studyVaultLayout` (existing pane DOM untouched); `body min-width` 1460→1740px so the flexible reader doesn't collapse.
- `src/client/commands/registry.test.ts` — `layer.toggle` patches `enabled` + fires `onLayersChanged`; unavailable without a `layerId`/boolean `enabled`. (Existing command tests' client mock gained `patchLayer`.)
- `e2e/study-layer.spec.ts` (NEW, web/chromium) — seeds an HTML source, then drives the REAL Layers-pane UI: pick a `.studypack` (3 anchors built to hit each state, matched by fingerprint title) → preview shows **matched 1 / fuzzy 1 / unmatched 1** → Import → an `imported` layer appears AND the matched passage paints (`.sv-annotated`) → toggling the layer off hides the highlight, on restores it → server lists the imported layer under the source.
- Smoke case: `scripts/smoke-study-layer.ts` (`STUDY_VAULT_ROOT=.vault-dev npx tsx scripts/smoke-study-layer.ts`) seeds source "Study Layer Smoke — Rendering Notes" into the dev vault and writes `docs/samples/sample.studypack` (anchors → matched/fuzzy/unmatched). Verified it runs and emits the pack + the source id; manual steps printed (open source → Import Layer → preview 1/1/1 → Import → toggle).
- Deferred (flagged, not done): `copy-note-to-mine`, `anchor.rematch` + match-inspector view, `layer` FocusTarget / note-list-by-layer filter.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (41 files, 267 tests; +2 layer.toggle), `npm run e2e` (15 web — all prior flows + the new study-layer flow). `npm run e2e:electron` NOT run by this task (no webview/Electron-specific path changed; V2 is server/core + a host-page pane) — flagged, not claimed.

## 2026-06-25 — Product Kit foundation + Textbook Kit phase 1 (verification)

- Scope: register-only Product Kit infra (`src/kits/{types,language,clientContext,clientKits,index,server}.ts(x)`) + Textbook Learning Kit phase 1 (`src/kits/textbook-learning/{contentTypes,noteTypes,language,index}`): 3 Study Block note types (Explanation/Practice/Mistake) registered into the EXISTING NoteTypeRegistry + core NoteContentSpec registry. No core schema change; core never imports a kit (server `createApp`→`installServerKits`; client `views.tsx`→side-effect `clientKits`).
- `src/kits/textbook-learning/contentTypes.test.ts` — all 3 specs register into the core registry; each `createDefault` round-trips its schema; API-path `parseNoteContent` accepts valid content + rejects bad enums; `.default([])`/`.default(...)` fields fill on parse; `toSearchText` reduces to clean text. Proves the server validates `textbook.*` content with zero core changes.
- `src/kits/textbook-learning/noteTypes.test.tsx` (jsdom) — installing the client kit registers a plugin for each type + exposes domain labels via `kitContentTypeLabel` (Explanation/Practice/Mistake); each render emits its card (prose as sanitized markdown, options, mastery/difficulty badges); renders NEVER throw on foreign/mis-shaped content; the explanation editor's onChange emits schema-valid content.
- `e2e/textbook-kit.spec.ts` (NEW, web/chromium) — drives the REAL composer: the picker offers "Explanation" (`option[value="textbook.explanation"]`), authoring via the kit's structured editor + Save renders the kit's Explanation card in the note list (kind label, level badge, markdown-bold prose, key-points list). Proves register-only end to end (no App/Workspace/Shell edits).
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (43 files, 279 tests; +12), `npm run e2e` (16 web — all prior flows + the new textbook-kit flow). `npm run e2e:electron` NOT run by this task (phase 1 adds no webview/Electron-specific path; it is composer/note-list, covered by unit + web e2e) — flagged, not claimed.
- Phase-2 research (for the next task): the `ModelProvider` does free-text markdown only (no structured/JSON output) — phase 2 must add a structured-generation path + deterministic mock JSON; and there is no selection-toolbar surface yet (selection fills the chat chip) — the `surfaces.contribute('selection-toolbar', …)` collector is in place as the seam.

## 2026-06-25 — Textbook Kit phase 2: AI commands + structured generation + selection toolbar (verification)

- Scope: register-only Product Kit phase 2. Structured generation core (`src/ai/provider.ts` optional `completeStructured`; `src/ai/mockProvider.ts` echoes the host `sample`; `src/kits/structured.ts` `generateStructuredContent`/`extractJson`/`StructuredGenerationError`; `src/kits/prompts.ts` registry), server endpoint `POST /api/kits/generate` (`src/server/app.ts`, `installServerKits` now registers prompts too), client `entityClient.generateStructured` + `CommandContext.client` Pick. Textbook kit: `prompts/{explainConcept,generatePractice,markAsMistake}.prompt.ts`, `commands.ts` (3 host Commands), `surfaces.ts` (selection-toolbar items), `index.tsx` registers commands+prompts+surfaces. Host UI: `SelectionToolbar.tsx` + mount in `views.tsx`; `kits/clientContext.tsx` now registers kit commands into the CommandRegistry + exposes `kitSurfaceItems`. No core schema change; `src/ai` never imports a kit.
- `src/kits/structured.test.ts` — `extractJson` parses bare/fenced/embedded JSON + throws on none; `generateStructuredContent` returns the prompt's deterministic mock content via the mock; a text-only flaky provider that emits invalid-then-valid output is RETRIED and validated; persistently invalid output → `StructuredGenerationError`; unknown promptId/contentType/mismatched outputType all rejected.
- `src/kits/textbook-learning/prompts.test.ts` — each prompt `build()` embeds the passage + asks for JSON; each `mockContent()` PARSES against its matching `NoteContentSpec` schema (the e2e-determinism guarantee); practice answer is one of its options; every prompt `outputType` equals its spec contentType.
- `src/kits/textbook-learning/commands.test.ts` — explain/practice/mistake `isAvailable` gate on a focused passage; `run` calls `materializeAnchor` → `generateStructured({promptId, contentType, input.anchorText})` → `createNote({contentType, anchorIds})` → `onNoteCreated`; each targets its own contentType; unavailable with no focus.
- `e2e/textbook-kit-ai.spec.ts` (NEW, web/chromium, mock provider) — select a passage → the kit selection toolbar (Explain/Practice) appears → Explain generates a structured Explanation Study Block that renders as the kit card → Practice generates an Exercise card on the same passage. Proves structured generation + kit commands + selection-toolbar contribution end to end, register-only.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (46 files, 292 tests; +13), `npm run e2e` (17 web — all prior flows + the new textbook-kit-ai flow). `npm run e2e:electron` NOT run by this task (no webview/Electron-specific path changed; the surface is the host study panel, covered by unit + web e2e) — flagged, not claimed.
- Manual check with the real provider (claude-cli/-pty): the desktop providers have no `completeStructured`, so they take the text→JSON fallback — verify Explain/Practice in the client by selecting a passage and clicking the toolbar action; the JSON-only prompt + retry should yield a valid card (tune the prompt against the live CLI if a model adds prose).

## 2026-06-25 — Textbook Kit phase 3: Review Pack + propagation policy + student layout (verification)

- Scope: register-only Product Kit phase 3, closing the spec §16 MVP loop. Adds the 4th Study Block `textbook.review-pack` (core spec in `textbook-learning/contentTypes.ts` + render/edit in `noteTypes.tsx`), the source-level `textbook.generate-review-pack` command (`commands.ts`, gathers the source's explanation+mistake notes via `client.notes`), a new `source-actions` surface slot + host `SourceActionsToolbar.tsx` (mounted top of the study panel), a React-free layer-policy registry (`src/kits/policy.ts` + `textbook-learning/policy.ts`) consumed by the export filter in `src/server/studyLayer.ts` (`buildStudyPack` drops private-by-default notes + orphan anchors), and a `textbook.student-learning` column layout preset (`textbook-learning/layout.ts`). No core schema change; core never imports a kit.
- `src/kits/textbook-learning/contentTypes.test.ts` — now asserts all 4 Study Block types register + round-trip; review-pack requires `scope.sourceId`.
- `src/kits/textbook-learning/prompts.test.ts` — the review-pack prompt builds scoped to the source + its `mockContent` parses against the review-pack schema (keyPoints from explanations, weakPoints from mistakes); every prompt `outputType` matches its spec.
- `src/kits/textbook-learning/commands.test.ts` — `generate-review-pack` `isAvailable` gates on an active source; `run` calls `client.notes(sourceId)`, passes the gathered explanation+mistake content into `generateStructured`, and saves an unanchored `textbook.review-pack` note.
- `src/kits/policy.test.ts` (NEW) — `textbook.mistake` is private-by-default (NOT exportable); explanation/exercise/review-pack + generic `markdown` stay exportable; re-registering a kit policy is idempotent.
- `e2e/textbook-kit-review.spec.ts` (NEW, web/chromium, mock provider) — (1) the source-level "Review Pack" action (a `source-actions` contribution) generates a structured review-pack Study Block that renders as a kit card; (2) creating a Mistake + an Explanation then exporting the source's owned layer yields a pack containing `textbook.explanation` but NOT `textbook.mistake` (propagation policy, end to end through the real export endpoint).
- Smoke: `scripts/smoke-textbook-kit.ts` (`STUDY_VAULT_ROOT=.vault-dev npx tsx scripts/smoke-textbook-kit.ts`) seeds source "Textbook Smoke — Cell Biology", fills its owned layer with all four block types, exports to `docs/samples/teacher-layer.studypack`, and asserts the export excludes `textbook.mistake`. Verified: exported note types = explanation, exercise, review-pack (mistake stripped); §16 manual steps printed.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (47 files, 298 tests; +6), `npm run e2e` (19 web — all prior flows + the two new textbook-kit-review tests). `npm run e2e:electron` NOT run by this task (no webview/Electron-specific path changed; phase 3 is host-page study panel + server/core, covered by unit + web e2e) — flagged, not claimed.
- Deferred (flagged): full §8 dock layout + a layout-switcher UI to activate the student preset (base shell renders columns only); `copy-note-to-mine`; `anchor.rematch` UI.

## 2026-06-25 — Per-source Product Kit activation (verification)

- Scope: a source picks its active kit(s). Data array `source.metadata.activeKitIds` (core metadata escape hatch — no schema change); single-select reader-header dropdown. Gates creation entry-points only (selection toolbar, source-actions, composer type picker, kit commands, language); note-type render + content specs stay global. New files: `src/kits/activation.ts` (pure `effectiveKitIds` + localStorage default + `activeKitIdsForSource`), `src/kits/activation.test.ts`, `e2e/kit-activation.spec.ts`. Edited: `src/kits/clientContext.tsx` (tag registrations with `kitId`: `kitSurfaceContributions`/`kitNoteTypeOwners`/`kitCommands`; `kitSurfaceItems(slot, kitIds?)`, `noteTypeOwnerKit`, `installedKits`), `src/kits/language.ts` (kitId-tagged, gate-aware accessors), `SelectionToolbar.tsx`/`SourceActionsToolbar.tsx` (`kitIds` prop), `workspace/views.tsx` (Kit dropdown + `noteTypeOptions(activeKitIds)` + toolbars threaded), `WorkspaceContext.tsx` (`activeKitIds`/`installedKits`/`setActiveKit`), `data/entityClient.ts` (`updateSourceMetadata`), `server/app.ts` (`PATCH /api/sources/:sourceId` metadata merge), `styles.css` (`.kit-select`).
- `src/kits/activation.test.ts` (NEW) — `effectiveKitIds`: metadata array authoritative (empty = Core), absent → workspace default, core sentinel/non-strings filtered, default `core`/null → []. Plus the gate: `kitSurfaceItems` returns a kit's items only when its id is in the active set (`[]` and a foreign id exclude; omitted = all); `noteTypeOwnerKit` tags kit types and returns undefined for built-ins.
- `e2e/kit-activation.spec.ts` (NEW, web/chromium, mock provider) — default source has the textbook kit active (dropdown = `textbook-learning`): selection toolbar Explain visible, generate a Study Block, composer offers `textbook.explanation`. Switch the dropdown to **Core**: toolbar gone + composer no longer offers the kit type, **but the already-created Study Block still renders** (render not gated). Switch back to textbook: toolbar returns (per-source override).
- PATCH endpoint: NEW minimal `PATCH /api/sources/:sourceId` (merges `metadata` only, re-parses with `sourceSchema`, upserts) — no existing source-update route existed.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (48 files, **314** tests; +6), `npm run e2e` (**21** web — all prior flows + the new kit-activation test). `npm run e2e:electron` NOT run (no webview/Electron-specific path changed; host study panel + a server metadata PATCH, covered by unit + web e2e) — flagged, not claimed.
- Deferred (flagged): workspace-default-kit settings UI (default hardcoded `textbook-learning`); multi-kit activation UI (data already an array); `kitTerm` term substitution across the wider shell (accessor is gate-aware but not yet consumed).

### Dock layout engine — phase 1 (2026-06-25)
- Scope: `WorkspaceShell` recursively renders `layout.layout` as a dock tree (`split`/`leaf`); root split = the `.app-shell` flex container; leaves wrapped in `.dock-pane` carrying flex sizing; bidirectional resize gutters (col-resize in rows, row-resize in columns) persisting to `localStorage` (`sv-panel-widths`). New: `src/client/workspace/dock.ts` (pure model + `isFlexChild`/`childInitialPx`/`flexFor`/`dockLeafIds`/`clampDockPx`/`gutterTarget`), `dock.test.ts`. Edited: `WorkspaceShell.tsx` (recursive renderer), `presets.ts` (threePane/studyVault as single-row dock trees), `styles.css` (`.app-shell` grid→flex + `.dock-split`/`.dock-pane`/`.dock-resize-*`/`.dock-gap`, removed `.col-resize-*`), `WorkspaceShell.test.tsx` (pane order via `.dock-pane`).
- Compatibility: each view's own class/DOM unchanged — only the `.app-shell` outer container changed (grid→flex dock). Existing layouts re-expressed with identical pane order/sizes.
- Suite green: `npm run check` (clean), `npm run test` (49 files, **323** tests; +9 dock), `npm run e2e` (**21** web — all prior flows incl. PDF/regions/study-layer/textbook/concepts/marginalia, zero regression). `npm run e2e:electron` NOT run (no webview/Electron-specific path changed; the `.app-shell` markup change is exercised by the web suite — desktop `app.spec` asserts panel content by the same view classes, which are unchanged) — flagged, not claimed.
- Deferred to phase 2: pane collapse/expand, layout switcher UI, student layout preset, responsiveness (`body min-width:1740`); drag-to-dock + canvas later.

### Dock layout engine — phase 2 (2026-06-25)
- Scope: pane collapse/expand (chevron `.dock-collapse-btn` → `.dock-rail`), layout switcher (`.layout-select` in reader header), student layout preset (nested `column` split: reader over a Practice panel), minimal responsiveness (auto-collapse secondary panes below 1280px). New: `src/client/workspace/practiceViews.tsx` (the `practice` view — filters notes to `textbook.exercise`/`textbook.mistake`, renders via existing NoteType plugins; no new data path), `e2e/layout-engine.spec.ts`. Edited: `dock.ts` (pure `isCollapsibleLeaf`/`isPaneCollapsed`/`paneCollapseKey`/`paneLabel` + `RESPONSIVE_BREAKPOINT_PX`/`SECONDARY_KINDS`), `presets.ts` (`studentLearningLayout` + `LAYOUT_PRESETS`/`DEFAULT_LAYOUT_ID`/`getLayoutPreset`), `WorkspaceShell.tsx` (collapse state + viewport-width tracking + rail/chevron render + `.dock-gap` at collapsed boundaries), `WorkspaceContext.tsx` (`activeLayoutId`/`availableLayouts`/`setActiveLayout`, persist `sv-active-layout`), `views.tsx` (layout `<select>` in reader header), `App.tsx` (`ActiveWorkspaceShell` consumes `activeLayoutId`), `styles.css` (collapse btn/rail + `.layout-select` + `.practice-panel`; `body min-width:1740`→`0` with `.app-shell{overflow-x:auto}` + reader pane `min-width:360px`), `dock.test.ts` (+collapse/label/student-layout cases), `WorkspaceShell.test.tsx` (force `innerWidth=1400` so the wide-layout assertions aren't auto-collapsed; pane order read by panel class since the chevron is now the pane's first child).
- `dock.test.ts` (+cases): `isCollapsibleLeaf` (fixed leaf yes; flex reader / fixed `source.viewer` / nested split no); `isPaneCollapsed` (explicit toggle wins at any width; secondary auto-collapses `<1280`, stays at/above; non-secondary `study` never auto-collapses); `paneLabel` (kind map / `params.title` / raw-kind fallback); student layout `dockLeafIds` = `library, source-viewer, practice, study`.
- `e2e/layout-engine.spec.ts` (NEW, web/chromium): (1) collapse Concepts via chevron → `.concept-panel` gone + Expand rail visible + reader stays → expand restores; (2) switch the picker Study Vault → Textbook Learning → `.practice-panel` (nested bottom panel) appears, concept/layer panes gone → switch back restores; (3) shrink viewport to 1100 → Sources/Concepts auto-collapse to rails (reader stays) → widen to 1400 → restore.
- Regression caught + fixed: naively dropping `body min-width:1740`→`0` crushed the flex reader to ~0 at the 1280 e2e viewport (five fixed panes = 1300px), failing 10 web e2e (pointer-interception). Fixed by `.app-shell{overflow-x:auto}` + reader pane `min-width:360px` (shell scrolls when expanded panes overflow; secondaries collapse to fit on narrow screens). Re-run green.
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (49 files, **335** tests; +12 dock), `npm run e2e` (**24** web — all prior flows + 3 layout-engine, zero regression), `npm run e2e:electron` (**9** desktop — all green; ran it this time since `App.tsx`/shell render path changed).
- Deferred: drag-to-dock / pane reorder; workspace-default-layout settings UI (active layout persists, but no settings surface); richer Practice/tutor UX (current panel is a minimal drill list).

## 2026-06-25 — AI orchestration base v1: foundation + streaming (verification)
- Scope: (1) sink the structured-generation engine into the kit-agnostic base layer — new `src/ai/structured.ts` (`generateStructured(provider, {messages, schema, sample?, contentType?, context?}, maxAttempts)`, `extractJson`, `StructuredGenerationError`), exported from `src/ai/index.ts`; `src/kits/structured.ts` rewritten as a THIN wrapper (resolves `getKitPrompt` + the contentType's `getNoteContentSpec().schema` + sample, delegates to the engine; re-exports `extractJson`/`StructuredGenerationError` so existing import paths are unchanged). Iron rule held: `src/ai` imports no kit. (2) Unified provider capabilities — `ProviderCapabilities.streaming: boolean` + optional `ModelProvider.stream?()`; mock `streaming:true` with `complete()`/`stream()` sharing one private `answer()` (chunks rejoin byte-identical), `claude-cli`/`claude-pty` `streaming:false`. (3) Streaming transport — new `POST /api/chat/stream` (SSE `chunk`/`done`/`error`; validates via `chatRequestSchema` BEFORE streaming so a bad body still 400s; iterates `provider.stream` when present else wraps `complete()` as one chunk); `entityClient.chatStream(input,onDelta)` consumes the SSE stream with non-streaming `chat()` fallback; `anchor.ask-ai` feature-detects streaming (`onAssistantChunk` progressive render) with fallback; `/api/chat`, `/api/kits/generate` untouched. Design: `docs/design/ai-orchestration.md`.
- New/edited tests: `src/ai/structured.test.ts` (NEW — engine parse/validate/retry/exhaustion + JSON-only system injection + `completeStructured` path); `src/ai/ai.test.ts` (+mock `streaming` capability + multi-chunk stream rejoining `complete()`); `src/server/app.test.ts` (+SSE chunk/done framing rejoins one-shot answer + empty-body 400 before stream opens); `src/kits/structured.test.ts` (unchanged behavior; only added `streaming:false` to two ad-hoc provider literals for the new required capability field); `e2e/streaming-chat.spec.ts` (NEW).
- `e2e/streaming-chat.spec.ts` (web/chromium): seed HTML source → select passage (fills `.chat-source`) → Ask AI → assert the answering response is `text/event-stream` (proves the stream endpoint, not the fallback) + the assistant bubble grows (in-page length sampler over the streaming window observes >1 distinct positive length — `STUDY_VAULT_MOCK_STREAM_DELAY_MS=60` set on the e2e server spaces chunks) + final content is the deterministic mock answer (`You asked: …` + the quoted passage).
- Constraints verified: subscription mode still strips `ANTHROPIC_API_KEY`/`ANTHROPIC_AUTH_TOKEN` (`buildSubprocessEnv` test green); e2e uses the deterministic mock; `/api/chat`, `/api/kits/generate`, existing textbook AI e2e, and `loop.spec` AI-chat all green (zero regression).
- Suite green 2026-06-25: `npm run check` (clean), `npm run test` (**50** files, **346** tests; +new engine/stream/SSE cases), `npm run e2e` (**25** web — 24 prior + streaming-chat, zero regression). `npm run e2e:electron` NOT run — no Electron/webview-specific path changed: streaming runs through the same React client + Express server the web suite drives over the identical `fetch`/SSE path; no preload/IPC/main-process change. Flagged, not claimed.
- Deferred (out of scope for v1): tool-calling / agentic loops; embeddings / semantic retrieval; `buildChatContext` refactor (kept as-is); real-provider native token streaming (`claude-cli`/`claude-pty` are `streaming:false` → server single-chunk fallback).

## 2026-06-26 — Generation preview (generate → preview → edit → save) (verification)
- Scope: decouple a kit AI action from note persistence with a client-side preview stage. A new `GeneratedDraft` type + optional `CommandActions.onGenerated` (`src/client/commands/registry.ts`); `anchor.add-note` honors an explicit `payload.anchorIds` (present, even `[]`, OVERRIDES focus materialization) so Save reuses the anchor the generating command already created. `WorkspaceContext` gains `pendingDraft`/`regenerating` state + `savePendingDraft`/`regeneratePendingDraft`/`discardPendingDraft` and wires `onGenerated → setPendingDraft`. New `GenerationPreview` view (`src/client/workspace/GenerationPreview.tsx`) renders/edits the draft through the SAME client NoteType plugin a saved note uses (`getNoteType`), mounted as a separate DOM subtree above the chat log (`views.tsx`). Textbook commands (`textbook-learning/commands.ts`: `generateBlock` + `generate-review-pack`) emit via `onGenerated` when a host wired it, else keep the legacy auto-save (`createNote`) fallback. No `src/ai` change, no server change, no new core entity (the draft is transient client state until Save). Design: `docs/design/generation-preview.md`.
- `src/client/commands/registry.test.ts` (+3) — `anchor.add-note` with explicit `anchorIds:["anchor_pre"]` SKIPs `materializeAnchor` and saves to the given anchor; an empty `anchorIds:[]` still OVERRIDES focus (saves unanchored, no materialize — the source-level/Review-Pack case); without `anchorIds` it still materializes from focus (legacy path).
- `src/kits/textbook-learning/commands.test.ts` (+2) — explain (preview host): `run` still calls `generateStructured` but emits the draft via `onGenerated` (carrying `anchorId:"anchor_1"`) and does NOT `createNote`/`onNoteCreated`; review pack (preview host): emits the source-level draft via `onGenerated` (`anchorId:undefined`, `sourceId:"src_1"`) and does NOT auto-save.
- `e2e/generation-preview.spec.ts` (NEW, web/chromium, mock provider) — drives the REAL app: select a passage → Explain → the preview appears (`.generation-preview`, type `textbook.explanation`) with NOTHING in the note list yet (`.tb-explanation` count 0) → Edit toggles the kit editor, fill a unique sentinel title → Save persists exactly one card carrying the sentinel and clears the preview → Explain again → Regenerate keeps it pending (mock deterministic — content-change NOT asserted) with the saved count unchanged → Discard removes the preview, count still 1. Proves nothing crosses into `.note-list` until Save.
- Backward-compat verified: the feature is gated on the host wiring `onGenerated`; without it the textbook commands keep their original `createNote`+`onNoteCreated` path (existing `commands.test.ts` cases stay green), and `anchor.add-note` without `payload.anchorIds` is its old materialize-from-focus self. `src/ai` + server untouched (Save reuses `POST /api/notes` via `anchor.add-note`; Regenerate reuses `POST /api/kits/generate`).
- Suite green 2026-06-26: `npm run check` (clean), `npm run test` (**50** files, **351** tests; +5), `npm run e2e` (**26** web — all prior flows + the new generation-preview test, zero regression). `npm run e2e:electron` NOT run by this task (no webview/Electron/IPC/preload path changed; the preview is a host-page React view over the existing client+server fetch paths the web suite drives) — flagged, not claimed.
## 2026-06-30 - LIB-001 Library Open Folder + Recent Read

- `npm run check` - passed.
- `npx vitest run src/client/workspace/generatingState.test.tsx` - passed: 1 file, 4 tests. Added coverage for multiple opened folders, duplicate folder picks, and closing one root.
- Playwright browser smoke against `http://127.0.0.1:5173` - passed. Confirmed `.library-body-split` renders `Open Folder` above `Recent Read`, both lists use `overflow:auto`, and Recent Read remains below the Open Folder section. Screenshot: `C:/Users/Jump/AppData/Local/Temp/growte-library-split-smoke-2.png`.
- `npm run build` - passed. Vite reported only the existing large-chunk warning.
- Desktop restart - passed. `npm run electron:dev` relaunched and Electron process `60716` is running against the live Vite server; `http://127.0.0.1:5173` returned 200.

## 2026-06-30 — Note-list right-sidebar sub-tab + chat reply actions + blue selected-anchor

- Scope: three coupled UX fixes after the Growte IA rebuild removed the source-side note surfaces.
  1. **Note list as a collapsed sub-tab** in the right column. New `src/client/workspace/NoteListPanel.tsx` — a collapsible (folded by default, `.note-list-head` chevron) "Notes (N)" section listing the active source's VISIBLE non-bookmark notes as the §10 shared `ArtifactCard` PreviewCards (click → CenterView; row crosshair → `focus.setAnchor` reveal; row trash → `note.delete`). Embedded at the bottom of the Anchor pane (`anchorViews.tsx`) so it lives in the right sidebar with no dock surgery and zero wasted space when folded. Reads only `useWorkspace()` (`visibleNotes`/`anchors`/`focus`/`dispatch`).
  2. **Chat reply actions (§10 Add as note / Regenerate)** — `ChatMessageBody` gains optional `onAddNote`/`onRegenerate`/`busy` props and renders a `.chat-artifact-actions` row beneath any assistant reply (the rich card path is unchanged). `WorkspaceContext` adds `addReplyAsNote(text)` (classify via the SAME `classifyContent` heuristic → `anchor.add-note`, attaching to the focused passage if any, else an unanchored note on the source — so it surfaces in the new note list) and `regenerateChatReply()` (re-dispatch the last user prompt through `anchor.ask-ai`). `StudyView` passes `onAddNote` to every assistant reply and `onRegenerate` to the last. Also **re-mounted the orphaned `GenerationPreview`** above the composer (the IA rebuild had dropped its only caller), restoring the command-driven generate→preview→Save/Regenerate/Discard loop.
  3. **Blue selected-anchor highlight** — new `setSelectedAnchorInDoc(root, anchorId)` in `annotationLayer.ts` paints a persistent UI-accent-blue `.sv-selected` class on the focused anchor's element (clearing the previous one), mirroring `revealAnchorInDoc`. Wired into the three host-document readers (`DomReader`, `PdfReader`, `ImageReader`) alongside their reveal effects + repaint (so it survives re-paints). The `.sv-active` reveal pulse + `.sv-annotated` base were also recolored amber→accent-blue (annotationLayer CSS). Cross-realm webview readers (LocalHtml/Webview) keep reveal-only for now (persistent paint needs preload plumbing — deferred).
- Tests: `src/client/annotationDom.test.ts` (+4 `setSelectedAnchorInDoc`: paints match only / single-selection move / clear on empty id + null-root no-op / quote-escaped id). `src/client/workspace/artifactCard.test.tsx` (+3 chat actions: HTML reply cards AND shows the actions row → Add-as-note fires with the reply content; Regenerate fires + no row without handlers; `busy` disables). tsc clean; `npm run test` **657** tests green (650 prior + 7).
- Visual: screenshot-verified the collapsed `▸ NOTES 1` fold, the expanded fold rendering a real §10 markdown PreviewCard, and the blue `.sv-selected` highlight on "大数的认识" after a note-row jump (PDF page 5). HMR picked up the changes live; no client restart needed.
- e2e: NOT run this pass — the dev server occupies ports 4177/5173 (`reuseExistingServer:false` requires them free) and the baseline viewer/study specs were already red from the IA rebuild (separate reconciliation). Flagged, not claimed.
- Deferred: persistent blue selection inside webview/local-HTML guests (preload message); reconciling/refreshing the IA-broken e2e specs against the new note surfaces.

### Follow-up (same day) — persistent blue selection in webview/local-HTML guests
- Extended the blue selected-anchor highlight to the cross-realm webview surfaces (live `WebviewReader` + `LocalHtmlReader`). New `sv:select` IPC channel: `selectWebviewAnchor(webview, id)` (host, in `webviewSelection.ts`) → guest preload (`electron/webview-preload.ts`) calls the SAME `setSelectedAnchorInDoc`, and remembers the id to re-apply it after each `sv:anchors` repaint (which `clearAnnotations` would wipe). Wired into both readers' reveal effects (sends select on focus change + clears on focus-none). Now ALL five surfaces (iframe HTML / PDF / image / live web / local HTML) paint the focused anchor blue uniformly.
- Tests: `src/client/selection/webviewSelection.test.ts` (+4 `selectWebviewAnchor`: sends id / empty-id clears / null-webview no-op / swallows throwing send). `npm run check` clean; webview-preload esbuild bundle clean; suite **661** green.

## 2026-06-30 — e2e reconciliation after the Growte IA rebuild (non-creation specs)

The Growte IA rebuild made the right-panel composer AI-only, removing the manual
"select passage → write note" flow (`.chat-source` chip, Note-mode composer,
`.note-list .record-card`) AND several note-card affordances (per-note `.note-layer-*`
picker; `.note-anchor-*` multi-anchor controls are orphaned/not rendered). Per the user's
decision (先只修非创建类), the NON-creation specs were fixed to drive the new UI and the
manual-creation/generation specs were `test.skip`ped pending the note-creation UX decision.

Approach for fixes: seed sources/anchors/notes (and layer membership) via the API
(`POST /api/anchors` with a placeholder studyId + real quote → paints via the text-quote
fallback; `POST /api/notes` with `anchorIds`/`layerIds`; `PATCH /api/notes/:id` for
membership moves), then drive the surviving UI — the right-sidebar NoteListPanel fold
(`.note-list-head` → `.note-list-row`, `.note-edit-start`/`.note-edit-save`,
`.note-delete`), the Layers pane filter (`.layer-toggle`), and the shared CenterView
(click `.sv-preview-card` → `.sv-focus-overlay`). The focused-passage assertion moved from
`.chat-source` to `.anchor-excerpt-quote`. The chat "save reply as note" path moved to the
§10 `.chat-artifact-add` action.

FIXED (now green):
- note-edit-delete.spec.ts (3): seed note via API → NoteListPanel edit/delete + highlight removal.
- layer-as-lens.spec.ts (4): seed notes with `layerIds` via API, membership moves via PATCH, drive the Layers-pane filter, assert NoteListPanel visibility + derived `.sv-annotated` painting.
- streaming-chat.spec.ts (2): `.chat-source`→`.anchor-excerpt-quote`; the mermaid save-reply now uses `.chat-artifact-add` (waits for it to enable after streaming; selects the substring + clicks in one synchronous step so the selection is live).
- loop.spec.ts (2 of 3): marginalia (seed anchored note via API + toggle Document/Notes-Overlay; the default annotation mode is no longer guaranteed floating, so it clicks Document first); AI-chat save-reply via `.chat-artifact-add`.
- adaptive-note-forms.spec.ts (2 of 7 failing): markmap note-viewer + interactive-html ESCAPE guard — open from the NoteListPanel via card click → CenterView (the card is a light preview now; the live diagram/`.sv-interactive-frame` mounts only in the overlay). The two API-only tests (path-like-html degrade, Range) were already passing.

SKIPPED (manual-creation / generation / removed note-card UI — pending product decision):
- viewer-flows (2), web-snapshot (1), regions (3 of 6: pdf-quote-chip, pdf-region, image-region), loop:21, bookmark (2), textbook-kit (1), textbook-kit-ai (1), textbook-kit-review (2), kit-activation (1), generation-preview (1), operation-authoring (1), adaptive-note-forms (5: composer chip, bare-YouTube, form-router, .xmind import, generating-indicator).
- multi-anchor (2): the `.note-anchor-link/count/jump` card affordances are orphaned (not rendered) after the IA rebuild — deviation from the original "fix" classification (their interactive UI is gone, so they belong with the pending note-card-UX decision).
- concepts (1): NOT a note-IA issue — the concept-pane inspector overlaps the concept list (`.concept-inspector-host` intercepts clicks on `.concept-item`); a R7 concept-pane LAYOUT regression. Flagged for the concept/layer-pane work.

Product changes (minimal): `WorkspaceContext.addReplyAsNote` now respects the in-reply text
selection (`selectedTextOr`) — restores the old "save just the selected part" parity so a
selected diagram classifies on its own. `e2e-electron/{viewer-flows,local-html-highlight}`
pixel predicates were already updated yellow→blue by the prior commit.

Result: web e2e **23 passed / 24 skipped / 0 failed**; `npm run check` clean; `npm run test`
**661** unit green. NOT run: `e2e:electron` (needs a desktop build; the changes here are
web-spec + a tiny shared WorkspaceContext tweak covered by web e2e + unit).

Follow-ups for the parent / a later pass: decide the note-creation UX (manual composer vs
AI-only) to un-skip the ~24 creation specs; fix the concept-pane inspector/list overlap
(concepts.spec); the multi-anchor card affordance if multi-anchor UX is kept.

## 2026-06-30 — R7 Layer Lens + hierarchy (R7.1 core + R7.2 Lens UI)
- **R7.1 (core, committed a2e1f25)**: `studyLayerSchema += parentId` (additive); `ensureImportedParent` per-source "导入图层" container (owns no notes → never affects the enabled-OR filter); `commitImport` nests each imported `.studypack` layer under it (idempotent). Test: import creates one parent + hangs the layer; second import reuses it.
- **R7.2 (Lens UI)**: replaced the TopBar Layer Lens FALLBACK fiction with REAL data + a parentId **hierarchy tree**. New pure `layerTree.ts` (`buildLayerTree` w/ cycle+orphan guards, `descendantLeafIds`, `parentToggleState` on/off/mixed, `countNotesInLayers` distinct roll-up) + 6 unit tests. `LayerLensPopover` renders nested rows (indent by depth), leaf click → `toggleLayerFilter`, parent click → new `WorkspaceContext.setLayersEnabled(leafIds, target)` **cascade** (batch PATCH + single refresh), indeterminate (`Minus`) parent mark, roll-up counts, real "Visible note count". Filter semantics unchanged (per-leaf enabled-OR; a parent checkbox is only a cascade master + roll-up). `entityClient.StudyLayerRecord += parentId`.
- Verified: screenshot shows the math source's real layers (owned 数学 + 预习/学习/复习/拓展) with real counts, no fallback. tsc clean; `npm run test` **668** green (+6 layerTree).
- Deferred (full §8.2): multi-layer `.studypack` packs (a teacher pack carrying 预习/复习/拓展 as siblings under one parent); **R7.3** = fold layer management (rename/recolor/reorder/create/delete/import-export) from the `layer.switcher` pane into the Lens.

### R7.3 — fold layer management into the Lens
- New `LayerLensManage.tsx` (driven by `ctx.sourceLayers` + `ctx.refreshLayers`): create / rename / recolor / reorder / delete-custom + import-preview-commit / export `.studypack`, reusing the exported `downloadPack`/`groupOf`/`GROUP_ORDER` from `layerViews.tsx`. The Lens popover gained a footer **Manage** toggle that swaps the filter tree for this manager (spec §8.1 "管理(就地)"). The standalone `layer.switcher` pane is left untouched (its `.layer-toggle` filter backs the layer-as-lens e2e), so this is the Lens-embedded twin — no behavior change there (export-only edit to layerViews). tsc + 668 unit green; screenshot-verified Manage mode (Import / Add / MINE rename·recolor·reorder / STAGES export). e2e not re-run this pass (dev server holds the ports; the layerViews edit is export-only).
- **R7 complete** (R7.1 core + R7.2 Lens UI + R7.3 management). Deferred: multi-layer `.studypack` packs (full §8.2 teacher-pack hierarchy).

## R6 — Action Toolbar System (§7) — PLANNED (SUPERSEDED — implemented; see "2026-06-30 — R6 Action Toolbar System" below)
Starting context so a fresh session doesn't re-discover:
- **Already exists** (from the operation-as-data work): `ToolbarAction` model + `orderActions` + `EMPTY_OPERATION_PREFS` (`WorkspaceContext.tsx`); `selectionActions` + `sourceActions` memos (kit `kitSurfaceItems("selection-toolbar"|"source-actions")` + custom `operations`, ordered by `operation-prefs`); `runAction` (fires builtin command or `operation.run`); components `SelectionToolbar.tsx`, `SourceActionsToolbar.tsx`; `operationViews.tsx` (the operation builder/manager); server `operation-prefs.json` persistence.
- **R6 remaining**:
  - **R6.1 Anchor Action Bar** — the missing 3rd surface: a dense icon grid around the focused Anchor in the right Anchor pane (anchorViews.tsx), with DISABLED states when an action doesn't apply to the current anchor. Reuse the same action list + runAction; add an `anchorActions` memo (anchor-scope).
  - **R6.2 shared-model polish** — add a `description` to `ToolbarAction` → Hover tooltips (`name — description`); a grouped **More menu** (groups: Create Note / AI Actions / Study Actions / Custom Actions — the `group` field already exists); apply uniformly to all 3 surfaces.
  - **R6.3 Customize Toolbar panel** — per-surface (Inline / Anchor / My Actions) drag-reorder + show/hide + change-icon + pin custom Operation + Reset; persist to `operation-prefs` (extend the existing order/disabled with per-surface keys + icon overrides).
- Spec: `docs/design/ui-redesign-growte.md` §7. After R6: **R8** = bookmark hover-index (§9, needs optional `category` on `bookmarkSchema`).

## 2026-06-30 — Right sidebar rework: VS-Code vertical split + Notes/AI-Chat refinements
- **Vertical split (committed ed6e32b)**: the tabbed right sidebar (Anchor / Notes / Layers / AI Chat) can now be split VS-Code-style — drag a tab out onto a TOP/BOTTOM drop zone to POP that sub-page into its own vertically-stacked pane; the other pane keeps the remaining tabs. Row-resize divider (ratio clamped 0.2–0.8), merge-back `×` on the popped-pane header, single-pop V1. State persists to `localStorage` keyed by layout id (`sv-right-split:<layoutId>`). Both panes render through the SAME `getView(kind).render` path — no sub-view logic duplicated; the global dock engine (`dock.ts`/`WorkspaceShell.tsx`/`presets.ts`) is untouched. New: `rightSplit.ts` (pure clamp/persist/remaining-tabs helpers) + `rightSplit.test.ts` (+12); `RightSidebarTabs.tsx` rewritten (HTML5 DnD tabs + drop zones + divider). tsc clean; `npm run test` **680** green.
- **Notes chrome + AI-Chat default/fill (committed 79b586a)**: the Notes full-panel form now uses the shared `.panel-title` header (icon + title + count chip) to match the Anchor and Layers panes (replacing its ad-hoc uppercase label); rows scroll under the pinned header. `DEFAULT_RIGHT_SPLIT` now opens with AI Chat (`study`) popped into the BOTTOM pane by default (top group = Anchor/Notes/Layers, ratio 0.45 → bottom ~55%). The study pane became a full-height column flex (history `flex:1; min-height:0; overflow:auto`, composer pinned bottom); `.right-tabs-popped-body`/`-body` give it `height:100%` so it fills whether it's the active tab OR the popped bottom pane. tsc clean; `npm run test` **682** green.
- Verified: screenshot-verified the real tab drag → bottom pop + the forced-split render + merge-back; the default split (AI Chat bottom with composer pinned); the unified Notes header against Anchor/Layers. HMR live; no client restart needed.
- Deferred (flagged): multi-pop (>1 popped pane), horizontal split, cross-column drag out of the `right.tabs` leaf.

## 2026-06-30 — R6 Action Toolbar System (§7) — implemented (R6.1–R6.3 + polish)
Key finding from the planning pass (supersedes the PLANNED stub above): the Action Registry already lived in `WorkspaceContext` (the `ToolbarAction` model, `selectionActions`/`sourceActions` memos, `runAction`, `orderActions`, the `generating` flag) but had ZERO mounted surfaces (`SelectionToolbar`/`SourceActionsToolbar` were never rendered); and operation-as-data was already shipped, so custom Operations needed only PRESENTATION — no new data layer. R6 = build the render surfaces, wire them to the existing memos, and add per-surface prefs. Adaptive-note contract held throughout: surfaces only TRIGGER via `runAction`; results still flow through `GenerationPreview`/`getNoteType().render`. e2e-keyed classNames/data-attrs preserved.
- **R6.1 Anchor Action Bar + model groundwork (committed 01a0299)**: `description?` added to `KitSurfaceItem` + `ToolbarAction` (hover tooltips `name — description`); custom ops grouped `"Custom Actions"`, bookmark `"Create Note"`, textbook surfaces remapped to canonical groups (`AI Actions`/`Study Actions`) with descriptions. New `actionIcons.ts` (single `actionIcon()` map: kit glyphs + operation→`noteTypeIcon`, `Wand2` fallback) + `ActionGrid.tsx` (dense icon grid, `.action-grid-btn[data-action-id][data-action-kind]`, disabled/busy). Mounted in `anchorViews.tsx` between Excerpt and Linked-notes, disabled when no anchor/draft. New `e2e/anchor-action-bar.spec.ts` (written, not run). tsc clean; `npm run test` **682**.
- **R6.2 grouped More menu + bottom bar (committed 4122bd5)**: pure `groupActions()` (`actionGroups.ts`) buckets actions into the fixed order Create Note / AI Actions / Study Actions / Custom Actions / Other (empty dropped) + unit tests. `ActionMoreMenu.tsx` grouped overflow popover (name—description rows + Customize footer), PORTALED to `document.body` with fixed positioning computed from the trigger rect (right-aligned, flips above when space is tight, viewport-clamped) so ancestor `overflow:hidden` panes can't clip it; outside-click treats the portaled node as inside. `BottomBar.tsx` registered as view kind `action.bar` (NOT added to any preset/dock). More menu wired into the Anchor bar + inline `SelectionToolbar`. tsc clean; `npm run test` **687**.
- **R6.3 Customize Toolbar — per-surface (committed 73a699a)**: `operationPrefsSchema` gains optional `surfaces:{[surface]:{order,hidden}}` (global `order`/`disabled` remain the fallback; absent → `{}`, no migration) + server round-trip & old-shape back-compat tests. `WorkspaceContext`: `orderActionsForSurface(actions,prefs,surface)`; new `anchorBarActions` memo (anchor pool ordered for `"anchor"` so the Anchor bar configures independently of the inline toolbar); `saveActionPrefs()` seam; `openOperationManager()`/`registerOpenOperationManager()`. `operationViews.tsx` got surface tabs (Inline/Anchor/Bottom/My Actions) over a per-surface list with hide toggle + drag/arrow reorder + Reset to default; the More menu's "Customize Toolbar" footer now opens it. tsc clean; `npm run test` **689** (+2 server).
- **R6 polish (committed 0d54055)**: per-action icon picker — `operationPrefsSchema` gains top-level `icons:{actionId->name}` (additive + back-compat test); a curated `ICON_CHOICES` set (20 lucide glyphs incl. all legacy names) shared by the picker and resolver; `actionIcon` resolves override-first; Customize each row gets a Change-icon grid popover writing `prefs.icons[id]` via `saveActionPrefs` + Reset icon (global per id). Customize deep-link: `openOperationManager(surface?)` records `customizeSurface` ctx state; the panel consumes it once (effect) to pre-select the matching tab; `ActionMoreMenu` passes its own `surface` to `onCustomize`. tsc clean; `npm run test` **695**.
- Verified (screenshot, against the live dev server): anchor bar disabled (empty) vs enabled (5 actions, `name — description` tooltips); the grouped More menu rendering unclipped over the bottom pane with all four canonical groups + the (initially disabled) Customize footer; the Customize panel's per-surface hide (hid Practice on the Anchor surface → the live bar dropped 5→4→persisted across reload) and surface tabs; the icon picker propagating a picked glyph (flag) to the live Anchor bar; the deep-link landing on the Anchor surface tab. Verification-only vault prefs mutations were restored (textbook params re-seeded; `icons`/`surfaces` cleared).
- e2e NOT run during the build (dev server holds ports 4177/5173); a full e2e pass is being run separately as a follow-up.
- Deferred per user: the bottom action bar is NOT added to the default layout (registered only).

## 2026-06-30 — R8 Bookmark hover-index (§9)
- **Committed 97e9d7b**. `bookmarkSchema` gains optional `category` (`src/core/notes/contentTypes.ts`; additive, absent = ungrouped). New `useBookmarks.ts` hook centralizes bookmark list + jump + `isJumpable` + `currentAnchorId` (+ distinct categories for the editor datalist); `bookmarkViews.tsx` now consumes it (behavior-identical, `.bookmark-row`/`data-note-id` selectors preserved). New `groupBookmarks.ts` (pure `groupBookmarksByCategory` — named groups first, "Ungrouped" last, empty omitted, rows by `order` then `label` — + `distinctCategories`) with unit tests. New `BookmarkIndex.tsx`: the outermost right-edge hover-reveal strip (collapsed = `List` icon, doesn't eat reading width) → hover slides a category-grouped catalog in over content; pin toggle persists (`localStorage` `sv-bookmark-index-pinned`); rows jump to the anchor (reusing the hook), disabled when layer-filtered, active row highlighted; mounted as a `.app-body` overlay symmetric to the left `IconRail`. The bookmark inline editor (`builtinNoteTypes.tsx`) gains a `category` input backed by a `<datalist>` of recent categories, saved via `note.edit`. The standalone `bookmark.list` pane stays registered + working.
- Verified: screenshot-verified collapsed strip → hover panel (`«` collapse + pin) with 3 bookmarks under "UNGROUPED"; then set `category="重要公式"` on one bookmark via the API and confirmed the index rendered two groups (重要公式 then UNGROUPED) END TO END before restoring the bookmark. tsc clean; `npm run test` **694** green (+ grouping tests; `bookmarkViews.test.tsx` still green).
- e2e NOT run during the build (ports held by the dev server); covered by the separate follow-up e2e pass. Pure `--sv-*` tokens, two themes.

## 2026-06-30 — Full e2e follow-up pass (post right-sidebar / R6 / R8)
- Ran the full Playwright suite (stopped the dev server to free 4177/5173; the suite boots its own isolated `.e2e-vault`, so `data/vault` is untouched). First run: **11 failed / 24 skipped / 13 passed**. All 11 failures were navigation-only fallout from the right-sidebar IA rework — specs reached the note list via the old collapsible-fold header `.note-list-head`, which is no longer mounted; the note list now renders as the always-open full-panel tab (`.note-list-panel-tab`) behind the "Notes" right-tab (not active by default). NOT product regressions (the row/jump/edit/delete selectors are unchanged).
- Fix: added a shared `openNotesTab(page)` helper (`e2e/helpers.ts`) — clicks `.right-tabs-tab` ("Notes"), waits for `.note-list-panel-tab`, idempotent — and replaced the obsolete fold-expand in the 5 affected specs (`note-edit-delete` ×3, `layer-as-lens` ×4, `loop` ×1, `streaming-chat` ×1, `adaptive-note-forms` ×2). `streaming-chat` re-activates the Notes tab after the reader-passage click switches the top group to the Anchor tab. No assertions weakened, no tests skipped.
- Result: full `npm run e2e` **24 passed / 24 skipped / 0 failed**; tsc clean. (Skips remain the pre-existing manual-creation/composer specs pending the note-creation UX decision.) Note: the IconRail "Notes" button maps to the `study`/chat kind, not the note list — the right-tabs "Notes" tab is the canonical path.

## 2026-07-04 — LIB-2 Library pane redesign (统一加号 + 容器分区)
- `npm run check`: tsc clean, 0 errors.
- Full `npx vitest run`: **166 files / 1631 tests, all green.** New coverage: `src/client/workspace/libraryView.test.tsx` (×19 — registry sections render in order with counts; recent capped at 5; collapse toggle + `sv-library-collapsed` persistence across remounts; fixture kit section registers/renders/unregisters; per-section empty-state guidance incl. 空文档区 → “点 + 导入第一份资料”; the NEW full 文档 list with derived type chips + chip filtering + active-row highlight; header search filters items across sections, updates counts, and shows the no-match line instead of import guidance; `+` menu renders the registry's 导入/新建 groups, built-ins run against the workspace ctx, the 网页 inline block drives setImportUrl/importFromUrl, a fixture create action appears under 新建, runs with ctx, and disappears after unregister; desktop-only items disabled on web with the hint; folder tree renders + per-folder close; refresh icon → loadSources; recent one-click open; en locale flip re-renders title/labels/empty text) and `src/client/i18n/i18n.test.ts` (×7 — zh default, setLocale flip, defineMessages identity, t() per locale, both-locale completeness, resolveText string/record forms).
- `npm run build`: green (pre-existing chunk-size warnings only).
- Concurrency note: the parallel session's in-flight edits (entityClient/settings/layer/study-layer files) were dirty in the working tree during the full run and ALL their suites passed — no external failures to report. None of those files were touched by LIB-2 (verified via `git status` before and after).
- e2e NOT run this pass. Library DOM changed: the header kebab menu and `.library-body-split` split layout are gone (replaced by the `+` menu, search, and stacked registry sections); `.library-panel`, `.source-item*`, `.folder-tree-host/close`, `.panel-menu-*` selectors are preserved. Any Library-specific e2e should be re-pointed in the next e2e pass.

## 2026-07-04 — X1-001 Windows desktop packaging (installer + updater + packaged smoke test)
- `npm run check`: tsc clean, 0 errors.
- Full `npx vitest run`: **166 files / 1633 tests, all green** (+2 over the LIB-2 baseline: `electron/shell.test.ts` gains the `resolveVaultRoot` case — packaged→`<userData>/vault`, `STUDY_VAULT_ROOT` wins, dev unchanged; `UserMenu.test.tsx` gains the 反馈问题 case — `window.open` spy asserts the GitHub issues URL + `_blank`, entry-id list updated to include `feedback`). Concurrency note: the parallel session's dirty files (design.md, entityClient, settings/layer/study-layer set) were in the tree during the run and every suite passed — no external failures to report.
- `npm run build`: green (pre-existing chunk-size warnings only). `electron:build:main` (now a FULL bundle: `--external` only electron/node-pty/claude-agent-sdk/codex-sdk/bufferutil/utf-8-validate instead of `--packages=external`) bundles clean, `dist-electron/main.cjs` = 4.7MB, zero esbuild warnings.
- **Packaged for real**: `npm run dist:dir` → `release/win-unpacked/` (591MB unpacked; app.asar 16.4MB; asar-unpacked node_modules = node-pty 2.8MB trimmed to win32-x64 prebuilds + claude-agent-sdk 3.1MB + its win32-x64 native claude.exe 215MB) and `npm run dist` → **`release/Growte Setup 0.1.0.exe` (152.6MB NSIS x64)** + `.blockmap` + `latest.yml` (sha512 feed) + `resources/app-update.yml` (github/tingxia1028/growHTML). Build-env caveat: electron-builder's zip extract→rename EPERMed persistently (a directory-handle holder on this machine) → switched to `electronDist` + `scripts/prep-electron-dist.cjs` staging (also keeps the dev launcher's branded Growte.exe out of the package without killing the live dev client).
- **Packaged smoke test (transcript facts)**: launched `release\win-unpacked\Growte.exe` with a clean env (no STUDY_VAULT_ROOT). Process tree up (4 procs), real window (`MainWindowTitle: GrowHTML`). Boot log at `%APPDATA%\Growte\logs\main.log`: `[boot] Growte 0.1.0 server http://127.0.0.1:58246 vault C:\Users\Jump\AppData\Roaming\Growte\vault`. Server proven from outside: `GET /api/health` → `{"ok":true,"app":"ai-study-vault"}`, `GET /api/about` → `{"app":"ai-study-vault","version":"0.1.0"}`, `GET /` → 200 with the built client HTML (served from inside app.asar). Vault created at the USERDATA path (NOT the repo): full `.study/` seed (manifest.json + all entity jsonl files) + `sources/ assets/ exports/`. Second run (post-`dist`) verified the updater end-to-end: reads `app-update.yml`, queries GitHub, gets "No published versions on GitHub" (none published yet) → logged `[warn] non-fatal`, app keeps serving. Packaged processes then stopped (dev-client Growte processes from `node_modules\electron\dist` left untouched) and the test vault/logs cleaned from `%APPDATA%\Growte` (the dir itself pre-existed — it is also the branded dev client's Chromium profile).
- `git status`: `release/` ignored (added to .gitignore); no build artifacts stageable. e2e NOT run this pass (web e2e untouched by X1; `e2e:electron` still boots dev-style via launch-electron — unchanged behavior, main.cjs boots identically unpackaged).

## 2026-07-04 — TRUST-3-001 回收站 (soft delete / recycle bin, data-trust.md TRUST-3)
- `npm run check`: tsc clean, 0 errors.
- Full `npx vitest run`: **184 files / 1834 tests, all green** (over the 182/1804 baseline: +`src/server/trash.test.ts` ×17 — DELETE-route soft-delete interception for notes/sources incl. orphan-anchor cascade markers, shared-anchor keep, stored-file kept on disk; restore roundtrips (note→anchors repaint, source→exact cascade, direct-deleted notes stay binned), 409 conflicts `source-in-trash`/`source-missing`, stale-layer pruning; purge one/all + 清空回收站 typed confirm; auto-purge with injected clock/retention + scheduler pass + `resolveTrashRetentionDays` env config; tombstones-ride-the-verbatim-export + live-read-exclusion guards; +`trashViews.test.tsx` ×8 jsdom — registry registration, typed sections + cascade counts + conflict hints, 恢复/彻底删除 via the IO/UI seams, typed-phrase 清空回收站 (cancel/mismatch/match), empty state disables purge-all, load-failure line, zh→en flip; +4 `snapshotStore.test.ts` tombstone cases — list/get exclusion vs listTrashed/getAny, compaction survival, delete()=purge, restore-by-upsert; UserMenu entry-list + dispatch tests updated for 回收站).
- `npm run build`: green (pre-existing chunk-size warnings only).
- e2e: `npx playwright test e2e/note-edit-delete.spec.ts` → **3 passed** (ephemeral-vault harness; assertions UNCHANGED — delete still empties the list and unpaints the highlight because live reads exclude tombstones).
- Concurrency note: the parallel session's dirty set (design.md, 00-research-and-plan.md, entityClient/ProfilePanel/SettingsHub/AiProvidersSection/styles.css, layer/study-layer/RightSidebarTabs/NoteListPanel.test/pluginManagerViews batch) was in the tree during the full run and every suite passed; none of those files were touched by TRUST-3 (`git status` checked before and after). UserMenu.tsx/WorkspaceShell.tsx were FREE this window — the 回收站 entry + side-effect import were mounted for real (no deferral needed).

## N1A-D2-001 (2026-07-04)

| Task | Type | Command | Expectation | Result | Status |
|---|---|---|---|---|---|
| N1A-D2-001 | Unit | npx vitest run markerOverlay/annotationDom/annotationMarkers/annotations/webviewSelection/webview-preload/anchorViews tests | Slot builders pinned (dedupe+count in note slot); layout() places anchor chip at rectsFor().first left / note chip at .last right (multi-line stub adapter + multi-span fallback); anchor-chip toggle hides note chip + hover/pinned/margin cards, restores, other anchors unaffected; global switch hides all anchor chips, note slots stay | 7 files / 121 tests passed IN ISOLATED WORKTREE against the staged slice | Passed |
| N1A-D2-001 | Guest bridge | npx vitest run electron/webview-preload.test.ts | Two-slot chips in guest overlay; in-guest toggle round-trip; sv:anchors {anchorGlyphsVisible} prefs mirrored; prefs-less push leaves visibility unchanged | Passed | Passed |
| N1A-D2-001 | Type check | isolated worktree (HEAD + staged slice) npx tsc --noEmit | 0 errors | 0 errors | Passed |
| N1A-D2-001 | Build | npx vite build · npm run electron:build:webview-preload | Client bundle + guest preload build; preload React-free | ✓ 1.26s · 39.2kb, 0 react refs | Passed |
