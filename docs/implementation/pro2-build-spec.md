# PRO-2 — 教回/口语 teach-back KIT (text-first) — BUILD SPEC (approve-with-changes)

Status: Plan → adversarial review → **APPROVE-WITH-CHANGES** (5 deltas folded). The flagship
proactive-learning experience: a register-only KIT + a runner panel + the "AI 装不懂 (Feynman)"
persona. Text-first (voice = PRO-3). Build on the codex-WIP-disjoint tree (PRO-2 touches
WorkspaceShell/presets/commandEntries/searchMessages/new-kit/teachback.css — codex holds
styles.css/SlashPalette/anchorViews/actionIcons; ZERO overlap).

## KIT-vs-core (the user's own framing: PRO's AI-native = an upper-layer KIT)
Register-only KIT `src/kits/teachback/`. The ONLY core touches are **4 precedented one-liners** (delta 1 — the review found the plan undercounted): (a) `WorkspaceShell.tsx` side-effect import of the panel (like review.panel WorkspaceShell.tsx:62); (b) a `presets.ts` node `{id:"teachback",kind:"teachback.panel"}` (like :75) so navigateShell resolves it; (c) a `NAV_COMMANDS` entry in `commandEntries.ts` (the list is STATIC in V1 :32-44, NOT registry-derived — so global-search launch needs this); (d) a `searchMessages.ts` string. NO rail icon (rail trimmed to 4). NO core evaluator edit (see delta 2). Everything else is register-only.

## Verified seams (cite): the `views` kit sink is a DEAD phase-3 collector (clientContext.tsx:191-193, kitViews[] unconsumed) → the panel MUST `registerView` directly (ReviewPanel.tsx:769 precedent). AI = STRUCTURED seam `generateKitContent→generateOperationContent→generateForPrompt` (structured.ts:66, single-shot, schema-validated :136; mock echoes `mockContent(input)` mockProvider.ts:97-98). profileContext gate is GENERALIZED (no allow-list; the old PROFILE_GATED_PROMPT_IDS is gone) → `applyProfileContextGate` strips for managed (ai.ts:97-105) inside generateKitContent — teach-back prompts auto-gated, `teachbackIo` clones reviewIo's gated `generateStructured` (reviewIo.ts:12), NO chat/leak route. `note.review` mode:"teach" = free `z.record` payload (memory.ts:53; ReviewPanel already writes {result,mode} :354). `getNoteType().render` is the one contract (noteTypeRegistry.tsx:63). weakReviewBuckets (queue.ts:116) returns [] on a fresh vault (no crash).

## THE 5 FOLDED DELTAS (MANDATORY)
1. **4 core one-liners, honestly budgeted** (not 2): WorkspaceShell import + presets node + `commandEntries.ts` NAV_COMMANDS entry + `searchMessages.ts` string. The palette list is static (commandEntries.ts:44 — registry-derivation is deferred SEARCH-2), so the command WON'T surface in global search without the commandEntries edit — the launch test would silently fail otherwise.
2. **Double-nudge → ship the teach-back trigger `enabled:false`** (opt-in, the 克制 default; a disabled trigger is rejected evaluate.ts:102). The evaluator HARDCODES `signals.reviewDue` (evaluate.ts:139-140) with per-trigger caps and no shared budget, so enabling BOTH review-push + teach-back on `reviewDue>=1` would double-nudge the same due state. PRO-2 does NOT edit the core evaluator; it ships teach-back's trigger off-by-default + a `04-decision-log` known-gap note: "enabling both double-nudges the same due state until PRO-3 adds a distinct `teachDue` signal (teach what you LEARNED today, not what's DUE — the design-faithful split)." The trigger still validates the wiring (fires when manually enabled → navigateShell(teachback.panel)).
3. **Fresh-user arming:** `enabled:false` (delta 2) already prevents auto-navigating a new user into an empty panel. PLUS delta 5's empty-topic state covers manual launch on a fresh vault.
4. **Mock determinism rule:** each prompt's `mockContent(input)` MUST be a pure, STABLE projection of its (growing-transcript) input (e.g. `teach.probe`'s mock returns a fixed-shape probe naming `input.weakestTopic`) so the panel drive-test asserts on intentional, not incidental, output.
5. **Empty-topic panel state:** when topic-pick yields `[]` (weakReviewBuckets empty on a fresh vault), the panel renders a graceful "还没有可教回的主题 · 先复习几道题" empty state (mirror ReviewPanel's degrade), NOT a broken runner.

## Session model (pure reducer `teachbackSession.ts`, no React — mirror ReviewPanel index/state)
`state = {phase, topic, turns:Turn[], round, wrapUp}`; phases `picking→posing→explaining→probing→assessing→done`; `TEACHBACK_MAX_ROUNDS=3` (REVIEW_SESSION_CAP idiom). pick = weakReviewBuckets-biased topic (empty→empty-state delta 5). pose (`teach.pose`) → `teachback.turn {role:"ai",kind:"pose",text}`. explain = student `<textarea>` (review-answer-input idiom) → `{role:"student",text}`. probe (`teach.probe`, transcript+profileContext appended) → next confused follow-up, bounded to MAX_ROUNDS. wrapup (`teach.wrapup`) → `teachback.summary {topic,explainedWell:string[],gaps:string[],summary}`. persist = `ctx.dispatch("anchor.add-note",{contentType:"teachback.summary",content,anchorIds})` + `recordMemoryEvent("note.review",subj,{mode:"teach"})` once. All AI via `teachbackIo.ts` (reviewIo clone + `setTeachbackIoForTests`).

## Persona seam: in `prompts/pose.prompt.ts`/`probe.prompt.ts` `build()` — "You are a CURIOUS, CONFUSED student (Feynman); you do NOT understand; ask the human to teach you; voice specific confusion; never lecture; answer in the student's language; return ONE {role:'ai',kind,text}." Append profileContext in the EXACT explain.prompt.ts:52-53 shape (→ auto-gated). `mockContent` returns a deterministic pose/probe (delta 4).

## Commits (each compiles + tested)
1. **contentTypes** — `src/kits/teachback/contentTypes.ts` (`teachback.summary` + `teachback.turn` hidden:true) + test (parse/createDefault/toSearchText). NEW type warranted (≠ mistake/review-pack semantics).
2. **prompt pack** — `prompts/{pose,probe,wrapup}.prompt.ts` + `index.ts` + test (persona text present; profileContext block iff present; mockContent validates against the spec; delta-4 stable projection).
3. **register** — `noteTypes.tsx` (render/edit) + `index.tsx` (ProductKit members) + `src/kits/index.ts` (+specs/+prompts) + `clientKits.tsx` (+teachbackKit) + test (`getNoteType("teachback.summary").render` returns element; `getKitPrompt("teach.pose")` resolves post installServerKits).
4. **session reducer + IO** — `teachbackSession.ts` + `teachbackIo.ts` (+setTeachbackIoForTests) + test (phase transitions; round cap; empty-topic→empty state delta 5; wrap-up assembles valid teachback.summary).
5. **runner panel + CSS + view reg + core one-liners** — `TeachbackPanel.tsx` (`registerView({kind:"teachback.panel"})` + `import "./teachback.css"`) + NEW `teachback.css` (NOT styles.css — codex) + WorkspaceShell import + presets node + RTL test (drive pose→explain→probe→wrapup with stubbed teachbackIo echoing mockContent; assert `anchor.add-note` contentType teachback.summary once + `recordMemoryEvent(note.review,_,{mode:"teach"})` once; empty-topic state).
6. **launch: command + trigger (enabled:false) + core one-liners** — `teachback.start` command (member ctx.commands) + `commandEntries.ts` NAV_COMMANDS entry + `searchMessages.ts` string (delta 1) + `teachbackTrigger.ts` builtin `enabled:false` navigate→teachback.panel (delta 2) + test (command dispatch → navigateShell(teachback.panel); a manually-enabled trigger fires → navigate; reviewPush.test.ts:93 idiom).
7. **e2e (deterministic mock)** — `e2e/teachback.spec.ts`: open panel → pick topic → type explanation → advance a probe → land a teachback.summary wrap-up note that renders.

## Deferred to PRO-3: 英语口语 roleplay + vocab-seeded topics; voice (SPEECH TTS/STT drop-in on the input row); a distinct `teachDue` signal (teach-what-you-learned-today) + enabling the trigger by default without double-nudge; actionRef:operation launch (headless focus host); making teachback.summary reviewable; event-triggered launch.

## Gates: tsc 0 · full vitest green (baseline 244f/2550t) · build · e2e (mock). Commit PRO2.N own sequence, don't push, don't touch docs (report TEXT blocks). Kit register-only + 4 honest core one-liners; adaptive-note contract for the wrap-up.
