# A4b — agent-loop CLIENT UI — BUILD SPEC (approve-with-changes)

Status: Plan agent → adversarial review → **APPROVE-WITH-CHANGES** (5 deltas folded, no blockers).
**Build AFTER W3 lands** (A4b builds ON TOP of W3 — W3 already touched entityClient/views/WorkspaceContext;
A4b's edits are symbol-disjoint additive). The tool-calling agent loop is SHIPPED server-side (A4a);
A4b adds the client transcript so the capability stops being invisible.

## Server contract (A4a — verified, cite)
- `runAgent` (`src/ai/http/aiSdkProvider.ts:213`) yields `AgentStepEvent` (`step`/`text-delta`/`tool-call`/`tool-result`/`done`; tool-error→in-band `tool-result{error}`; hard error→thrown). ONLY `AiSdkProvider` implements it (`capabilities.agentic/tools=true`).
- Route `POST /api/agent/stream` (`src/server/agent.ts:54`, wired app.ts:880). **SSE wire (exact — parse THESE names):** `event: step {index}` · `event: text-delta {delta}` (NOT `chunk`!) · `event: tool-call {id,toolName,argsJson,truncated}` · `event: tool-result {id,resultJson,truncated}` · `event: done {message,provider}` · `event: error {error}`. `argsJson`/`resultJson` are PRE-SERIALIZED JSON strings capped at `AGENT_EVENT_PAYLOAD_CHAR_CAP=4000` (agent.ts:16,44) — **truncated ones do NOT parse**. Route 501s `agent_unsupported` when `runAgent` absent (agent.ts:71).
- Tools (agentTools.ts:79-158): `search_notes`/`get_source`/`list_anchors` — ALL READ-ONLY. No write tool ships → no note created → adaptive-note contract does NOT apply. `createNote` tool is deferred.

## Design (verified sound)
- **Tool cards = a SEPARATE render-only `agentTurn` state on WorkspaceContext**, NOT `ChatMessage`s (those are `{role,content}` persisted verbatim by useChatSessions — untouched). Only the final `done.message` persists as one real assistant turn (via the existing `persistAssistant`/`appendAssistant` seam) → Add-as-note / regenerate keep working, tool cards never reach a persisted/exported surface.
- **Capability-gated "🛠 用工具" button** in StudyView `.chat-panel-actions`, shown only when active provider `capabilities.tools===true` (read `entityClient.aiProviders()` → match `active.id` in `providers[]`; a NEW WorkspaceContext read). No mode toggle. Falls back to plain chat (button absent) otherwise. 501→typed error surfaced once (no silent chat fallback).
- `entityClient.agentStream` mirrors `chatStream`'s SSE fetch + `\n\n` frame buffering (entityClient.ts:1122-1176, handles frames straddling read() boundaries) but dispatches the 6 agent events to typed handlers.

## THE 5 REQUIRED DELTAS (from the review — MANDATORY)
1. **mock-agent via COMPOSITION.** `answer()` is `private` on MockModelProvider — do NOT call it. Build `mock-agent` holding `new MockModelProvider()` and delegate `complete` + `stream` + **`completeStructured`** to it (byte-identical), adding a scripted `runAgent` + `capabilities` = mock's with `agentic:true, tools:true` (keep `structured:true`). Register in `src/ai/index.ts`.
2. **e2e provider pin is GLOBAL** (`playwright.config.ts:45` hard-pins for the WHOLE suite; no per-spec override in e2e/harness.ts). So flipping `STUDY_VAULT_AI_PROVIDER=mock-agent` runs EVERY spec under it. That is SAFE **iff delta 1 holds** (mock-agent forwards all THREE model methods byte-identically → streaming-chat.spec `You asked:` / chat-attachments `Attached:` / all structured-gen specs unchanged; no `instanceof MockModelProvider`/`id==="mock"` gate exists in src). PRIMARY: flip the global pin + **run the full e2e suite to PROVE non-perturbation**. FALLBACK (if any spec perturbs): give the agent e2e its own webServer/project keeping the rest on `mock`.
3. **agentStream event names + JSON.parse guard.** Use the six agent event names (esp. `text-delta`/`{delta}`, not chat's `chunk`) — a blind copy of chatStream compiles but silently drops text. The reducer must guard `JSON.parse(argsJson/resultJson)` — truncated payloads are non-parsing strings; render the raw string + a "…(截断)" badge on parse failure.
4. **W3 already landed** — build ON TOP, no rebase. A4b touches entityClient (agentStream, ≠ synthesize), views.tsx (`.chat-panel-actions` button + `.chat-log` transcript, additive siblings to W3's 生成文档 button), WorkspaceContext (agentTurn state, ≠ onSourceSynthesized). A4b does NOT touch registry.ts (the agent turn is invoked directly from WorkspaceContext, bypassing the command registry — sound: no persistence contract until `done`).
5. **User turn recorded exactly ONCE** (via the existing chat history seam `chatDomain.recordHistory`), reuse `buildChatContext()` (WorkspaceContext.tsx:1287) + `resolveAttachmentBundles` (:1318) exactly as `askAi` does — do NOT let the SSE `done` re-append the user turn.

## Commit sequence (each compiles + tested)
1. **`src/client/workspace/agentTranscript.ts` (+test)** — pure reducer. `AgentTranscriptItem = {kind:"text",content} | {kind:"tool",id,toolName,args,result?,error?,state:"calling"|"done"|"error",truncated}`; `AgentTurnState={items,status,error?}`; `reduceAgentEvent(state,event)`: text-delta→append trailing text item; tool-call→push calling item keyed by id; tool-result→find by id, set result, done (or error if parsed result has `error`); done→status done; error→status error. Guard JSON.parse (delta 3). Test: accumulation, keyed transitions, truncated-no-throw, interleave order, error states.
2. **`entityClient.agentStream` (+test)** — mirror chatStream fetch/buffer; 6-event dispatch table (delta 3); 501→typed `agent_unsupported` error (no chat fallback), 400→throw, mid-stream error→reject. Test: scripted SSE body drives handlers in order + resolves {message,provider}; 501/400/error paths.
3. **`src/ai/mockAgentProvider.ts` + register in `src/ai/index.ts` (+test)** — composition (delta 1); scripted `runAgent`: `step → tool-call(search_notes) → tool-result(rows) → step → text-delta(answer) → done`. Test: runAgent yields the scripted events; capabilities honest; `createModelProvider({STUDY_VAULT_AI_PROVIDER:"mock-agent"}).id==="mock-agent"`; delegates complete/stream/completeStructured byte-identical. Plus a server route test (`createApp({modelProvider:new MockAgentProvider()})` → POST /api/agent/stream emits tool-call+result+done frames).
4. **`src/client/workspace/AgentTranscript.tsx` (+test) + css** — tool cards (collapsed `🛠 {toolName}` + state glyph Loader2/check/alert; expand args/result pretty-printed if parseable, truncation badge; human one-liner for the 3 known tools; running spinner tail). Text items render through `ChatMessageBody role="assistant"`. Test (RTL): collapsed/expand, error state, truncated badge, running spinner, text via ChatMessageBody.
5. **WorkspaceContext wiring + StudyView mount** — `agentTurn` state + `runAgentTurn(text)` (recordHistory ONCE (delta 5) → setAgentTurn running → resolve context via buildChatContext+resolveAttachmentBundles → agentStream with handlers doing setAgentTurn(reduceAgentEvent) → on done persistAssistant(done.message) → on reject status error keeping items) + `agentAvailable` (from aiProviders() mount read); StudyView: gated button in `.chat-panel-actions` + `<AgentTranscript>` in `.chat-log`. Test (RTL StudyView fixture — reuse studyViewSessions/slashComposer harness): button hidden@tools:false / visible+wired@true; clicking with a stubbed agentStream renders tool cards then final answer; final message persists via the session seam.
6. **e2e `e2e/agent-tools.spec.ts`** — mock-agent driven (delta 2); seed a source, click 🛠 用工具, assert a `.agent-tool-card` for search_notes + a tool-result + the deterministic final answer bubble. Verify the full suite still green under the global mock-agent pin.

## Deferred: client-side turn timeout/abort (AbortController); persisting tool transcripts into sessions; a mode toggle / multi-turn agent conversation; vault-refresh-on-write-tool (no write tool yet); <ChatPanel> extraction. 

## Gates: tsc 0 · full vitest green · build · new e2e green + FULL e2e suite green under mock-agent (delta 2 proof). Commit A4b.N own sequence, don't push, don't touch docs (report TEXT blocks).
